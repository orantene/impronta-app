"use client";

/**
 * SurfaceSwitcher — the Agency | Talent surface toggle used across the Builder
 * Lab (Site Defaults, Connected, Site Starter Kit). It IS the shared
 * {@link PillToggle}; kept as a named re-export so existing imports/semantics
 * stay stable while every pill row renders through one component.
 */

export { PillToggle as SurfaceSwitcher } from "./ui";
