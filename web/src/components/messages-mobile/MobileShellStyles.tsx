/**
 * MobileShellStyles — load the mobile-shell stylesheet once per
 * surface root. Components that wrap their tree in
 * `<div data-msg-shell>` get the plan §19 acceptance rules applied
 * for free.
 *
 * Messages Consolidation Plan v2 — Slice I.
 */

import "./mobile-shell.css";

export function MobileShellStyles() {
  return null;
}
