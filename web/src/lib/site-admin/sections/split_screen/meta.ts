import type { SectionMeta } from "../types";

export const splitScreenMeta: SectionMeta = {
  key: "split_screen",
  label: "Split screen",
  description:
    "Two side-by-side panels: image + copy, or copy + copy. Optional reverse + sticky media.",
  businessPurpose: "feature",
  visibleToAgency: true,
};
