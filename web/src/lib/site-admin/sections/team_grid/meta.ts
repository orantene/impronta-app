import type { SectionMeta } from "../types";

export const teamGridMeta: SectionMeta = {
  key: "team_grid",
  label: "Team grid",
  description:
    "Headshots + name + role grid. For an in-house team page (separate from the talent roster).",
  businessPurpose: "trust",
  visibleToAgency: true,
  category: "showcase",
  inDefault: false,
};
