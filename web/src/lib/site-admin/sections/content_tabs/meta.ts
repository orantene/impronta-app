import type { SectionMeta } from "../types";

export const contentTabsMeta: SectionMeta = {
  key: "content_tabs",
  label: "Content tabs",
  description:
    "Tabbed panels with rich-text bodies. CSS-only — uses radio inputs + :checked.",
  businessPurpose: "feature",
  visibleToAgency: true,
  category: "story",
  inDefault: false,
};
