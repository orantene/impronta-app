import type { SectionMeta } from "../types";
export const eventListingMeta: SectionMeta = {
  key: "event_listing",
  label: "Event listing",
  description: "Upcoming events list with date / time / location / RSVP link.",
  businessPurpose: "feature",
  visibleToAgency: true,
};
