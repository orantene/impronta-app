import type { SectionMeta } from "../types";

export const editorialSplitHeroMeta: SectionMeta = {
  key: "editorial_split_hero",
  label: "Editorial split hero",
  description:
    "Two-column editorial hero — copy + CTA group on one side, media frame on the other. Configurable side, mobile order, overlay. Theme-neutral.",
  businessPurpose: "feature",
  visibleToAgency: true,
  category: "hero",
  inDefault: false,
};
