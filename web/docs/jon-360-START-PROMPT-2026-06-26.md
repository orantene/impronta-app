# START PROMPT — Build & harden the "Jon Inquiry 360" CRO program (Impronta/Tulala)

> Paste this whole file into a fresh chat. It contains your decisions, the verified state of the work, the defects you inherit, and a list of places the plan is provably weak. You are expected to break it further before you build.

## 0. Who you are and the bar
You are continuing a live, in-flight product workstream on the **Tulala/Impronta** multi-tenant talent-agency SaaS (Next.js + Supabase + Vercel; app code under `web/`; `main` is canonical/prod; live tenant improntamodels.com). **Ultracode is your default**: orchestrate with the Workflow tool, verify adversarially, optimize for the most correct and exhaustive result — token cost is not a constraint.

You are **not** a task-executor. You are expected to: audit this plan, QA the running app in a real browser, interact with the current build, challenge assumptions, and design better where the plan is wrong. Roughly half of this brief is *decisions to implement*; the other half is *a list of places the plan is provably weak* — go pressure-test those before you write code, and tell the owner what you found.

The owner's standing rules: **honest, non-inflated audits** (give a blunt score + a path, never soften; per-dimension gains are NOT the overall gain — don't sum lane wins into the headline); **deliver visible, QA-proven results** (judged by demonstrated UX on a real host, not tsc-clean commits); **dev/QA locally first**; **simplify by redesign, never strip features**; **no dead/fake CTAs** (an acceptance blocker); **no em dashes in user copy**; never say "buyer" or "cart" to clients (use "client"/"lineup"); accent via the `color.accent` token, never hardcoded gold; real imagery, never placeholder boxes.

## 1. Step one — earn your knowledge (verify; do not trust this prompt)
Build a verified mental model BEFORE coding. Read, then confirm each load-bearing claim against a real `file:line`:
- **Plans:** `web/docs/jon-inquiry-360-cro-plan-2026-06-26.md` (the 8-phase build), `web/docs/message-impronta-inquiry-sync-plan-2026-06-24.md` (the shipped feature), `web/docs/message-impronta-handoff-2026-06-26.md` (handoff + integration §2).
- **Memory:** `~/.claude/projects/-Users-oranpersonal-Desktop-impronta-app/memory/project_message_impronta_inquiry_sync_plan.md` (full running log) + `MEMORY.md` index.
- **Shipped code:** everything under `web/src/app/t/[profileCode]/_chat/`, the two mounts, `web/src/app/t/[profileCode]/_actions/`, and `web/src/lib/inquiry/` (esp. `inquiry-lifecycle.ts`, `inquiry-engine-submit.ts`, `guest-reply-latency.ts`).

**Where this prompt and the code disagree, the code wins — surface it to the owner.** Every assertion below was verified by a recon pass on 2026-06-26, but the tree moves; re-confirm.

## 2. Locked product decisions (owner-approved 2026-06-26 — these answer the 4 open questions)
1. **CTA label = LIFECYCLE-AWARE** (this *overrides* the earlier static "Book Now"). The launcher pill label changes with inquiry state: empty → **"Message {agency}"**; draft → **"Your lineup (N)"**; sent → **"Inquiry sent"**; replied → **"{agency} replied"**. Rationale: the label does real work at every state instead of one transactional word, which resolves the audit's standing fake-CTA flag + house rules + binding spec. This is **coupled to the resolver** (see Phase 3 / §3) — the label is one output cell of the cross-surface CTA matrix, not a hardcoded string.
2. **Old InquiryDrawer = KEEP AS THE SYNCED FORM VIEW** (demote, do not delete). Make it the expanded form view of the *same* synced inquiry record; remove **only** the guest instant-submit path that silently drops the lineup. Never strip features.
3. **RECEIVED trust beat = NAME + FACE + REPLY ETA, but tiered and with a hard prerequisite.**
   - **Prerequisite (this is a real bug, not a nicety):** coordinator assignment lives only in `submitInquiry`/`resolveInquiryCoordination` (`inquiry-engine-submit.ts:340`), reached via `createInquiryFromIntent`. The shipped early-row→send path (`use-mini-chat-send.ts` `continueEarlyInquiry`) does **not** call it, so `coordinator_id` stays **null for the common path**. Wire coordinator assignment into the draft→submitted promotion, or the trust beat is vaporware.
   - **Tier the card by what's actually true:** (1) coordinator assigned + ≥3 reply samples → name + face + ETA; (2) coordinator assigned, no samples → name + face, **no** time promise; (3) no coordinator yet → "{agency} has your inquiry. We're assigning a coordinator now." **Never** a placeholder face.
   - **The ETA is honest or it's gone.** `getTypicalReplyLabel` (`guest-reply-latency.ts`) is a *tenant/talent population median over 50 inquiries*, returns null below 3 samples, and `_formatLatency` emits an em-dash string `"within 2–3 days"` (house-rule violation). Do not pin a population median next to a specific human's face as a personal promise; fix the em-dash; decide the 0-sample (every new tenant) behavior.
4. **How the work reaches production = NOT a direct PR to main.** See §6. Short version: merge PR #690 into `feat/bl10-p5`; build the 360 work stacked on `feat/bl10-p5`; land on `main` only via the curated `ship/bl10-p5-onto-main` branch. Keeping the owner's uncommitted profile-template WIP isolated (worktree) is a good instinct — keep it.

## 3. Verified current state — what's BUILT vs what the 360 plan still needs
**Built and working (the shipped Message Impronta feature):** `use-unified-inquiry.ts` (single-record debounced `patch()`, `promoteContact()`, `contactPromoted` send-gate, per-field + panel `syncState`); early-row create + chip capture (`guest-chat-actions.ts` `ensureGuestChatInquiry`, `guest-detail-chips-actions.ts` extended `captureGuestChip`); inbound realtime reconcile (`use-guest-detail-reconcile.ts`, 2s self-echo grace); send pipeline (`use-mini-chat-send.ts` + `SendToAgencyBar` + `SyncStatusBar`, contact-gate-before-send); launcher-as-cart (`TalentProfileChatLauncher` + `LauncherAvatarStack` + `FlyingAvatar`/`use-fly-to-rail` + registry/preload); cold-load portrait backfill (`use-resolve-cart-portraits` + `resolveGuestCartPortraits`); collapsible details rail (`InquiryDetailsRail`/`InquiryDetailRow` + reused editors); i18n en/es/fr via `createTranslator` + `public.guestChat.*`. The lifecycle enum **already has `draft`** and a `draft→submitted` transition (`inquiry-lifecycle.ts`).

**Still TODO (by 360 phase — none built yet):** Phase 1 = early row as `status:'draft'` + send-time promotion + admin draft filter (no migration). Phase 2 = `InquiryReceiptCard` + SENT airlock transition + humanized coordinator header. Phase 3 = pure `web/src/lib/inquiry/inquiry-context-resolver.ts` (+ tests) + cross-surface CTA matrix + collapse the competing inquire front doors. Phase 4 = `DraftPrivacyBanner` ("Draft. Only you can see this.") + next-actor status strip. Phase 5 = projects model (multi-inquiry switcher, project labels, auto-park). Phase 6 = route rail remove through `patch()` + undo + lineup recap. Phase 7 = craft/motion/mobile/dark + the a11y fixes below. Phase 8 = cross-session "Finish your inquiry (N)" + replied pulse + abandoned-draft cron.

## 4. Table-stakes defects you INHERIT (confirmed in code 2026-06-26 — fix as baseline before craft)
1. **Rail/avatar X-remove self-echoes "the agency updated talent" + desyncs the form** — `TalentProfileChatLauncher.tsx:103-128` (`onCaptureChip` at 119-127 bypasses `useUnifiedInquiry.patch()`, so it never enters `savingKinds` and the reconcile guard treats it as a remote edit). Fix: route removal through `patch()` (this is Phase 6).
2. **Cart-portrait backfill capped at 24** — `default-storefront-roster.ts:66` (`.limit(24)`) + `guest-roster-actions.ts:152-159` + `use-resolve-cart-portraits.ts:65` (marks ids "attempted" *before* the lookup, so a capped miss never retries → permanent initials). Fix: uncapped targeted by-id query; mark attempted only after a real lookup.
3. **Inputs `outline:none` with no focus-visible replacement** — `mini-chat-styles.ts:292-304` (302). WCAG 2.4.7. Inline styles can't express `:focus-visible`; needs a class or focused-state branch.
4. **Active toggle = raw accent text on accent tint (~2.27:1)** — `GuestDetailChipEditor.tsx:104-117` (color at 114, `${accent}18` bg at 110). WCAG 1.4.3. The fix helper `accentText()` already exists (`mini-chat-styles.ts:139`) and the sibling `GuestDetailChips.tsx:245` already uses it — just apply it here.
5. **`remoteNoteFor()` remote-change notes hardcoded English** — `mini-chat-panel-helpers.ts:127-149`. Thread `t()` (distinct from the deferred server-stored note bodies).
6. **Early row inserts `status:'new'` (should be `'draft'`)** — `guest-chat-actions.ts:1633-1648` (1639). `'new'` maps to phase `submitted` (`inquiry-lifecycle.ts:98`), so half-assembled private lineups already render as live admin threads. Fix = `'draft'` + send-time promotion + admin draft filter; no migration (enum has `draft`). This IS Phase 1's P0.

## 5. The challenges — answer these before/while building (do not skip)
1. **Prove the trust beat has a backer.** Open `guest-chat-actions.ts ~1635` and `inquiry-engine-submit.ts:340`. Show the exact path by which an early `draft` row, promoted at real send via the SHIPPED `continueEarlyInquiry`, gets a `coordinator_id`. If it doesn't, "Maria has it" is null for the common path. Wire it or cut it.
2. **The lifecycle is only half-encoded.** `CANONICAL_EDGES` has no edge back into `draft` and maps legacy `new→submitted`. How does the admin "drafts filter" distinguish a true `draft` from the thousands of legacy `new` rows that already read as `submitted`? Write the exact query + the backfill/no-backfill decision.
3. **Split-brain durability.** The lineup is localStorage-only (`public-discovery-state.tsx:161`); the draft inquiry is server-side by guest cookie. Walk through EXACTLY what Jon sees when (a) he drafts on phone then opens laptop, (b) Safari ITP evicts the guest cookie after 7 days, (c) he logs in mid-draft. If any answer is "his work is gone" or "403", you have not satisfied never-destroy. Which store is the source of truth, and why?
4. **A CRO plan with zero events.** Name the single conversion metric, the full event taxonomy with keys, the baseline, and the holdout/flag design — BEFORE writing UX. If you can't state how you'd prove a 1% lift, you can't claim one. (Amplitude is available.)
5. **The ETA is a population median next to a human face.** Justify it, or fix the honesty tiering. What does a 0-sample new tenant show? What happens to trust the first time the real reply lands 2 days after the card said "~2 hours"? And kill the em-dash in `_formatLatency`.
6. **The 8-state resolver has no failure/terminal states** (rejected/expired/archived/talent-unavailable/send-failed/duplicate). A declined inquiry will show "awaiting reply" forever — the dead-CTA class this codebase keeps catching. Add them; write the test that forbids a forward-motion CTA on a dead inquiry. Also: `sent_awaiting` vs `live_conversation` must resolve off `coordinator_id != null && lastMessageBy === 'coordinator'`, not phase alone.
7. **Multi-tenant lineup breaks the single-{agency}/{Maria} invariant.** On the hub host a lineup can span agencies. Whose coordinator/agency/ETA fills the one RECEIVED card? Define the multi-agency behavior (recommended: fan out into per-agency sub-inquiries at SEND, one receipt card each) or gate the beat to single-agency lineups.
8. **Realtime self-echo + optimistic-lie flicker.** Memory already records a "spurious agency-updated" echo patched with a 2s grace hack. Your optimistic airlock flip + inbound reconcile multiplies it. Specify idempotency keys, the single-flight guard, and the write-ordering for auto-park's `clearSavedIds`-then-server-patch so a failed patch can never silently destroy the lineup.
9. **Rebase FIRST, then re-enumerate the front doors.** `main` is far ahead with NEW inquiry entry points the plan never mentions (#682 `directory-inquiry-review-bar.tsx`, #692 `?inquiry=open`, #691 `directory-inquiry-sheet.tsx`). "Collapse 3 front doors" is now 4+. The Phase 3 file/line refs in the plan are stale.
10. **Don't pile motion on unmet a11y/i18n debt.** The predecessor re-audit left a11y at 72 with the WCAG 1.4.3/2.4.7 + EN-hardcoded items in §4 above. Fix those as table stakes before "felt quality 7→9" craft.

## 6. Better ideas to weigh (from the adversarial pass — argue with these, don't just adopt)
- **Make the server draft row the single source of truth for the lineup; demote localStorage to a cache** hydrated from the server on load. Collapses the split-brain (cross-device, cookie-eviction, guest→login) into one reconcile and makes never-destroy a single server transaction.
- **Tier the coordinator/ETA card by truth** (see §2.3) instead of always promising a time; forces the real fix (coordinator on promotion).
- **Ship instrumentation as Phase 0**, gate the whole arc behind one flag, pre-register `contact_promoted` as the primary metric (it's the airlock). Turns "Needle:" prose into testable claims.
- **Replace the one-way SEND freeze with a reversible recall window** (60–120s, before any agency view; needs a guarded `submitted→draft` edge) + a first-class "add talent to a sent inquiry" the coordinator one-taps. Restores Jon's control honestly.
- **Add terminal/failure states to the resolver** and derive every sent-state CTA from live `phase + coordinator_id + last message`.
- **Split a cross-agency lineup into per-agency sub-inquiries at SEND**, each with its own coordinator/agency receipt card (reuses the projects model).

## 7. How the work reaches production (verified topology 2026-06-26 — do not improvise)
- PR #690 (`feat/message-impronta-unified-inquiry` → `feat/bl10-p5`) is **MERGEABLE/CLEAN**. The 360 feature commits ride cleanly on `feat/bl10-p5`.
- **Step 1:** merge PR #690 into `feat/bl10-p5`. **Step 2:** build the 360 work as a new branch off `feat/bl10-p5`, PR back into `feat/bl10-p5`. **Step 3 (integrator):** land on `main` only via `origin/ship/bl10-p5-onto-main` — rebase ship onto latest `main`, then cherry-pick/rebase-onto the clean `feat/bl10-p5` commits. **Step 4:** gate `cd web && NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit && npm run lint`. **Step 5:** PR ship→main. **Step 6:** after deploy, `cd web && npm run db:check` then `npm run deploy:smoke`.
- **TRAPS (verified):** Do NOT PR `feat/*` directly to `main` — `feat/bl10-p5` is ~51 commits behind and conflicts on `edit-chrome/topbar.tsx`, `web/package.json` (CI manifests), `snapshot-tree.test.ts`. Do NOT carry `ed5bfe2e8` (a merge-of-main-into-feature → diamond history) or `14a1301aa` (a WIP chore = 330 mockup files + deleted templates + `.local/bin/stripe` + `.claude/launch.json`) onto `main`. Do NOT update ship by *merging* `feat/bl10-p5` into it (pulls both noise commits) — cherry-pick/rebase-onto. Do NOT assume ship already contains the 360 work — it does not. Do NOT skip `db:check`/`deploy:smoke` (PR #693 on main needs a migration).
- **Do not touch:** `inquiry-permissions.ts`, favorites (`client_favorites`), the submit engine internals beyond the coordinator wiring above, or the owner's uncommitted profile-template WIP. No migration is needed for the 360 plan.

## 8. Working method, QA, gotchas
- **Orchestrate with Workflows (ultracode):** understand→design→synthesize, then implement→verify, with per-task models (Sonnet for mechanical, Opus-high for load-bearing/adversarial) and a final verify agent that runs tsc+lint and self-repairs. Adversarially verify findings before you trust them.
- **Dev/QA locally:** `preview_start "Next.js Dev Server"` (port 3000) + `preview_start "Impronta storefront (impronta.lvh.me)"` (port 3114 host-proxy). QA at **http://impronta.lvh.me:3114/directory**. Server idles → restart; routing/compile wedged → `rm -rf web/.next` and restart.
- **Browser QA:** use the **Chrome MCP** (claude-in-chrome) for deep React/chat state (preview_eval can't drive it). The pill's hit area is partly under the avatars — click the label text. **Interact with the current build** (add talent, watch the fly-to-pill, open the chat, edit a field, send) and QA the real flow, not fixtures.
- **DB verify:** Supabase MCP, project `pluhdapdnuiulvxmyspd`. **Admin QA:** `qa-admin@impronta.test` via `http://localhost:3000/api/dev/signin?email=qa-admin@impronta.test&next=/impronta/admin/messages`.
- **tsc:** `NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit` (OOMs otherwise). Errors only in `.next/dev/types/*` → `rm -rf .next/dev/types` and re-run.
- **Watch for:** smart/curly-quote corruption introduced by edits (convert to straight ASCII); 800-line `max-lines` cap (split god-files); transient workflow rate-limits (resume the run); classifier "temporarily unavailable" on browser/destructive actions (retry; read-only ops unaffected). `verify:server-actions` must stay green; never re-export non-async from `"use server"` files.

## 9. Your mandate
Don't just execute the 8 phases. **Audit the plan, QA the running app in Chrome, interact with the current build, find what's broken or missing, and propose better designs where the plan is wrong** — then build the strongest version, verify it adversarially, and prove it with real-browser QA + DB checks. Start by confirming §1, answering the §5 challenges, and fixing the §4 table-stakes; then build Phase 1→2 (draft boundary + the coordinator-backed RECEIVED beat), then Phase 3 (resolver + collapse front doors after rebase), then the rest. Report honestly — blunt scores, a clear path, no inflation.
