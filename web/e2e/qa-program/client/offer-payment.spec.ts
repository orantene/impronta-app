import { expect, test } from "../_harness";

test.describe("QA 6.3 client offer/payment (deferred deep)", () => {
  test.skip(true, "Requires seeded offer + payment card on a fixture thread; covered after admin offer send is green");
  test("placeholder", async () => {
    expect(true).toBe(true);
  });
});
