import { expect, test } from "../_harness";

test.describe("QA 6.3 client cards (deferred deep)", () => {
  test.skip(true, "Requires choices/times/confirmation/tickets seeded cards");
  test("placeholder", async () => {
    expect(true).toBe(true);
  });
});
