# Services rebuild execution status (2026-09-24, round 2)

Port 3000 only. Branch `feat/services-rebuild`, upstream `origin/main`. No PR.

Home page `bad420b5-13cc-45ab-a915-841e775b1a7c` still matches restored revision `79ef2414` (sha256 prefix `6d83ddc965`, no `services_catalog`). The named token `f8156c9405` is not that digest.

| Item | Result |
|---|---|
| Commits + rebase | Done. Upstream is `origin/main`. |
| Widget proof | Slots API called, empty window, no booking written. Extras total, no-extras jump, consult pending, Spanish, 390, and the live comparison are in `evidence/`. |
| Category note | Group note / `selling_defaults.categoryNotes`. The manicure line is static copy, not a column. |
| Near-match | `categoryNearMatch` on Organize and the editor. Not re-shot this round. |
| Ship | Coming soon. Checkout does not charge it. |
| Defaults | Talent currency. Buffer does not change slots; the screen says so. |
| Add many / editor card | "+" opens the portfolio picker. Preview card shows name, city, and last saved when `updated_at` is present. Not shot: the talent shell did not hydrate. |
| Schema | `db:check` clean. `first_published_at`, `category_order`, `selling_defaults`, `talent_addon_groups` exist remotely. |
| Gates | typecheck 0, lint 0, builder-node bindings 1802 pass, billing 679 pass. |
| Left for the owner | Live band swap, PR, guest access-link, slot buffers. See REPORT.md. |
