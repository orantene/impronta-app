# Phase-boundary QA list — the owner clicks this ONCE

> **New rows go in [`qa/<area>.md`](qa/README.md), not in this table.**
>
> This file is one flat table that every area appends to at the same spot, so it conflicts exactly
> as often as it is used — one Events PR hit the same conflict twice in an hour on 2026-09-05, and
> four open PRs across two departments append to these same lines today.
>
> **Nothing here was moved.** Splitting the table would have conflicted every open branch that has
> rows in it, including other departments' — so the fix is additive only: new per-area files, plus
> this paragraph at the top, where nobody appends. The list below stays valid and is still the one
> the owner runs. It drains as areas re-touch their own rows.

**Status:** seeded by the CEO 2026-09-04. This file did not exist; managers were being told to
"batch QA" with nowhere to batch it to.

## The rule
Managers do **not** ask the owner for click-verification per slice. Every item that genuinely needs a
human with a browser or a phone goes here, with **exact steps and the exact thing that would falsify
it**. The owner runs this list once at the phase boundary. A slice ships on tests and CI; it is
**not called finished** until its line here is ticked.

Write items so someone who has never seen the code can execute them. One line per item:
`| area | what to do | what proves it | what would falsify it |`

## Items

| Area | Do this | Proves | Falsified by |
|---|---|---|---|
| Events E8 | Buy a ticket, open the receipt on a **real iPhone** (Safari or installed PWA), scan the QR with the door tool | Camera scan works on the device the owner described | Anything green from the iOS Simulator or desktop Chrome — the Simulator **has no camera** and proves nothing |
| Events E8 | Scan the **same** QR a second time | Goes red, "already scanned" | Green twice |
| Events E8 | Refund a ticket, then scan it | Goes red **with the reason** | Admits, or red with no reason |
| Events E8 | Sell one admission at the door as **cash** | Admission recorded, seat consumed, **zero commission**, stated plainly in the UI | A fee appears, or the seat is not held |
| Events E9 | Publish an event with one **booked** act and one **quoted** act. Open the public event page, then the quoted performer's own public page | Only the booked act appears in either place | **The quoted act appears anywhere public.** This is the highest-harm item in the area and it does not look like a bug: it looks like a venue announcing someone. It damages that performer's leverage on a fee still being negotiated, and their reputation if it falls through, and it is unrecoverable once indexed |
| Events E9 | On the same event, set "set times public" **off** | The acts are still listed; their times are gone | The act disappears entirely — a venue with an unfixed running order still wants the poster, and a wrong time is worse than no time because people plan an evening around it |
| Events E2 | **NOT EXECUTABLE UNTIL THE RAIL SLOT LANDS** (no `events` segment exists in `WorkspacePage` or `WORKSPACE_PAGE_SEGMENTS`). When it does: sell some tickets on a tier, then **rename** that tier | The sold seats stay attached and the remaining count is unchanged | The count resets or the sold seats detach. The pool is keyed on an immutable `pool_key`, never the label, and this is the row that proves it |
| Events E4 | On a tenant host, open `/events` and then `/events/<slug>` for a **published** event; then try the same URLs for a **draft** event | The published one renders; the draft returns 404 on both pages | The draft renders — two independent guards (the query filter and the RLS policy) both have to be wrong for that, and if it happens, one of them is not there |
| Events E4 | Open `/events/<slug>` from a browser in a **different timezone** from the venue (or emulate one) | The date and doors time shown are the venue's, not the browser's | The night named changes when the viewer's timezone changes — an instant formatted without a named zone silently becomes the reader's wall clock |
| Events E4 | On a tenant host, request `/review` and `/register` | Both still resolve exactly as before | Either 404s — `/r` was registered as a prefix and must not shadow them; `hasPrefix` is segment-aware, and this is the row that proves the belief rather than states it |
| Events E5b | Buy tickets for a session-backed event and let the Stripe webhook settle the order. **NOT EXECUTABLE UNTIL GUEST CHECKOUT EXISTS.** When it does: buy 2 GA and 1 VIP table for 6 | `admissions` has **three** rows — two of `party_size` 1, one of `party_size` 6 — and `admissions_mint_shortfall` is **empty** for that order | Six rows for the table, or one row for the two GA tickets, or a shortfall row: the first two are the row-count-vs-people confusion, the third means the mint did not run and the reconciler caught it, which is the design working and still a failure to report |
| Events E5b | Re-deliver the same `payment_intent.succeeded` webhook (Stripe dashboard → resend) | Still exactly three admissions; no duplicates | Six admissions — `(order_line_id, line_seq)` is unique precisely so a retry conflicts away, and if this doubles, the idempotency is not where it was claimed to be |
| Events E8 | Scan a ticket, then **re-issue it** (transfer / resend, once that exists) and scan the **old** QR | The old QR goes red with **"superseded"**; the new one admits | The old QR admits — its signature is genuine, so only the row's `token_version` can refuse it, and `check_in` is the only place that comparison lives |
| Events E8 | On `/admin/events/door?session=<id>` for a session with a **party of 4** admission, tap **Admit 4**; then tap the same row again | First: green, "In — 4 of 4". Second: the button is gone and the row reads "In"; a re-scan of its code goes red "Already scanned — 4 of 4 in" | A second admit succeeds, or the screen goes green from anything other than `doorAdmits(outcome)` — the tap path and the scan path must land on the same `check_in` under the same row lock, and this is the row that proves neither surface decides on its own |
| Events E6 | **NOT EXECUTABLE UNTIL A PUBLIC TICKET PATH EXISTS** (`/events` is gated on the `surface-allow-list.ts` decomposition). When it does: apply a $5-off code to a **$3** ticket | The order is free and the UI says the code covered the whole order | A checkout error. `orders` has `CHECK (total_cents >= 0)`, so an unclamped discount fails the insert and the buyer sees an error instead of a free ticket |
| Commerce | Platform Admin → Commerce → Commission: change the take, the fixed floor and the buyer/seller split, save, then run one real checkout | The saved value actually reaches a live charge | The page saves but the charge uses the old number |
| Tenant pages | Open a `/w/<slug>` tenant page **in a browser** and interact | The hydration SEV-1 is really closed | An error card after a correct-looking server render — no curl, smoke test or crawler can see this |
| Reservations | Book a table end to end as a guest, then check it in at the host stand | One scanner, one RPC, works on a table | A second scanner exists |
| Reservations | On El Paisa's **hand-composed** page, book: party, date, time, name, email | The `reserve_table` island renders and reserves on a REAL page | The island is "complete and callable" but was never rendered — five recorded instances of *documented as wired, resolves to nothing* |
| Reservations | Tap the party stepper fast, 2 → 6, and watch the times | The list shown matches the party finally selected | Times for a party you are no longer booking — a slow response landing after a fast one |
| Reservations | Tap **Reserve** twice quickly | One booking, not two | Two bookings, or a dead button that does nothing on retry after a refusal |
| Reservations | Force a capacity refusal | Copy says **"Nothing was booked"** in en and es | Any wording that leaves a guest unsure whether they double-booked |
| Reservations | Settings → Reservations: set party 1–8 and card-on-file from 6, save, reload | Both numbers survive; unchecked card-on-file reads **"never"**, not 0 | A workspace with no venue renders an empty form instead of saying so |
| Reservations | Load any admin page on a tenant that has **never had a venue** — most of them. Watch the network panel and the layout's server timing. | The Reservations rail item is absent AND the page costs **no extra database round trip**: `takes_reservations` is a column on the tenant row `loadTenantIdentity` already selects | A new query to `venue_service_rules` or `venues` in the layout fan-out · admin pages measurably slower for tenants with no venue · the item shown to a workspace that has never taken a booking |
| Reservations | Settings → Reservations: toggle "Reservations are on", save, reload an admin page. Toggle back, save, reload. | The rail item appears and disappears with the toggle, with no extra query either way | The item lags a page load behind the setting (the trigger did not fire) · it survives deleting the last venue · **anything gating ACCESS on `agencies.takes_reservations` instead of reading `venue_service_rules`** — that flag draws a link and must never decide what somebody may do |
| Sessions | **No UI creates a series yet** (P1.3b). Insert one `session_series` row with a **confirmed** `timezone`, then `curl -H "Authorization: Bearer $CRON_SECRET" .../api/cron/materialise-sessions` — the cron is daily at 04:20 UTC, so waiting is not a step. Then open the workspace **Calendar** | Occurrences appear on their **local** day, prefixed `Class:` (`Clase:` in es) | A session renders unlabelled and indistinguishable from an inquiry; or appears a day out; or does not appear at all because the series had no confirmed timezone and the sweep refused it — which is correct behaviour and looks identical to a bug from the Calendar |
| Sessions | **NOT HUMAN-QA-ABLE YET, and that is the finding.** The DST collision refusal names what it collided with, but only into `improntaLog` — there is **no operator surface at all**. Verify by reading the sweep's structured log for `sessions.cron.materialise_refused_occurrence` after materialising two series an hour apart across a spring-forward date | The log line carries `collidesWithTitle` and `collidesWithSessionId` and a `detail` sentence | **The row cannot be executed by a human as written.** An operator whose class silently did not appear has nowhere to look. Needs a surface before it is a QA row rather than a log check |
| Cross-area | On a **non-UTC** venue, put a **session** at 20:00 in Cancún (01:00Z next day) and another at 01:00 in Madrid (23:00Z previous day), then compare against a **booking** at the same local times | Both sessions land on their own local day; the bookings are expected to be **a day out** | The sessions land a day out — that half is fixed. The bookings landing correctly would mean someone fixed `ymdFromInstant` and did not say so. Sessions use `localYmd`; bookings, holds and orders still slice the UTC date off the ISO string, which is Dashboards' to fix and is deliberately unfixed here |
| Orders 0.10 | Open `/<tenant>/admin/orders` signed in as a **non-owner** staff member (any role with `view_dashboard`, e.g. manager) | The desk is gated at the operational level, not owner-only | A 404 for someone who can see Bookings — that means it is gated at `manage_billing` and front-of-house cannot work orders |
| Orders 0.10 | On a tenant with at least one order, click each bucket tab (All / In progress / Awaiting payment / Paid / Cancelled and refunded) | Row counts change and the totals strip changes with them | The totals stay the same while the rows change — that means a figure beside a filtered list is describing something wider, which is how someone acts on the wrong number |
| Orders 0.10 | Find an order that is **part paid** (a deposit taken, balance outstanding) | "Still owed" shows the balance, and it matches what the same order's transactions add up to | The desk and the order status disagree — that means the desk's `collected` sum has drifted from the rule in `complete-order.ts`, and staff will chase a customer who already paid |
| Orders 0.10 | Reach the desk **only by clicking**, from Analytics → Money → "Open the orders desk". Do not type the URL | There is a real door | Nothing links to it, or the link 404s — an engine with no door |
| Orders 0.10 | Switch the workspace to Spanish and reload the desk | Every label is Spanish, including the empty state and the bucket tabs | Any English string, especially in a state you have to work to reach (empty, unavailable) |
| Orders 0.10 | **Hardest and most valuable:** get the desk to fail its read (kill the network for the request, or hit it while Supabase is throttled) | It says "We could not load your orders" | It says **"No orders yet"** on a workspace that has orders. That is the whole reason the loader refuses instead of returning an empty list, and it is the one failure a screenshot of the happy path can never rule out |
| Orders 0.7 | Set a tenant's words row for `menu.order` to **"Quote"**, reload a thread that has an order, and read the card title | The card follows the tenant's own vocabulary instead of the word "Order" | The title still says "Order" — the noun is resolved once at load and put ON the order, so a stale title means the wrong loader ran (there are TWO message producers, and only one was wired the first time) |
| Orders 0.7 | Open a thread with an order as a **client**, then as **staff** | The client sees **Pay now**; staff never do, and staff can add lines only while the order is draft/quoted | A Pay-now button in front of staff, or an add-line control on a paid order |
| Orders 0.7 | Find an order that is **part paid** and read the card | It shows what is **still owed**, not the total | It shows the full total on a part-paid order — a customer reads that as "my deposit did not register" |
| Orders 0.7 | Make the card's order **fail to load** (bad id, or kill the request) | The card renders a neutral "unavailable" state | It renders **$0.00** — a price of zero is a claim, and this one tells a customer their order is free |
| Menu | On the live tenant, set the **Posing course** stock to **0** in Menu → the item's Stock field, then open the public menu board **in a normal browser tab** (not the builder preview) | The item renders a **Sold out** badge, its + stepper will not increment, and submitting refuses by name before any network call | The item looks live and orderable. **This is the last gap in the oversell story:** the engine already refuses the thirteenth sale, so a customer clicking a live-looking item is refused **at checkout** instead of seeing it greyed out. A test cannot cross this hop — the badge reads a **server-rendered snapshot** of pool stock, so only a real page load proves the deployed board sees the pool |
| Menu | Set that item's stock to **20** while at least one order is holding units, then reload the editor | The field reads **20** with a quiet **"N held by open orders"** beneath it | The field reads **23** (available + held), or reads 20 with no held line. Either is how an operator "fixes" it by typing 23 and oversells. A test proves the RPC arithmetic; only a human proves the **field shows what the RPC returned** rather than what was typed |
| Menu | Place a menu order **signed out**, on an item whose offering allows pay in person | The confirmation says the order is **pay in person**, and **no card payment request appears** anywhere in the thread | A card request appears. A guest **cannot complete** one today — both payment actions require a session, the pay sheet mounts only inside the account dashboard, and the guest thread renders payment cards read-only. This shipped broken once (#1528); the falsifier is the customer having a button that cannot work |
| Menu | Switch the workspace's industry preset to **Restaurant** in Settings → Words, then reload the public menu board | Board copy reads the operator's noun — **"In your order: 3 dishes"** — matching the chat and the rail | A literal **`{nounPlural}`** on screen (the words load failed and substitution was skipped), or the board saying "items" while another surface says "dishes". Only a human can see the **two surfaces disagree**; each passes its own test alone |
| Appointments | Set a capacity pool's hold TTL to **15 minutes**. Start a booking through the **slot picker** and abandon it; separately have staff **propose a time** on an inquiry and abandon that | Both holds disappear ~15 min later and the slot is bookable again | Either slot stays blocked. Before #1594 the staff-proposed hold sat for **48 hours** - 192x the configured TTL |
| Appointments | Put one talent on **two different agencies**. Book them at 3pm from agency A, then try 3pm from agency B | Agency B is refused; the talent cannot be in two places | Both bookings succeed. This is the one-calendar rule and #1593 is the only thing enforcing it |
| Appointments | Book 10:00-11:00, then book **11:00-12:00** for the same talent | Both succeed - back-to-back is not an overlap | The second is refused, which would mean the range stopped being half-open |
| Appointments | Cancel a booking, then rebook the **same** slot | Succeeds - cancelling frees the time | Refused, meaning cancelled rows still hold the slot |
| Appointments | Open **Booking Hours** in the workspace, clear the timezone field, save, reopen | It comes back as the **venue's** zone, never a literal UTC | It shows UTC for a venue that is not in UTC |
| Appointments | Book a **deposit** or **full-prepay** service through the **slot picker** (not the storefront) | A card is charged, exactly as the storefront charges | It completes as a free reservation with no charge - that was live until #1583 |
| Orders 0.9 | Create a promo code with **max_redemptions = 1**, then have **two people check out with it at the same moment** | Exactly one order gets the discount; the other is refused and **no order is created for it** | Both get it — the redemption is decided by a check-then-insert instead of under the row lock. This is the whole reason 0.9 was a slice and not a wire, and a load test is the only thing that can see it |
| Orders 0.9 | Use a code with **per_customer_limit = 1** twice as the same customer | The second attempt says **"You have already used that code"**, not "fully claimed" | It says the code is used up. The code is alive and someone else can still use it, so that sentence is false to this person |
| Orders 0.9 | Type a code that does not exist, then one that has **expired**, then one **scoped to another event** | Three different sentences, and the order is **not created** in any of them | The purchase goes through at full price. A code typed and then ignored is an overcharge the customer only finds on the receipt |
| Orders 0.9 | Buy **one VIP and four GA** tickets with a code scoped to **VIP only** | Only the VIP line is discounted; the GA tickets are full price | The whole order is discounted. `applyPromo`'s scope check only asks whether the tier is PRESENT, so passing it the order subtotal discounts everything |
| Orders 0.9 | On a discounted order, check the **customer's card statement** against `orders.total_cents` | They match to the cent | They differ by one or two cents — apportionment rounding, which reconciles wrong quietly and is invisible until someone compares a statement |
| Orders 0.8b | Refund **one line** of a two-line order, then look at the other line | The other line is untouched and still refundable | Both lines are marked, or the second refund is refused. Per-line refund state is new and nothing has exercised it |
| Orders 0.8b | Refund a line on an order paid by **deposit + balance** (two charges) | Two refunds appear in Stripe, oldest charge drained first, summing to the line's value | One refund for part of it, or a failure after one leg — the split is the gap most likely to be found by a customer rather than a test |
| Orders 0.8b | **Scan a ticket in, then try to refund it** | Refused, naming how many tickets **were** available to refund | It refunds. Someone who walked in and then wants money back is a dispute, not a refund-by-line |
| Orders 0.8b | Refund **2 of 4** tickets on a session, then have the other **2 scanned at the door** | Both admit normally; the 2 refunded ones are turned away and the door **says "refunded"** | A refunded ticket admits. Releasing a seat does not void its ticket — that is why the stamp and the release are one operation |
| Orders 0.8b | After a **partial** refund, check whether the freed seats are **resellable** | They are NOT — and that is correct today | They are resellable. The engine cannot release part of an allocation, so a partial refund deliberately holds the seat. A visible revenue cost beats an invisible oversell |
| Orders 0.8b | **Fully** refund an order that used a one-per-customer code, then try the code again as that customer | It works — a full refund gives the redemption back | It is still spent. The purchase was undone, so the code should not be burned |
| Orders 0.8b | **Partially** refund an order whose code had a minimum spend, dropping it below that minimum | Nothing is clawed back; the customer owes nothing extra | The discount is reversed. A customer who receives a refund we granted must never end up owing money |
| QR & Links | **Bleed — needs a CUT piece, not a print.** Once the print canvas ships, print a design whose background runs to the edge, then **cut it to trim size** and look at the edge | No white sliver on any side | A green result from an UNCUT sheet. A correctly drawn trim guide and a guillotine cutting 1mm off-centre are indistinguishable on screen and on an uncut page — the scan row's falsifier ("anything that did not come from a phone camera") does not cover this one. Its falsifier is **anything green that did not come from a cut edge** |
| QR & Links | Print a `/q/<code>` table tent at **actual size** (Share → Print PDF, print at 100%, no "fit to page"), put it on a table, scan it with a **real phone camera** | Ink-to-camera works — the one step no script can fake. Every layer below it is proven in software | Phone will not focus or lock on; a scan from a screen working while the print does not (screens are backlit and forgiving, paper is not); or the printer scaling the page, which shrinks the modules below the ~0.4mm floor |
| QR & Links | Scan the **same** printed code a second time, and a third | **Works every time, and each scan is a new row.** A link is not a ticket | Anything "already used". If a link ever becomes single-use, the entire product is broken: a table tent is scanned by a different guest every night for years. This row exists because the ticket rows above say the opposite, and someone will one day make them consistent |
| QR & Links | Scan a code for a link that has been **paused** in the workspace | A real 404 saying *"This code is not active"*, in the phone's language (set the phone to Spanish and confirm *"Este código no está activo"*) | A redirect to the homepage or the menu. A guest acts on whatever they are shown; sending them somewhere plausible is worse than an honest dead end |
| QR & Links | Print **eleven** table codes in one go and check the file | One PDF, eleven pages, each page the size you asked for, each code different | Eleven downloads; one page; or every page carrying the same code — the last is the failure nobody notices until the tents are on the tables |
| QR & Links | Open the Share popover on a bookable thing and use **Copy**, then **WhatsApp**, then **Email** | Copy puts the short link on the clipboard; WhatsApp opens the contact picker with the message and link; Email opens a draft with a subject and the link **in the body** | WhatsApp pre-addressed to the operator's own number; a truncated message (an unencoded `&`); an email whose body is empty — a raw newline in a `mailto` silently drops everything after it |
| QR & Links | In the Share popover, confirm **"Design it"** is visibly disabled and says why | The print designer does not exist yet (Q3) and the UI admits it | A lit button that opens nothing. This is the exact defect removed from the talent publish screen in #1618 and it must not reappear here |
| QR & Links | **Blocked until an env var is set.** After `LINK_SCAN_SALT` exists in Vercel, scan one code five times from one phone, then once from another | Distinct visitors reads **2**, scans reads **6** | Visitors equal to scans (the salt is still unset and every `session_key` is null — the code refuses to hash an IP unsalted, so this reads *unknown*, never a wrong number) |

## Owner-hands items (not clicks — these need an account or a bank)
Kept separate so the QA pass is not blocked behind them.

- MX + SPF for `tulala.digital` on the existing impronta.group Google Workspace, and who reads that mailbox. **Every prospect emailing hello@ bounces today.**
- Bank ···4702 re-verification.
- Subscribe `refund.failed` and `refund.updated` in Stripe.
- Set `STRIPE_ALLOW_LIVE_PAYOUTS=false`.
- Stripe Tax head office.
- **The card-mix answer:** will these eleven businesses' customers pay mostly with Mexican cards or
  mostly foreign — above or below ~2/3? Unsure defaults to staying put, which is safe.

## Parked by the Menu Workspace Manager, with reasons

Not QA rows — these are named as unfinished so nothing here is mistaken for shipped.

- **The Menu board UI** (page views Items / Orders / Kitchen / Tabs, columns rendered from the
  pipeline, grouped by table when `space_id` is set). The stage model it renders shipped in #1660;
  the board itself did not. Menu's stated definition of done is "a pizza maker and a laundry each
  have a working board" — **the stages that make both boards possible exist and are tested; the
  board does not.** Half met, and not called finished.
- **Rekeying `booking_fulfillment` onto orders**, with `status` becoming the stage key. Needs the
  Orders & Checkout Manager: it is their table's grain, not mine to change unilaterally.
- **Menu orders produce no commission snapshot.** `lib/orders/purchase.ts` has zero references to
  `persistBookingCommissionSnapshot`; the engine deleted in #1580 called it and treated failure as
  fatal. **Not yet realised** — `orders` is empty in production, checked — so nothing has been lost,
  but the first real menu order loses attribution silently, with nothing erroring. Migration
  `20261229000241` already made the commission context read `order_lines`, so the reading half was
  built and nothing invokes it. Not patched into the feature action on purpose: money belongs in the
  engine, and putting it back into a feature is what the re-home existed to end. **Owner: Orders.**
- **Table QR context** (`space_id` on the draft order). Waiting on QR & Links #1658 to merge; the
  popover component lands with it. Not started rather than half-started.
