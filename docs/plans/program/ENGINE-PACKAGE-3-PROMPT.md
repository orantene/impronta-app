# Tulala engine — Package 3 prompt (for Cursor)

Paste everything below the line into a Cursor agent opened in `/Users/oranpersonal/Desktop/impronta-app`. It is self-contained. When it finishes, paste its closing report back to the integrator session verbatim.

---

You are a principal backend engineer on Tulala (Next.js 16 + Supabase, multi-tenant; repo root `/Users/oranpersonal/Desktop/impronta-app`, app in `web/`). The owner has given full standing authorization: no questions, decide and record, continue until only genuine external blockers remain. Never ask the owner anything; your closing report lists what is done and what is blocked with the one thing that unblocks each.

## Why you exist

A separate session (the integrator) has re-skinned every screen to the approved design boards and wires engine capabilities into them as they land. Two engine packages already shipped this way (`docs/plans/program/engine/pos-money.md`, `docs/plans/program/engine/scheduling.md`; read both first: they are the pattern). What is left on the boards as "disabled with a reason" is Package 3: the remaining tables, RPCs and server actions listed below. You build the engine; you never touch a screen. Each task ends with a contract file the integrator wires from.

## Ground rules (each has cost a round trip on this program)

1. **File scope.** You may change: `web/src/lib/**`, `supabase/migrations/**`, `web/src/lib/server-actions/**` (put every new action in **`web/src/lib/server-actions/venue-engine.ts`**, one file, `"use server"` at the top, async functions only; NEVER re-export it from another `"use server"` file, Next drops every action in that file when you do), `web/src/app/api/cron/**` (crons), `web/messages/{en,es,fr}.json` (refusal sentences only, under `dashboard.venue.engine.refusal.*`), `web/e2e/**` new case files, `web/scripts/verify-*.mjs`, `docs/plans/program/**`. You may **not** change anything under `web/src/components/**`, any `page.tsx`/`*-client.tsx` under `web/src/app/(workspace)/**`, `web/src/app/globals.css`, or the admin shell. Two public pages are the only UI you may add: the guest table page's ordering/pay routes under `web/src/app/(public)/visit/[token]/**` (route handlers + server pages; the existing `/visit/[token]` page stays as it is) and the ticket self-service page under `web/src/app/(public)/t/[code]/**`. Everything else a screen needs goes into the contract file, not into a component.
2. **Worktree, never the shared checkout.** `git fetch origin && git worktree add /private/tmp/engine-3 -b engine/venue origin/main && web/scripts/setup-worktree.sh /private/tmp/engine-3`. Never `git switch` in `/Users/oranpersonal/Desktop/impronta-app`. Copy `web/.env.capacity-isolated.local` from `/private/tmp/claude-505/-Users-oranpersonal-Desktop-impronta-app/e30ce090-ce88-474f-861f-a6f78cb4f718/scratchpad/wt-candidate/web/.env.capacity-isolated.local` into your worktree (`chmod 600`). Never print or commit any value from it.
3. **Database.** Production `pluhdapdnuiulvxmyspd` is READ ONLY for you (reads via `POST https://api.supabase.com/v1/projects/pluhdapdnuiulvxmyspd/database/query` with `read_only:true`, token `SUPABASE_ACCESS_TOKEN` from `web/.env.local`). All schema work goes to the isolated branch `fxlankepwnvelxjrahwk` first: `cd web && npm run journeys:repair -- ../supabase/migrations/<file>.sql`, then prove the objects exist by querying the branch (`DATABASE_URL` in the isolated env; a tiny node script with `pg` from `web/node_modules` works). Never `npm run db:push`, never `reset_branch`. Migration version = `date -u +%Y%m%d%H%M%S` when you create the file; `node --env-file=.env.capacity-isolated.local scripts/check-migration-version-collisions.mjs --remote` must exit 0. Conventions for every migration (copy from `supabase/migrations/20261231206000_visit_check_ops.sql` and `20261231210000_visit_transfer_expected_version.sql`): `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`; jsonb `{ok, reason, …}` replies; refusals are data, never throws; `REVOKE ALL … FROM PUBLIC, anon, authenticated; GRANT EXECUTE … TO service_role` plus a `has_function_privilege` DO-block; new tables RLS on, staff SELECT via `public.is_staff_of_tenant(tenant_id)`, writes service_role only; an executable proof block in the same file that breaks the rule on purpose and asserts the refusal, rolled back via a caught sentinel. **Two things that bit Packages 1 and 2:** extension functions live in the `extensions` schema, so write `extensions.crypt(...)`, `extensions.gen_salt(...)`, `extensions.gen_random_bytes(...)` (bodies run with `search_path = public`); and every state-changing RPC on a row a screen shows takes `p_expected_version integer` and refuses `conflict` when it differs (two concurrent moves of one thing must yield exactly one winner).
4. **Money and capacity are authoritative in SQL under `FOR UPDATE`**, replayed by `(tenant_id, operation_key)`; every TS caller passes an idempotency key; a race proof script (`web/scripts/verify-<thing>-race.mjs`, guarded by `isolated-target-guard.mjs` + its own env flag, ground truth read from tables after, exit 1 on any oversell/duplicate/leak, exit 2 without the isolated env) for anything that moves money or seats. Read `web/scripts/verify-visit-transfer-race.mjs` as the template.
5. **Refusals are codes**, mapped to sentences in three languages; a static test asserts every code has all three (copy `web/src/lib/pos/engine-refusals.static.test.ts`). Never return English prose from an action. A function that answers instead of refusing is a defect: absence must be structurally distinct from a value; times carry a timezone.
6. **Gates** before every commit, on a private typecheck lane: `cd web && TSC_QUEUE_LOCK=/tmp/tulala-tsc.engine3.lock TSC_QUEUE_TICKETS=/tmp/tulala-tsc.engine3.tickets npm run typecheck && npm run lint && npm run test:money && npm run test:capacity && npm run test:commands && npm run test:tenant-isolation && npm run test:size-ratchet && npm run test:phase1-i18n && npm run test:reservations && npm run test:events && npm run test:sessions`. Capture real exit codes. Never widen a baseline, budget, suppression or type; no casts; no skipped or inverted tests. The machine is shared with the integrator's builders: at most ONE typecheck and ONE test lane of your own at a time, no dev server unless a public page needs a screenshot (then one, briefly), no `next build`.
7. **Safety.** Never delete with a wildcard or a relative path (`rm -rf *` trips a guard that stops the owner's work); name the full absolute path. No em dashes in user-facing copy. Never print secrets. Never touch production data.
8. **Ship.** Commit per task (`<surface>: <what>`), push `engine/venue`, open ONE PR to `main` titled "Engine package 3: venue, events, guest and device engine" with the task table, evidence paths and every proof output pasted. Mark it ready (not draft). **Do not merge it and do not apply anything to production**: the integrator applies your migrations to production and merges after re-running your proofs on the isolated branch. Rebase on `origin/main` before the final push (the integrator merges design batches to main several times a day; `docs/plans/program/pos/decisions.md` is appended by both sides, so take both on conflict and renumber yours to the next free D-POS ids).
9. **Record.** Every decision as the next free `D-POS-n` in `docs/plans/program/pos/decisions.md` (currently ends at D-POS-81; start at D-POS-82). Every contract for the integrator in **`docs/plans/program/engine/venue.md`**: per task, the action names, input shapes, result unions with every refusal code and its sentence key, the reader that shows the result, and the board ids it unblocks. Evidence under `docs/plans/program/evidence/engine-venue/` (README per task: migration version, objects proven with the query and its rows, proof/race output with real exit codes).

## Existing engine you build on (do not duplicate)

- `visits (id, tenant_id, space_id, public_token, status, version, opened_at, closed_at, opened_by, service_kind, party_size, joined_space_id, server_user_id)`; a visit may own several `orders` (split check, D-POS-67); `visit_transfer/visit_split_check/visit_merge_checks/visit_change_server` (Package 1), `admin/tables/actions.ts` (seat, walk-in, close, reset), `lib/pos/floor*`, `lib/visits/*`.
- `spaces (id, tenant_id, venue_id, parent_id, kind, name, code, party_min, party_max, seat_count, turn_minutes, min_spend_cents, attributes jsonb, status, sort_order, needs_reset_at)`, `space_groups (…, kind, party_min, party_max, min_spend_cents, sell_mode, sort_order)`, `venue_service_rules (venue_id, tenant_id, is_active, party_size_min/max, horizon_days, min_notice_minutes, turn_time_bands, default_turn_minutes, allow_public_upsize, card_on_file_from_party, no_show_fee_*, deposit_*, free_cancel_hours, waitlist_enabled, walkins_enabled, notes_enabled, reservation_offering_id)`, venues + timezone in `lib/spaces/venues.ts`; capacity pools with `subject_kind: space`.
- `preparation_tickets (id, tenant_id, order_id, visit_id, station text, destination, status, revision, promised_at, submitted_at, acknowledged_at, ready_at, handed_off_at, cancelled_at)` + `preparation_ticket_revisions`; `lib/preparation/tickets.ts`, `posSubmitPrep`, K09 acknowledgement.
- `admissions (id, tenant_id, allocation_id, order_line_id, session_id, space_id, customer_id, holder_name, holder_email, starts_at, status, party_size, admitted_count, token_version, seated_at, no_show_at, completed_at, line_seq, door_amount_cents, door_paid_via)`; signed ticket codes in `lib/sessions/admission-token.ts` (`GUEST_COOKIE_SECRET`); `refund_admission`; `_door-actions.ts` (admit, manual admit, box office); tiers on `talent_offering_variants` with per-session pools; `lib/events/*`.
- `session_waitlist_entries` + `waitlist_offers` (Package 1: **classes only**, D-POS-68).
- Orders: `lib/orders/purchase.ts`, `lib/pos/draft.ts` (`pos_mutate_draft_line`, `pos_apply_draft_totals`), `lib/pos/collection.ts` (`pos_reserve_collection`), `payment_links` + `/pay/<code>` (Package 1), `orders.tip_cents`, guest identity (`guest_session_id` + `receipt_code`, `lib/guest-cookie.ts`).
- POS: `pos_device_sessions` (Package 1: lock/operator), `pos_shifts`, `agencies.settings.pos.locations.default.modes` via `lib/pos/pos-modes-store.ts` (the string `"default"` is the one implicit location: keep it working).
- Customer self-manage token from Package 2 (`docs/plans/program/engine/scheduling.md`), reschedule/cancel RPCs, policy overrides, `approval_requests`, `role_limits`.

## Package 3 — venue, events, guest and device engine

Build in this order; each = migration + lib + action contract + tests + proof; commit per task.

1. **Locations and zones** (W23 LocationsZones, POS location chip, per-location modes; D-POS-18, D-POS-55, D-POS-80). Tables `locations (id, tenant_id, slug, name, venue_id, timezone, address jsonb, is_default, sort_order, status)` and `location_zones (id, tenant_id, location_id, name, kind 'floor'|'bar'|'terrace'|'room'|'counter', surcharge_bps, sort_order)`; a migration that inserts one `default` location per existing agency (slug `default`, `is_default true`, venue = the agency's first venue, timezone from it) so nothing changes for anyone; `spaces.location_id` and `spaces.zone_id` nullable columns, backfilled to the default. `lib/pos/pos-modes-store.ts` keeps reading `settings.pos.locations.<slug>.modes` and gains `readLocationModes(tenant, slug)` / `writeLocationModes`. Actions: `locationsList`, `locationUpsert`, `locationSetDefault`, `zoneUpsert`, `zoneDelete` (refuse `has_spaces`). Refusals `duplicate_slug`, `last_location`, `has_spaces`, `conflict`.
2. **Party waitlist (restaurant)** (T08, POSWalkIn waitlist, MW17 queue; D-POS-59/68 said this is a separate table). Table `party_waitlist (id, tenant_id, location_id, zone_id, party_size, holder_name, holder_phone, holder_email, note, quoted_minutes, status 'waiting'|'notified'|'seated'|'left'|'expired', joined_at, notified_at, notify_expires_at, seated_visit_id, position)`; RPCs `party_waitlist_join`, `party_waitlist_notify(ttl)` (SMS/WhatsApp send goes through the existing transactional sender contract; if no sender exists for phone, record `notified_at` and return `channel:'none'`, never pretend), `party_waitlist_seat(entry, space, operation_key, expected_version)` → opens the visit through the same path `admin/tables/actions.ts` seat uses, `party_waitlist_leave`, reaper in `api/cron/expire-orders`. Race: two seats of one entry → one wins. Refusals `already_seated`, `space_occupied`, `expired`, `conflict`.
3. **Layouts, service periods, prep stations** (W13 LayoutEditor, W14 ServicePeriods, W15 PrepStations, R06 LayoutsShareCapacity, T26 station shell; D-POS-64). `space_layouts (id, tenant_id, location_id, name, is_active, canvas jsonb {w,h}, created_by)`, `space_layout_items (layout_id, space_id, x, y, w, h, rotation, shape)` with `layout_activate(tenant, layout, expected_version)` (exactly one active per location; activating never changes capacity: capacity stays on pools, R06 rule); `service_periods (id, tenant_id, location_id, name, weekday_mask int, starts_local time, ends_local time, turn_minutes, rules jsonb)` read by the reservation slot generator in place of the single `venue_service_rules` window when present (fallback: today's behaviour); `prep_stations (id, tenant_id, location_id, code, name, kind 'kitchen'|'bar'|'pickup'|'pass', sort_order)` + `talent_offerings.prep_station_id` nullable + `preparation_tickets.station_id` nullable (keep the `station text` column; backfill from it), and `course_seq` on `order_lines` with `prep_fire_course(tenant, visit, course_seq, operation_key)` that submits only that course's lines. Refusals `overlap` (periods), `two_active` (layouts), `station_in_use`.
4. **Guest QR ordering and pay-my-share** (Q02 Browse, Q03 Submit, Q04 Substitute, Q05 PayAtTable, Q06 PayMyShare, Q07 AlreadyPaid; D-POS-63). Guest identity = the visit's `public_token` + the existing guest cookie. Actions (public, token-scoped, rate-limited by the existing pattern in `lib/guest-cookie.ts`): `guestVisitMenu(token)` (published items for the tenant with `prep_station` and availability), `guestVisitAddLine(token, offeringId, variantId, qty, note)` → a guest-sourced draft order on the visit (`source_channel 'guest_qr'`, `pos_mutate_draft_line`), `guestVisitSubmit(token)` → prep tickets via the existing submit path with `destination 'table'`, `guestVisitSubstituteAccept(token, lineId, substituteOfferingId)` (Q04: when a submitted line is 86'd by staff, the staff action `posLineOfferSubstitute` records the offer and the guest accepts), `guestVisitPayShare(token, amountCents | lineIds[], method 'link')` → reserves through `pos_reserve_collection` and mints a `payment_link` (Package 1) for that share; `guestVisitBill(token)` shows paid/owed per share (Q07). Public route handlers under `web/src/app/(public)/visit/[token]/**` only. Race: two guests paying the last share → one wins, the other sees `already_paid`. Refusals `visit_closed`, `item_unavailable`, `already_paid`, `exceeds_outstanding`, `not_submitted`.
5. **Events: seat maps, hold timer, exchange, comp, multi-day, delivery** (E03 Seats, E05 HoldExpired, E11 Exchange, E12 Comp, E14 MultiDay, E15 Delivery, W17 EventVenueSeating; D-POS-59 door). `event_seat_maps (id, tenant_id, session_id, layout_id)` reusing task 3's layouts with `space_layout_items` as seats (a seat = a `spaces` row of kind `seat` under the room; capacity through the existing per-space pools, never a parallel count); `admission_holds (id, tenant_id, session_id, seat_space_id, guest_session_id, expires_at, order_id)` with `admission_hold_seats(tenant, session, seat_ids[], guest, ttl, operation_key)` via `reserve_resource_set_v2` and the reaper releasing expired holds (E05); `admission_exchange(tenant, admission, to_session, operation_key, expected_version)` (moves the allocation; price difference becomes a draft line owed or a refund intent, never silent), `admission_comp(tenant, session, tier_variant, holder, reason, approver)` (a zero-priced order + admission, requires an `approval_requests` row from Package 2 when `role_limits` says so), multi-day = an `event_series (id, tenant_id, name)` + `sessions.event_series_id` so one order can carry admissions across nights (`purchase.ts` gains a series-aware line: one line per night, one allocation each), delivery = `admissions.delivery jsonb {method 'email'|'sms'|'print'|'wallet', sent_at, provider_ref}` written by `admission_deliver(tenant, admission, method)` through the existing email sender; `sms`/`wallet` return `channel_unavailable` (no sender), never pretend. Race: two holds on one seat → one wins. Refusals `seat_taken`, `hold_expired`, `same_session`, `price_up_needs_payment`, `needs_approval`, `channel_unavailable`.
6. **Ticket self-service page** (E08 CustomerTicket, E09 TicketLookup, E10 Transfer; D-POS-59). Public page `web/src/app/(public)/t/[code]/**` reading the signed admission code (`lib/sessions/admission-token.ts`): shows the ticket, its QR, the night, the venue's clock; actions `ticketTransfer(code, to_name, to_email)` (re-signs with `token_version + 1`, old code refused `superseded` at the gate), `ticketResend(code)` (email only), `ticketLookup(email, last4OfReceipt)` for E09 (rate-limited, returns codes only to the holder email). Three languages. Refusals `superseded`, `not_found`, `too_many_attempts`.
7. **Device registry and offline policy** (POSDevices, POSConnection, POSCounterOffline, W20 devices; D-POS-28, D-POS-32, D-POS-11 offline). `pos_devices (id, tenant_id, location_id, device_key, name, kind 'tablet'|'phone'|'display'|'printer'|'reader', last_seen_at, app_version, registered_by, status)` registered from the existing `device_key` (Package 1's `pos_device_sessions` gains `device_id`); `pos_device_heartbeat(device_key, app_version)`; per-device defaults (`default_mode`, `drawer_id`, `printer_id`) in a `settings jsonb` column with `posDeviceUpdate`; offline policy = a `pos_outbox (id, tenant_id, device_id, command jsonb, created_at, applied_at, result jsonb)` table + `pos_outbox_apply(tenant, device, operation_key)` that replays cash-only commands through the existing idempotent RPCs in order and refuses `not_replayable` for anything touching a provider; contract must state that the client queues only cash sales while offline (D-POS-11 says cash-only degraded mode). Refusals `unknown_device`, `not_replayable`, `stale_app`.

Acceptance per task: migration applied on the isolated branch with objects verified; unit tests against a fake `rpc`; static guard for the rule; race script PASS line where money/seats/rows contend; contract section written in `docs/plans/program/engine/venue.md`; gates green; committed.

## Closing report (paste this back to the integrator verbatim)

Use exactly this shape:

```
PACKAGE 3 REPORT
PR: #<number> (ready, not merged) · branch engine/venue · head <sha> · rebased on main <sha>
Contract: docs/plans/program/engine/venue.md
Evidence: docs/plans/program/evidence/engine-venue/

| Task | Migration(s) | Objects proven (isolated) | Race/proof (exit, PASS line) | Boards unblocked | Commit |
| 1 Locations & zones | 2026…  | tables/cols/fns: yes/no | n/a | W23, chip | <sha> |
| 2 Party waitlist | … | … | verify-party-waitlist-race.mjs exit 0 wins=1 | T08, MW17 | … |
| 3 Layouts/periods/stations | … | … | … | W13 W14 W15 R06 T26 | … |
| 4 Guest QR + pay share | … | … | verify-guest-share-race.mjs exit 0 | Q02–Q07 | … |
| 5 Events seats/holds/exchange/comp/multi-day/delivery | … | … | verify-seat-hold-race.mjs exit 0 | E03 E05 E11 E12 E14 E15 W17 | … |
| 6 Ticket self-service | … | … | n/a | E08 E09 E10 | … |
| 7 Devices + outbox | … | … | verify-outbox-replay.mjs exit 0 | POSDevices POSConnection POSCounterOffline W20 | … |

Gates (real exit codes): typecheck _ · lint _ · money _ · capacity _ · commands _ · tenant-isolation _ · size-ratchet _ · phase1-i18n _ · reservations _ · events _ · sessions _
Migrations applied to fxlankepwnvelxjrahwk: <list> (production: NOT applied, by design)
Decisions recorded: D-POS-82 … D-POS-<n>
Refusal codes added (dashboard.venue.engine.refusal.*): <count>, all three languages
Public pages added: /visit/[token]/… , /t/[code]
Defects found in existing code on the way (not fixed unless in scope): <list or none>
Not done / blocked: <task · what · the one thing that unblocks it>
Fixture rows left on the isolated tenant: <ids or none>
```
