# Defects

One log. Blocking / high-risk also get a GitHub issue. Normal and cosmetic stay here.

| ID | Severity | Disposition | Area | Finding | Evidence |
|---|---|---|---|---|---|
| D-001 | high-risk | implementing — P1-02 | orders | Paid order can end with no seat. Compensation now writes `ticket_refund_intents` for capacity-backed lines. Browser/cron execution still awaiting. | `capacity-lost-compensation.ts` |
| D-002 | high-risk | implementing — P1-03 | orders | `sweepExpiredOrders` now exists with cron + heartbeat. Remote run awaiting. | `expire-orders.ts`, `/api/cron/expire-orders` |
| D-003 | normal | implementing — P1-04 | events | Promo field added to guest picker. Browser proof awaiting fixture. | `ticket-picker-island.tsx` |
| D-004 | normal | closed — P0-05 | docs | Instruction docs updated. Remote `20261230000200`–`00600` applied to `pluhdapdnuiulvxmyspd` (visits, shifts, deliverables, visit slug, remaining holes). | CLAUDE.md, OPERATING.md |
| D-007 | cosmetic | closed | events | Ticket picker file headers now say card or pay-at-door. | `ticket-picker-island.tsx` |
| D-005 | high-risk | verified in test environment — P1-01 | capacity | 200 concurrent HTTP `reserve_capacity` on qa-journeys: 12 ok, 188 sold_out, 12 live units, 0 remaining. Throwaway pool cleaned up. Never production. | `qa-evidence/P1-01/capacity-concurrency.md` |
| D-006 | normal | closed — P0-01 | product | Source product documents now in `docs/product/`. | `Tulala-Business-Journeys-POS.md`, `Business-specific-labels-Workspace-Theme.md` |
| D-008 | high-risk | implementing — W-AUDIT | capacity | `reserve_capacity*` is service-role SECURITY DEFINER and keys on `pool_id` only. `reserveResourceSet` now refuses `wrong_tenant` / `pool_not_found` before the RPC. | `reserve-set.ts` |
| D-009 | high-risk | implementing — W-AUDIT | pos | Collect holds class places; cancel now releases this tenant's live allocations for the sale's lines. | `collection.ts` finalizeOrCancel |
| D-010 | high-risk | implementing — W-AUDIT | pos | Walk-in collect was holding `talent_offerings.capacity_pool_id`. It now holds the session `session_tier` / `default` pool via `tierReserveRequest`, same as the guest picker. | `hold-capacity.ts` |
| D-011 | high-risk | closed — 803b08325, 6aa9e5093 | fixture | Workspace B on qa-journeys was identity-only: `agencies` row, active host and owner membership, zero spaces, offerings, sessions, pools, customers and orders. **Filed as "normal" and it was not.** An empty second workspace does not merely block cases where B transacts — it makes the ID-substitution requirement untestable while appearing testable, because the only id available to substitute exists nowhere and "not found" is indistinguishable from "forbidden". Such a test passes on day one and keeps passing with authorization deleted. B now has a venue, station, technician with booking hours, two offerings, a one-unit pool, a customer and a paid $9.00 order; `PERM-cross-workspace.spec.ts` is 5/5, each refusal paired with a positive control. | `seed_journeys_program.sql`, `PERM-cross-workspace.spec.ts` |
| D-012 | high-risk | closed — schema drift | migrations | `schema_migrations` on qa-journeys recorded 763 versions up to `20261230000700` while objects *inside* that range did not exist — the historical replay recorded versions whose bodies never ran, so a ledger diff reported "in sync" over a schema missing `refund_admission`, `cancel_event_cascade`, `extend_capacity_hold` and 13 more. `check:migrations-applied` was independently blind because `list_applied_migrations` was never replayed either. Twelve migration files replayed from source; `journeys:probe` and `journeys:smoke` now cover it, and the ledger is no longer treated as evidence. | `qa-evidence/schema-drift/isolated-branch-repair.md` |
| D-013 | normal | closed — docs | docs | `web/AGENTS.md` said "Agents do not browser-QA" while this program's own evidence trail is ~20 agent-run browser passes on qa-journeys. The rule now scopes to production and real tenant data, and names the isolated branch as the place agent browser QA belongs. | `web/AGENTS.md` |
| D-014 | high-risk | open | migrations | **This repo cannot rebuild its own production schema.** 58 objects are in `database.types.ts` (generated from the live project) with no migration creating them — `cms_pages.blocks`, `cms_pages.is_freeform`, `cms_navigation_items.pin_in_menu`/`show_in_sticky_top_nav`, six `engine_audit_log` columns, five `platform_settings.default_theme*`/`default_component_styles`, `agency_taxonomy_settings`, `agency_taxonomy_terms`, `talent_type_field_groups`, `talent_profiles.subscription_template`, two `talent_discover_index` view columns, `saas_marketing_signups.recovery_email_sent_at`, plus deliberate `_archive_*` / `_backfill_*` / `_impronta_pages_backup_*` scratch. Applied by hand; nothing to replay. `cms_pages.blocks` is fixed (20261230001400) because it was breaking `loadVerbSlug` on every request; the rest are open. Any new environment silently lacks all of them. | `npm run journeys:audit`, `20261230001400` |
| D-015 | high-risk | closed — 20261230001400 | web | `loadVerbSlug` selects `cms_pages.blocks`, a column no migration creates, so on any repo-built database the read failed, the function returned null, and **every reserve button fell back to the chat cue instead of the published page that answers it**. Seven call sites select the column. Storefront now serves 200 with no schema errors and no verb-destination warnings. | `verb-destination.server.ts`, `20261230001400` |
| D-016 | high-risk | closed — 806c3505e | orders | An ungated basket carrying an attestation wrote `age_gate_min_age = NULL, confirmed_age = 21, confirmed_at = NULL`, because the insert assembled the triple from three expressions. `orders_age_gate_paired` requires all three or none, so the order INSERT was refused and a buyer who had just confirmed their age got "Could not start the order". Unreachable until the columns reached an environment the purchase path runs in. `ageGateStamp` now derives all three from one branch. | `age-gate.ts`, `purchase.ts` |
| D-018 | high-risk | closed — 03c0489d0 | reservations | The storefront `reserve_table` date strip offered *today* after last seating, so `availabilityForWindow` dropped every time and the guest saw a date with no slots while tomorrow was open. A day is now offered only when `lastSeatingAt >= now + minNotice`. The first diagnosis (no `talent_booking_hours` on a workspace-owned table) was the appointments-dead shape and was wrong for this failure. | `reserve-actions.ts`, `date-strip-open-days.test.ts` |
| D-019 | high-risk | closed — 870e1de48, 28a261b2c, 9a3cb1946 | pos | **POS cash and the event door could not record money at all.** Three faults stacked: `settleAtDoor` inserted `booking_transactions` with no `booking_id` (the shell was private to the POS card path, and the cash branch returned before reaching it), it inserted `status: 'paid'` which the transition trigger forbids on INSERT, and phase 8 had reverted the off-platform receiver exemption so cash demanded a Stripe payout destination it can never have. A cashier took the note and the order stayed unpaid with the full amount outstanding. Browser-verified: C06-OP walk-in cash passes and the row reads paid / manual / booking attached / no receiver. | `settle-at-door.ts`, `booking-shell.ts`, `20261230001500` |
| D-021 | high-risk | closed — 9a3cb1946 | migrations | `20260906100000_phase_8_corrective_hardening.sql` re-created `validate_booking_transaction_status_transition` to widen the transition graph and, doing so, re-emitted the receiver rule **without** the `provider IS DISTINCT FROM 'manual'` clause that `20260714161746_off_platform_settlement_rail_aware.sql` had added and argued for. Not an argued reversal — collateral damage of a whole-body `CREATE OR REPLACE`, which nothing flags. All off-platform money (cash, wire, venue_paid) became unrecordable. **A migration that re-creates a function silently owns every line of it**, and there is no gate for that. | `20260906100000`, `20261230001500` |
| D-020 | normal | open | ui | The Sales table prints the raw enum `pending_payment` in its Status column, while `order-card.ts` already defines the human label "to pay" for that state. Operators are being shown database vocabulary. Related to the M1 paired-label work. | `admin/sales` |
| D-017 | normal | closed 2026-09-11 (defects-close) | migrations | Residual after statement-level replay was **2 objects**: `field_definitions_archived_20260611` and `field_values_archived_20260611`, expected by the audit because `20261021000000_drop_system_a_field_tables.sql` creates them and their purge (`20260615194711`) sorts EARLIER by version although it ran later. Edited `20261021000000` in place (idempotent-safe: production holds none of the four tables, verified read-only): the archive is taken inside a `DO` block only when both System A tables exist, the System A drops are `IF EXISTS`, and the purge is re-asserted at the end of the file so version order agrees with what production holds. `npm run journeys:audit` before: `MISSING — 2 object(s)` (expecting 320 relations); after: `nothing the migrations promise is missing` (expecting 318). The file replays clean on the isolated branch in a rolled-back transaction (six notices, no error). The audit still exits 1 for the 54 D-103 objects, unchanged. | `evidence/defects-close/audit-before.txt`, `evidence/defects-close/audit-after.txt`, `evidence/defects-close/sql/04-d017-replay-rolledback.out.txt`, `20261021000000` |
| D-022 | high-risk | open | qa-harness | `/api/dev/signin` answers 307 after a cold boot, then can 404 after later Turbopack compiles because Edge HMR of `proxy.ts` re-reads an empty `process.env` and drops the inlined `TULALA_ALLOW_DEV_SURFACES`. Mitigated: `proxy.ts` also short-circuits `/api/dev/*` when `Host` ends with `.local` (isolated harness only; the route handler still 403s outside development / Vercel preview). Next dying under memory still 502s the proxy (C12-OP door once this morning). Wipe `.next/dev` and restart `next dev` when it sticks. | `_harness.ts`, `next.config.ts`, `proxy.ts` |
| D-023 | high-risk | closed — e3d0d844b | orders | A $0 pay-in-person reservation was left `pending_payment` behind a 15-minute payment hold. `decideOrderExpiry` cancelled it; `remaining()` stopped counting the hold even sooner, so the restaurant could resell a table the guest still held a confirmation for. `settlesOnCreation` asks whether anything is owed, not whether someone will pay later, and `settleOrHoldOrder` commits capacity before flipping to `paid`. | `settles-on-creation.ts`, `purchase-settlement.ts` |
| D-024 | high-risk | closed — 9d27323c9 | capacity | `20261229000218_batch_per_leg_ttl.sql` re-created `reserve_capacity_batch` to add per-leg TTL and wrote the loop from the pre-attribution body, so every request's `order_line_id` was ignored. 18/18 allocations created that morning were unattributed. `completeOrderForTransaction` finds holds by line, so it committed nothing and a paid order lost its seat when the hold lapsed. | `20261230001800` |
| D-025 | high-risk | closed — 80e578258 | capacity | `reserve_resource_set` forwarded an empty `p_capacity` array to `reserve_capacity_batch`, which refuses `empty_batch`. Every 1:1 booking (gel manicure, massage — talent hold, no pool) died with "Could not hold those resources." Couples passed because it carries a room pool. | `20261230001900` |
| D-026 | high-risk | closed — 20261230002000 | catalog | Instant booking needs an exact price, including $0. The original CHECK and `validateOffering` / `offeringIsDirectlyBookable` treated zero as unpriced, so complimentary class, table hold and free GA could not exist on a database rebuilt from migrations. Null / quote / custom / "from" still cannot be instant. C09-CUS class register **5/5** after the change (DIFF needed the Last place seat released first). | `offerings-types.ts`, `20261230002000` |
| D-027 | high-risk | closed | web | `/events/qa-night` 404 was **not** a missing CMS page. The event row was published; anon REST returned it. Next was serving the branded `not-found` because the `(public)/events` routes had not compiled into the Turbopack graph (same family as D-022). After the route compiled: list 200, detail 200, ticket picker present. C12 chromium **5/5** (OP door retried once after Next 502). Allow-list tests now pin `/events` and `/r` on agency/hub. | `events/page.tsx`, `surface-allow-list.test.ts`, `C12-event-venue.spec.ts` |
| D-028 | high-risk | closed | inquiry | `/directory?inquiry=open` opened the legacy sheet (or nothing) because `DirectoryInquiryUrlSync` ran before the chat launcher registered. `requestOpenChat` now queues until `registerChatLauncher`. Separately, `insertSystemMessage` omitted `tenant_id`; on schema-drift DBs three "Offer sent to client." rows landed on Impronta and `clientAcceptOffer` threw `[tenant-coherence]` in development. Payload now stamps the inquiry's tenant; the three isolated rows were repaired. C08-CUS inquiry + C08-OP assign re-verified; C08-CUS accept still not a journey pass this session (Next 502 / D-022). | `directory-inquiry-modal-context.tsx`, `inquiry-system-messages.ts`, `seed_journeys_program.sql` |

| D-100 | high-risk | open, reproducible | appointments / booking | The customer deposit journey stalls. On the isolated QA runtime, `e2e/cases/C01-nail-salon.spec.ts` "C01-CUS deposit" clicks "confirm this time" on `/book` and the page never leaves `/book`; the case expects to reach `/checkout/success\|cancel` or `checkout.stripe.com`. Stripe keys are not set on the preview, so the checkout helper degrades to a mock success URL; the click still produced no navigation. Found 2026-09-09. | `e2e/cases/C01-nail-salon.spec.ts` |
| D-101 | high-risk | resolved 2026-09-09, watch item | qa-harness / migrations | The isolated Supabase branch's pooled connection arrives with `default_transaction_read_only=on`, so every write refuses with `25006` and `journeys:smoke` dies at its first `SELECT ... FOR UPDATE`. Cleared on the isolated branch only with `ALTER ROLE postgres IN DATABASE postgres SET default_transaction_read_only = off` (and the same at database level). Must be re-checked after any branch rebase or reset. | `evidence/T0-04/README.md` |
| D-102 | high-risk | resolved 2026-09-09 | qa-harness | Asking Vercel for the protection-bypass **cookie** (`x-vercel-set-bypass-cookie`) makes the platform answer with its own 307 + `Set-Cookie`; a caller using `maxRedirects:0` reads that as the application's redirect and never reaches the route it asked for. This silently defeated `/api/dev/signin` for every journey case. Fixed in `playwright.config.ts` (header only, not the cookie). | `playwright.config.ts`, commit `db9279ed9` |
| D-103 | high-risk | open, partially addressed | migrations | Objects exist on production that no migration in `supabase/migrations` creates, so a fresh database cannot be rebuilt from the repo. **The three counts in circulation are reconciled: they were measured at different moments, not in disagreement.** D-014's 58 was counted from `database.types.ts` before `20261230001400` fixed `cms_pages.blocks`. The audit run recorded in `evidence/T0-06/README.md` returned 56. The re-run during this merge returned 54, because the migration written in this program had just created `agency_taxonomy_settings` and `agency_taxonomy_terms` on the isolated branch between the two runs. 54 is the current figure, and the number falls by one for every object this program teaches the repo to rebuild. Treat the audit output, never a remembered count, as the source of truth. The remainder are recorded and out of scope for the journeys work: archive and backup tables (`_archive_deprecated_field_defs_20260615`, `_archive_deprecated_field_recs_20260615`, `_archive_deprecated_field_settings_20260615`, `_backfill_is_primary_20260805`, `_impronta_pages_backup_20260817`), `talent_discover_index` columns, `platform_settings` theme columns, `cms_navigation_items.pin_in_menu` / `show_in_sticky_top_nav`, `engine_audit_log` columns, `saas_marketing_signups.recovery_email_sent_at`, `talent_profiles.subscription_template`, `talent_type_field_groups`. Supersedes the open half of D-014. **2026-09-11 (defects-close):** the four ledger-only versions are now files in the tree, see D-108; the 54 hand-applied objects are unchanged. | `npm run journeys:audit`, `evidence/T0-06/README.md` |

Classification: blocking / high-risk / normal / cosmetic. Fix blocking and high-risk in the active workstream. Do not stop unrelated work for cosmetic issues.

| D-104 | normal | closed, independently re-verified 2026-09-09 | migrations | **Fixed and re-proven.** The four files that touched `agency_taxonomy_settings` unconditionally before it exists are each wrapped in `DO $$ IF to_regclass('public.agency_taxonomy_settings') IS NOT NULL THEN ... ELSE RAISE NOTICE ... END IF; END $$;` (the repo's own pattern, e.g. `20260601200300_saas_p1_tenant_id_analytics.sql`): `20260527063534` (Sub-Task 1 ALTER ADD COLUMN + 2 COMMENTs), `20260527174407` (the disable-leaf INSERT + its audit-log INSERT, guarded together so a disable is never logged without happening), `20260615194714` (the one `create index if not exists` targeting this table — `IF NOT EXISTS` guards the index name, not the table), `20260615211200` (the ADD/UPDATE/COMMENT block and the DROP COLUMN block, guarded separately). Directory search re-run 2026-09-09: the real set of files referencing `agency_taxonomy_settings` before `20260909221926` is **8, not 7** — the original count missed `20260615181629_consolidate_languages_into_talent_languages.sql` (comment only, "assignments + agency_taxonomy_settings)"), a documentation undercount, not a functional gap. All four extra files (`20260615134658`, `20260615181629`, `20260615200004`, `20260907160000`) are comment-only or a `DROP INDEX IF EXISTS` (safe unconditionally — checks the index name directly, not the underlying table); the touching set genuinely is exactly the four guarded files. Re-verified item 1 (same behaviour when the table exists) by running the **original unwrapped SQL and the new guarded SQL from the identical starting state**, each in its own rolled-back transaction, and diffing the results rather than only re-running the guarded version once: file 1's column+comment set, file 3's index definition, and file 4's `custom_label_i18n` column all came back byte-identical between original and guarded. File 4's DROP-COLUMN half raises the identical error both ways on this already-migrated branch (`column "custom_label" ... does not exist` — pre-existing, see D-105). File 2's audit-log INSERT *also* raises an identical error both ways (`column "subject_kind" of relation "engine_audit_log" does not exist`) — this is **not** caused by the D-104 guard: `qa-journeys`'s live `engine_audit_log` table has columns `action`/`target_kind`/`target_id`/`payload`, but every migration in the repo that touches it (`20260520044731` creates it, `20260527174407`, `20261003000000`, `20261004000000` insert into it) uses `subject_kind`/`subject_id`/`subject_key`/`operation`/`before_value`/`after_value` — no migration file renames these columns, so this is live schema drift on the branch, already covered by D-103's "engine_audit_log columns" line, not a new defect. It does not block the from-zero replay in item 2 below, because when `agency_taxonomy_settings` is absent the whole guarded block (audit INSERT included) is skipped. Re-verified item 2 (replay past these four files succeeds from zero) with a single rolled-back transaction: dropped both `agency_taxonomy_terms`/`agency_taxonomy_settings` tables, ran all four files' guarded content in order (full content for `20260527174407` and the relevant block of `20260615194714`; the two edited blocks in isolation for `20260527063534` and `20260615211200`, since the rest of those two files is unrelated content that fails regardless of this fix — D-105), got `REPLAY_PAST_FOUR_FILES_OK` with no error, then rolled back. A fresh read afterward (`to_regclass`, row counts, column lists, index names, `engine_audit_log` row count) matches the pre-test baseline exactly — the branch is unchanged. `npm run check:migration-collisions` and `node scripts/check-migration-version-collisions.mjs --remote` both exit 0; `npm run typecheck` exits 0. Production (`pluhdapdnuiulvxmyspd`) was read-only queried, never written: `engine_audit_log` max `created_at` is 2026-08-17 (0 rows in the last 24h) and `agency_taxonomy_settings` max `created_at` is 2026-09-08 22:49, both before and unaffected by this session's work. | `supabase/migrations/20260527063534_tenant_taxonomy_overrides_and_canonical_surface_columns.sql`, `supabase/migrations/20260527174407_impronta_disable_home_fixer_leaf.sql`, `supabase/migrations/20260615194714_add_fk_covering_indexes.sql`, `supabase/migrations/20260615211200_ml_taxonomy_bio_services_i18n.sql` |
| D-105 | normal | open | migrations | **A true from-zero replay would ALSO fail on two unrelated, pre-existing breaks discovered while proving D-104**, unrelated to `agency_taxonomy_settings` table-ordering and not fixed here (out of this task's scope): (1) `20260527063534`'s Sub-Task 0.5 `INSERT INTO profile_field_definitions (..., label, ..., helper, ...)` references columns that a later migration renamed to `label_i18n`/`label_es` and `helper_i18n`/`helper_es` — confirmed live on `qa-journeys`: `information_schema.columns` for `profile_field_definitions` has no `label` or `helper` column today. (2) `20260615211200` is a one-shot, non-idempotent transform (drops `taxonomy_terms.name_en`/`name_es`, `talent_profiles.bio_en`/`bio_es`, etc. after backfilling `_i18n` columns); replaying its non-`agency_taxonomy_settings` statements against a database that already ran it once fails immediately (`column "name_en" does not exist`) because those columns are already gone — this is inherent to the migration's own stated design ("AGGRESSIVE, pre-launch one-shot transform"), not a new defect, but it does mean this file cannot be replayed twice, so a genuine from-zero rebuild still needs a database that has never run it. Neither blocks the journeys program (every database it touches already has the shared history applied), same as D-104's original scoping note. **2026-09-09 re-verification also confirmed a third pre-existing break in the same family**: `20260527174407`'s audit-log INSERT (guarded by D-104, not caused by it) fails on `qa-journeys` today because the live `engine_audit_log` table's columns (`action`/`target_kind`/`target_id`/`payload`) no longer match any migration file's assumed shape (`subject_kind`/`subject_id`/`subject_key`/`operation`/`before_value`/`after_value`) — the same drift D-103 already lists under "engine_audit_log columns"; `20261003000000` and `20261004000000` insert into `engine_audit_log` the same (old) way and would hit the identical error. Not fixed here; still open, tracked by D-103. | `supabase/migrations/20260527063534_tenant_taxonomy_overrides_and_canonical_surface_columns.sql`, `supabase/migrations/20260615211200_ml_taxonomy_bio_services_i18n.sql` |

| D-105 | blocking | open | capacity | **One seat can be given to two people through the waitlist.** `promote_session_waitlist_entry` holds the seat only while an offer is inside its window (`status='offered' AND offer_expires_at > now()`). Accepting writes no capacity allocation, so the moment an entry becomes `accepted` the pool reports the seat free again AND the entry stops counting as an outstanding offer. Reproduced on the isolated branch: promote A succeeds with 0 remaining; promote B is refused as full; set A to accepted; the public remaining count is 1 again; promote B now succeeds. One seat, two people. The migration's own header claims the function exists so two operators cannot both be told yes, which it achieves for the offer window only. Found by adversarial review of the appointments slice. | `20261231001100`, `promote_session_waitlist_entry` |
| D-106 | normal | open | appointments | **The appointments board cannot show a booking's state.** A translated column heading for the state exists in all three languages and is referenced nowhere. The board draws when, with whom, who is serving, the room and what is next, so tentative, confirmed, draft and in progress are indistinguishable; only cancelled and completed surface, and then only inside action text. | appointments list component |
| D-107 | normal | open | appointments | **A successful reschedule says nothing.** On success the dialog closes and refreshes, and the two authored sentences for a move and for an already-at-that-time answer are rendered nowhere. So the operation's idempotent answer, "It was already at that time. Nothing changed.", reads exactly like a real move. | appointments list component |
| D-108 | normal | closed 2026-09-11 (defects-close) | migrations | **Four versions in production's ledger had no file in the tree** (`20260906030451`, `20260907003312`, `20261229000801`, `20261229000802`; D-014 "the from-zero migration replay, four historical files"). Read back read-only through the management API (`statements` column): `…030451` is a second stamp of `20260906030257_product_features_localized_labels.sql` (recovered body diffed against the tree file: identical except two RAISE sentences); `…003312` stores no statements but its file exists in git at commit `7a3cccafa` on the UNMERGED branch `feat/plan-names-that-fit-every-industry`, and production's `product_tiers` carries its effect (Site / Team / Business); `…801` / `…802` are recorded as `applied_via_management_api` and are the original stamps of `…808` / `…809`, renumbered by `ca5f5d4c9` after production had recorded them. Four files now exist with those exact versions: `…030451` asserts the sibling's columns and changes nothing; `…003312` is the recovered body verbatim (value-matched, idempotent; zero `product_tiers` rows on the isolated branch so it touched nothing there); `…801` / `…802` re-assert the grant state inside a `to_regclass` guard so a from-zero replay (where the table is created by `…807`, which sorts later) gets a notice, not an error. Applied to the isolated branch with `journeys:repair` (4 OK); collision check exit 0 (804 local, 800 remote); `npm run db:check` against production now lists only the new `20260911022138` as pending. | `evidence/defects-close/sql/05-prod-ledger-four-versions.out.json`, `evidence/defects-close/sql/06-isolated-ledger-after-four-files.out.json`, `evidence/defects-close/repair-four-ledger-files.txt`, `evidence/defects-close/db-check-production-readonly.txt` |
| D-109 | high-risk | open — owner decision | pricing | **Production data and main disagree on the plan names.** `20260907003312` (D-108) renamed `product_tiers` website/studio/agency to Site / Team / Business on production on 2026-09-07 from the unmerged branch `feat/plan-names-that-fit-every-industry`; the code half of that commit (`web/src/lib/access/plan-catalog.ts` displayName, `web/src/lib/admin/plan-tiers.ts`, en/es messages) is not on main, which still says Website / Studio / Agency. Whichever surface reads the catalogue constant shows one name and whichever reads the table shows the other. Not fixed here: merging that branch or reverting the data is a product decision. | `git show 7a3cccafa --stat`, `evidence/defects-close/sql/05-prod-ledger-four-versions.out.json` |
| D-110 | high-risk | open — owner decision | migrations | **Production never ran `20260408113000` or `20260408150000` although its ledger records both.** Read-only checks 2026-09-11: no `guard_profile_self_update` function, no `profiles_self_update_guard` trigger, no `ensure_profile_for_current_user` function, no `bootstrap_profile_from_auth_email`; `profiles.app_role` is still `NOT NULL DEFAULT 'client'` and `account_status` still defaults to `registered` (the first statement of `…150000` drops both). The isolated branch, replayed from the files, is the first database that ever ran the guard, which is why D-111 surfaced there and never on production. `loadAccessProfile` (`web/src/lib/access-profile.ts`) tolerates the missing RPC (`PGRST202` fallback), so production works by falling back. Same class as D-012: a ledger row is not evidence. `20260911022138` re-creates the guard, the trigger and the four RPCs whole, so the next production push installs the guard there for the first time (every direct writer of these columns in `web/src` uses the service role or acts as staff on another row, checked); the two column defaults stay as they are on production until someone decides to replay `…150000`. | `evidence/defects-close/README.md` (production read-only queries), `20260911022138` |
| D-111 | blocking | closed 2026-09-11 (defects-close) | auth | **Client self-onboarding could not complete** (pos-projects README "Defects found on the way" #1). `complete_client_onboarding()` and the three sibling RPCs (`complete_talent_onboarding`, `complete_talent_onboarding_with_locations`, `ensure_profile_for_current_user`) are SECURITY DEFINER but run with the caller's `auth.uid()`, so the BEFORE UPDATE guard `guard_profile_self_update` reverted `account_status` / `onboarding_completed_at` / `app_role` on the sanctioned transition too: the RPC reported success, the `client_profiles` row was written, the profile stayed `onboarding`, and `auth-routing.ts` bounced every GET to `/onboarding/role`. Reproduced on the isolated branch in a rolled-back transaction (row `b8e341cc…`: after the RPC, `onboarding`, `onboarding_completed_at null`, `client_profile_rows 1`). Fixed at the root by `20260911022138`: a transaction-local flag `tulala.onboarding_rpc` raised by two helpers (`onboarding_transition_begin/end`) whose EXECUTE is revoked from `PUBLIC`, `anon` and `authenticated`, so only a SECURITY DEFINER RPC owned by postgres can raise it; the guard honours the flag for `account_status` / `onboarding_completed_at` and lets `app_role` become only `client` or `talent`, never a staff role; the four RPC bodies are the tree's bodies plus the two PERFORM lines. The migration's own proof block creates two throwaway auth users, asserts a direct self-UPDATE is reverted before, with the flag raised by hand (role stays), and after the RPC in the same transaction; asserts the client and talent RPCs land `active` with their side rows; then rolls its writes back through a caught sentinel. Run with the old guard body in place, the proof fails at the client RPC (`P0001 complete_client_onboarding did not land (onboarding client <NULL>)`). Applied to the isolated branch (`journeys:repair` OK); the same repro now reads `active`. The spec workaround in `POS-projects-collect-a-balance.spec.ts` (service-role status write) is replaced by a read-only assertion. Of the `c08-cus-*` accounts, two were already corrected by hand (identical `onboarding_completed_at` 2026-09-09T04:28:27Z) and a third (`c08-cus-1788943626587`, created after) still carries the defect signature; left as is, the C08 spec makes a new account per run. | `supabase/migrations/20260911022138_onboarding_rpcs_pass_profile_self_update_guard.sql`, `evidence/defects-close/README.md`, `evidence/defects-close/sql/01-objects-after-apply.out.json`, `evidence/defects-close/sql/02-client-rpc-after.rolledback.out.json`, `evidence/defects-close/sql/03-c08-cus-accounts.out.json` |

| D-112 | high-risk | closed 2026-09-11 (wire-messages) | messaging | **Request payment from the Messages surface always answered "This conversation just changed."** `messagingRequestPayment` compared `expectedVersion` with the ORDER's version while the shell sends the conversation's (`active.version`; a record chip carries no order version), so the two agreed only by coincidence. Reproduced live on the isolated tenant (version 5 vs the order's). The action now guards the inquiry's version and takes the order's own version for the basket snapshot and `payment_links.basket_version`. Same run: the shell minted every link at a hard-coded 100 cents ("Full amount" was $1.00); it now sends 0 and the engine reads the order's total for `full`, while a deposit with no figure is refused in a sentence (the sheet has no amount field, MS14 partial). After the fix a real $18.00 link was minted through the UI (`t3tdxujs1jfp6ege2kdf`). | `messaging-engine.ts`, `MessagesShell.tsx`, `evidence/pos-messages/MS15/live.png` |
| D-113 | high-risk | closed 2026-09-11 (wire-messages) | messaging | **On the phone every commerce control opened nothing.** `MessagesShell` returned `PhoneMessages` without `MessagingSheets`, so Actions, Payment link, Cash, Pay at pickup and the record chip set a sheet name that no component rendered (MM02B, MM03, MM05 unreachable live). The sheets are one tree for both shapes now; the phone thread also no longer forces `min-h-[844px]`, which put the composer under the bottom tab bar. | `MessagesShell.tsx`, `PhoneMessages.tsx`, `evidence/pos-messages/MM03/live.png` |
| D-114 | normal | closed 2026-09-11 (wire-messages) | messaging | **Record chips printed the database kind.** `conversation_records.record_kind` (`order`, `class_enrolment`) was the chip's label at all five render sites and on the essentials panel. `dashboard.pos.messages.record.*` now names each kind in en/es/fr and every site reads it; the workspace inbox chips (seam 4) use the same keys. | `copy.ts`, `InboxList.tsx`, `ThreadPane.tsx`, `EssentialsPanel.tsx`, `PhoneMessages.tsx`, `MessagingSheets.tsx` |
| D-115 | high-risk | closed 2026-09-11 (wire-messages) | payments | **`/pay/<code>?status=paid` rendered the Paid card on an OPEN link.** The page trusted the query string before the record; a hand-typed URL showed "Paid" for money never taken. The record alone says paid now; `?status=paid` on an open link is the processing state (Stripe returns the customer before the webhook settles, MC16). Same page printed the ISO instant (`2026-09-11T23:13:51.575…`) where the board reads "19:15"; both clocks are now the venue's `HH:MM`. | `pay/[code]/page.tsx`, `evidence/pos-messages/MC15/live.png`, `MC16/live.png` |
| D-116 | normal | open | messaging | **Ten sheets have no door in the live surface.** `offer` (MS12, MS13), `change` (MS17), `diff` (MS18, MS20), `recover` (MS19, MS19B, MM05), `delivery` (MS23), `resolve` (MS24) and `agency` (MS30) render only under `/c/t/preview?board=`; nothing in `ThreadPane`, `ActionsMenu`, `EssentialsPanel` or `PhoneMessages` calls `setSheet` with those names, and Resolve acts directly without the MS24 sheet. The boards' verdicts are not-wired until each sheet has a control. | `MessagesShell.tsx`, `BoardPreview.tsx` |
| D-117 | normal | open | messaging | **Search does not filter the inbox.** Typing in the inbox search leaves every row in place; submitting opens the search sheet and jumps to the first hit (`messagingSearch`), so MS25's "results show the matching snippet" is not what the surface does. The phone's Actions sheet (MM02B) is also empty: a title and the word Actions. | `MessagesShell.tsx`, `MessagingSheets.tsx` |
| D-118 | normal | open | messaging | **Nothing raises the incoming toast.** `toast` is set only from the preview fixture; no subscription or poll sets it on a real inbox, so MS01, MS31's strip and MM06 cannot happen live. A reader who opens a thread that another filter no longer lists also sees "No conversations yet." in the pane (the row id stays selected after a reload that drops it). | `MessagesShell.tsx`, `IncomingToast.tsx` |
