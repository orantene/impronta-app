# Agenda V2 QA audit — 2026-09-25

## Verdict

Ship is live (`TALENT_AGENDA_V2=all`, bridge + `agendaNow` pin on production). Core smoke **15 pass / 7 skip / 0 fail** after fixture reseed. Soft skips that remain are mostly missing product fixtures (accept-request, conflict-alt, undo-after-save) or clock-gated CTAs — not broken routes.

## Production truth

| Check | Result |
|---|---|
| Flag | `TALENT_AGENDA_V2=all` |
| Pointer | `production` @ `e5f10b09a` (#2257 agendaNow pin) |
| Aliases | `app.tulala.digital` / `tulala.digital` on that deploy |
| Migrations | `db:check` OK (869 applied) |
| QA V2 chrome | Today + Calendar Add + tablist present for `qa-agenda-jor@…` |

## Findings from this audit (fixing)

1. **QA week fixtures were empty** → soft skips for Collect / Release / Open. Reseeded; beauty talent id is now `f385fb27-…`.
2. **Still to collect showed `0.00`** with seeded leg charges — agency `total_client_revenue` empty/zero under talent session. Fix: fall back to `booking_talent.client_charge_total` in `load.ts`.
3. **Hold Release** is on **Attention**, not Today — smoke updated.
4. **Finish/Collect** needs clock after appointment start — smoke pins `agendaNow=…T10:30:00`.
5. **`agendaNow` dropped** on shell `window.location.assign` navigations — tracked in #2260 (Today open + TalentRouter pin).

## Soft skips OK without more seed

Conflict alt, block undo, accept request, deposit CTA, reschedule sheet — need fixtures or session state the week seed does not create.

## Still owner-gated

- Live Jor click-through (Step 3)
- Day-7 legacy delete (≥ 2026-10-02)
- Services `#servicios` swap (explicit `swap`)

## Evidence

`audit-today-after-reseed.png`, `audit-attention-after-reseed.png`, `audit-record-after-reseed.png`.
