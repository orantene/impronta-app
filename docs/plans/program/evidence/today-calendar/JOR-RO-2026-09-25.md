# Jor RO click-through — agent note (2026-09-25)

`TALENT_AGENDA_V2=all` is armed on Production. Live Jor profile `f048e578-…` is **in** the V2 cohort via `all`.

**Agent constraint:** no writes to Jor rows (no seed, no booking mutations, no pay-link mint against her bookings).

**Owner action still required (Step 3):**
1. Sign in as Jor on `app.tulala.digital` / `impronta.tulala.digital`
2. Open `/talent/today`, `/talent/calendar`, one booking record
3. Confirm chrome is Agenda V2 (Today greeting / Calendar Add / Attention) and nothing looks legacy
4. Optional: drop a one-line note here after the session

Day-7 legacy delete earliest was **2026-10-02**; owner waived early Step 4 on 2026-09-25 (legacy agenda branches removed; `CalendarPage.tsx` file retained unused one release).
