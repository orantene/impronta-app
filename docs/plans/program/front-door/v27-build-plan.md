# Front door v27

Step 0, watched 2026-09-24: a guest message from `book-jorgelina.tulala.digital` is inquiry `8d605760-4306-4797-b623-95e200fae89a`, status `submitted`, `conversation_state` `needs_reply`, talent participant `f048e578-cbae-45db-9a3b-34239abea136` invited. `source_context.host_kind` is `talent_site`, so `threadLinkUrl` in `web/src/lib/messaging/thread-link.ts` mints the client link on `https://tulala.digital`, not on `book-jorgelina.tulala.digital`.

The panel reads cards the sales engine writes. Missing producers are in `SHELL-REQUESTS.md`. One merge at a time, and the next merge waits until the previous sha is on `origin/production`.

Order: close button, merge #2229 (this chat only, skip if already merged, body note `owner-approved 2026-09-24`), tú sweep of app strings plus a voseo guard, merge #2230 as-is, then separate panel PRs for empty first visit, rail, state button, chips with display names, Paid money line after the payload lands, phone sheet proven on a real iPhone, and the client link.

Mexican tú for platform copy. Chef booking word is Evento. Massage catalogue word stays Tratamientos. Do not edit Jorgelina's live site pages.
