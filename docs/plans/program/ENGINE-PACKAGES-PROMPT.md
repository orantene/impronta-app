# Tulala engine packages — prompt for a parallel session

Copy everything below the line into a new Claude Code chat opened in `/Users/oranpersonal/Desktop/impronta-app`. It is self-contained. Give it one package at a time (Package 1 first), or run two chats, one per package.

---

You are a principal backend engineer on Tulala (Next.js 16 + Supabase, multi-tenant; project root `/Users/oranpersonal/Desktop/impronta-app`, app in `web/`). The owner has given full standing authorization: no questions, decide and record, run agents with full permission, continue until only genuine external blockers remain. Never ask the owner anything; the closing report lists what is done and what is blocked with the one thing that unblocks each.

## Why you exist

A separate session is re-skinning every screen to the approved design boards (259 boards). Its builders found that many board controls have **no engine behind them**: no table, no writer, no RPC. Each such control is drawn on screen, disabled, with a one-sentence reason in en/es/fr, and recorded as a decision in `docs/plans/program/pos/decisions.md` (D-POS-15 … D-POS-66). Your job is to build the engine for those controls, in packages, **without touching any screen**. When a package lands, the other session flips the disabled controls on.

## Ground rules (each has cost a round trip on this program)

1. **Scope of files.** You may change: `web/src/lib/**`, `supabase/migrations/**`, `web/src/app/(workspace)/[tenantSlug]/admin/**/actions.ts` and `web/src/lib/server-actions/**` (server actions), `web/messages/{en,es,fr}.json` (refusal sentences only, under keys you create), `web/e2e/**` new case files, `web/scripts/verify-*.mjs`, `docs/plans/program/**`. You may **not** change anything under `web/src/components/**`, `web/src/app/(workspace)/[tenantSlug]/admin/**/*.tsx` pages/clients, `web/src/app/globals.css`, or the shell. If a screen must change for your work to be visible, write the exact contract (action name, input, result union, refusal codes) into `docs/plans/program/engine/<package>.md` and stop there; the other session wires it.
2. **Worktree, never the shared checkout.** `git fetch origin && git worktree add /private/tmp/engine-<pkg> -b engine/<pkg> origin/main && web/scripts/setup-worktree.sh /private/tmp/engine-<pkg>`. Never `git switch` in `/Users/oranpersonal/Desktop/impronta-app`. Copy `web/.env.capacity-isolated.local` from `/private/tmp/claude-505/-Users-oranpersonal-Desktop-impronta-app/e30ce090-ce88-474f-861f-a6f78cb4f718/scratchpad/wt-candidate/web/.env.capacity-isolated.local` into your worktree (chmod 600). Never print or commit any value from it.
3. **Database.** Production `pluhdapdnuiulvxmyspd` is READ ONLY for you (reads via `POST https://api.supabase.com/v1/projects/pluhdapdnuiulvxmyspd/database/query` with `read_only:true`, token `SUPABASE_ACCESS_TOKEN` from `web/.env.local`). All schema work goes to the isolated branch `fxlankepwnvelxjrahwk` first: `cd web && npm run journeys:repair -- ../supabase/migrations/<file>.sql`, then prove the objects exist by querying the branch (the `DATABASE_URL` in the isolated env file). Never `npm run db:push`, never `reset_branch`. Migration version = `date -u +%Y%m%d%H%M%S` at the moment you create the file; then `node --env-file=.env.capacity-isolated.local scripts/check-migration-version-collisions.mjs --remote` must exit 0. Conventions for every migration (copy from `supabase/migrations/20261230000700_journeys_atomic_rpcs.sql` and `20261231000300_order_collection_reservations.sql`): `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`; jsonb `{ok, reason, …}` replies; refusals are data, never throws; `REVOKE ALL … FROM PUBLIC, anon, authenticated; GRANT EXECUTE … TO service_role` plus a `has_function_privilege` DO-block; new tables RLS on, staff SELECT via `public.is_staff_of_tenant(tenant_id)`, writes service_role only; **an executable proof block in the same file** that breaks the rule on purpose and asserts the refusal (rolled back via a caught sentinel). `unavailable` is reserved for transport failure and never triggers a second write path.
4. **Money and capacity are authoritative in SQL under `FOR UPDATE`**, replayed by `(tenant_id, operation_key)`; every TS caller passes an idempotency key; race proof scripts (`web/scripts/verify-*.mjs`, guarded by `isolated-target-guard.mjs` + an env flag, ground truth read from tables, exit 1 on any oversell/duplicate/leak) for anything that moves money or seats. Read `docs/plans/program/specs/*.md` and `docs/plans/program/evidence/prove-money/README.md` first.
5. **Refusals are codes**, mapped to sentences in three languages in the message catalogues; a static test asserts every code has all three. Never return English prose from an action. Never a function that answers instead of refusing (a null/absent value must be structurally distinct from a real one; times carry a timezone).
6. **Gates** before every commit, on a private typecheck lane: `cd web && TSC_QUEUE_LOCK=/tmp/tulala-tsc.<pkg>.lock TSC_QUEUE_TICKETS=/tmp/tulala-tsc.<pkg>.tickets npm run typecheck && npm run lint && npm run test:money && npm run test:capacity && npm run test:commands && npm run test:tenant-isolation && npm run test:size-ratchet && npm run test:phase1-i18n` (add `test:scheduling`, `test:reservations`, `test:sessions`, `test:events` when you touch those areas). Capture real exit codes from the commands, never from a wrapper. Never widen a baseline, budget, suppression or type; no casts; no skipped or inverted tests; prove every guard by breaking the rule and watching it go red. Max one typecheck and one dev/test server of your own at a time (this Mac is shared with the other session's builders).
7. **Safety.** Never delete with a wildcard or a relative path (`rm -rf *` after a `cd` trips a guard that stops the owner's work); name the full absolute path. No em dashes in user-facing copy. Never print secrets.
8. **Ship.** Commit per task (`<surface>: <what>`, ending `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`), push `engine/<pkg>`, open a PR to `main` with the acceptance checklist, evidence paths and the proof/race output pasted, then `gh pr merge --merge --admin` once its checks are green (the repo allows admin merge; do not wait on queues). After merge: apply the migrations to production **before** the code deploys: from your worktree `cd web && node --env-file=.env.local scripts/apply-migration.mjs --apply-pending` (this is the sanctioned applier; `db:push` is not linked), verify objects on production read-only, then `git push origin origin/main:production` and run `npm run deploy:smoke`. Schema before code, always; contract-shape changes are code-first.
9. **Record.** Every decision in `docs/plans/program/pos/decisions.md` as the next free `D-POS-n` (read the file; numbering is sequential; the other session also appends, so rebase before you push). Every contract for the UI session in `docs/plans/program/engine/<package>.md`: action name, input shape, result union with every refusal code and its sentence keys, the reader that shows the result, and the board ids it unblocks. Evidence under `docs/plans/program/evidence/engine-<package>/`.

## Existing engine you build on (do not duplicate)

- Orders: `web/src/lib/orders/*` (`purchase.ts`, `settle-at-door.ts`, `identity-policy.ts`, refunds), `web/src/lib/pos/draft.ts` (draft order + lines through `pos_mutate_draft_line`, `pos_apply_draft_totals`, `pos_cancel_draft`, version-locked), `lib/pos/collection.ts` (`pos_reserve_collection` / `pos_settle_collection_reservation`, idempotency key required), `lib/pos/shift.ts` (`pos_shifts` open/close), `lib/pos/sale-read.ts`, `lib/pos/hold-capacity.ts` (`reserve_resource_set_v2`), `lib/payments/*` (Stripe collection adapter, terminal availability, Mercado Pago adapter awaiting credentials), `lib/commands/run.ts` (command lease: `command_claim` / `command_complete`), exceptions inbox `lib/exceptions/*` (`reconcile_collection`, `mint_missing_admissions`).
- Capacity: `capacity_pools` / `capacity_allocations` + RPCs (`upsert_capacity_pool`, `reserve_capacity_batch`, `_capacity_reserve_locked`, `_capacity_move_locked`), `lib/capacity/*`, `lib/resources/reserve-set.ts`.
- Scheduling: `lib/scheduling/reschedule-booking.ts` over `reschedule_booking_set`, `talent_bookings`, `agency_bookings`, `talent_holds`, `talent_booking_hours` (+ proposals, `accept_booking_hours_proposal`), `lib/sessions/*`, `lib/classes/*` (`session_series`, cron materialiser, `promoteFromWaitlist`, `markAttendance`), `lib/pos/classes/*`.
- Tables: `visits`, `space_assignments`, `spaces`/`space_groups`, `venue_service_rules`, `lib/pos/floor*`, `admin/tables/actions.ts`, `admin/reservations/actions.ts`, `lib/preparation/tickets.ts`.
- Events: `admissions` (ticket AND reservation rows), tiers on `talent_offering_variants` with per-session pools, `lib/sessions/admission-token.ts` (signed codes, `GUEST_COOKIE_SECRET`), `_door-actions.ts`, `refund_admission`.
- Projects/clients: `inquiries` / `inquiry_offers` (versions), `booking_deliverables`, agreements/amendments (`lib/projects/*`, `admin/_pipeline-actions.ts`), three client models read through `lib/clients/*` (`customers` is the money identity).
- Catalog: `talent_offerings` (+ `talent_offering_variants`, `requires_identity`, `identity_reason`, `sort_order`, `is_featured`), `catalog_modifier_groups` / `catalog_modifiers`, `tax_categories`, `tenant_promo_codes`, `entitlement_credits` (table exists, no issuance path), `lib/resources/hybrid-combinations.ts` (packages, engine side).
- People: `agency_memberships` (roles viewer/editor/manager/admin/owner; no cashier/host rows), `lib/people/hats.ts`, `lib/saas/roles.ts` (`roleGrantsCapability`, `booking.payment.request`).
- Platform: `platform_settings.workspace_pos_enabled`; workspace POS modes at `agencies.settings.pos.locations.default.modes` (read/write only via `lib/pos/pos-modes-store.ts`); no `locations` table, no device registry.

## Package 1 — POS money & operator engine

Build, in this order (each = migration + lib + action contract + tests + proof; commit per task):

1. **Custom amount line + manager approval** (unblocks POSCustomAmount, POSManagerApproval; D-POS-23). A draft line of kind `custom` with a label and amount written through the existing `pos_mutate_draft_line` (extend the line kind, never a second table), refused above `agencies.settings.pos.approval.custom_amount_limit_cents` unless an approval exists. Approvals: table `pos_approvals (id, tenant_id, order_id, line_id, kind, approver_user_id, method 'pin'|'session', created_at)`; managers set a 4–6 digit PIN stored as a hash in `agencies.settings.people.pins[userId]` via an owner/manager-only action; `pos_approve_custom_amount(tenant, order, line, operation_key, approver, pin)` verifies the hash in SQL, writes the approval, unlocks the line. Refusals: `over_limit`, `pin_invalid`, `not_manager`, `already_approved`, `conflict`.
2. **Lock screen / operator switch** (POSLock, POSChangeServer for the till; D-POS-34). Table `pos_device_sessions (id, tenant_id, device_key, operator_user_id, shift_id, locked_at, unlocked_at, last_seen_at)`; actions `posLockTill`, `posUnlockTill(pin)`, `posSwitchOperator(pin)`; the drawer stays with the shift, the operator changes; every sale line records `operator_user_id` (column on `order_lines`, nullable, additive). Device key is a random id the client keeps in localStorage (contract: the action receives it; you do not touch the client).
3. **Link a booking to a sale** (POSLinkBooking, B03 POSAfterLink; D-POS-26). `pos_link_booking(tenant, order, booking_kind 'talent_booking'|'agency_booking'|'admission', booking_id, operation_key)`: the sale's lines gain `booking_id`/`booking_kind`; paying the sale settles the booking's balance through the existing `booking_transactions` writer (one shell, one paid row; respect `idx_booking_transactions_booking_active` as relaxed by T1-03); refuse `already_linked`, `wrong_tenant`, `nothing_owed`, `conflict`.
4. **Gratuity** (D01–D08 customer display tip states; D-POS-11). A `tip_cents` column on `orders` (additive, default 0) written by `pos_set_tip(tenant, order, tip_cents, operation_key, expected_version)` before collection only; totals rule: tip is outside subtotal/tax, inside total; receipts and `sale-read.ts` expose it; refunds never refund a tip unless the whole order is refunded (state that in the proof). Refusals `already_collected`, `negative`, `conflict`.
5. **Payment links** (POSPaymentLink, POSCollectAnotherWay, MW08_LinkSent; D-POS-27, D-POS-42). Table `payment_links (id, tenant_id, order_id, code, amount_cents, currency, provider 'stripe'|'mercadopago'|'mock', provider_ref, status 'open'|'paid'|'expired'|'cancelled', expires_at, created_by, created_at)`; action `createPaymentLink(orderId, amountCents, idempotencyKey)` reserves the amount through `pos_reserve_collection` (existing) and mints a public URL `/pay/<code>` (add the route under `web/src/app/(public)/pay/[code]` as a server page that redirects to the provider session or the mock; this is the ONE UI file you may add, since it is public and not a board); the reconcile cron (`api/cron/reconcile-collections`) resolves link states; expiry releases the reservation. Refusals `exceeds_outstanding`, `provider_unavailable`, `expired`.
6. **Table operations** (POSJoinTables, POSMergeChecks, POSChangeServer, POSMoveParty split; D-POS-61). RPCs over `visits` + `space_assignments` under lock: `visit_transfer(tenant, visit, to_space, operation_key)`, `visit_split_check(tenant, visit, line_ids[], operation_key)` → new order for the moved lines, `visit_merge_checks(tenant, from_visit, into_visit, operation_key)`, `visit_change_server(tenant, visit, user_id)`; each refuses `not_open`, `space_occupied`, `lines_paid`, `wrong_tenant`, `conflict`. Race proof: two transfers of one visit → one wins.
7. **Waitlist offers with hold timer** (T08, E05 hold expired analogue; D-POS-25). `waitlist_offers (id, tenant_id, waitlist_entry_id, pool_id, offered_at, expires_at, accepted_at, declined_at, allocation_id)`: `waitlist_offer_place(tenant, entry, operation_key, ttl_s)` reserves the seat via `reserve_resource_set_v2` with the offer's key, `waitlist_accept_offer` converts it, expiry (cron `api/cron/expire-orders` gains a reaper) releases it; refusals `no_place`, `expired`, `already_accepted`.
8. **Cash movements and hand-over** (POSCashMovements, POSCashClose note; D-POS-29). `pos_shift_movements (id, tenant_id, shift_id, kind 'paid_in'|'paid_out'|'drop'|'float_add', amount_cents, reason, by_user_id, created_at)`, `pos_shifts.close_note`, `pos_shifts.handed_over_to`; expected cash = float + cash sales + paid-in − paid-out − drops; the close proof asserts the variance math from rows.

Acceptance per task: migration applied on the isolated branch with objects verified; unit tests against a fake `rpc`; static guard for the rule; race script PASS line where money/seats move; action contract file written; gates green; committed.

## Package 2 — Scheduling & projects engine

1. **Series editor and generator** (W10, W40 "+ New series" / "Generate sessions"; D-POS-35). Writers over `session_series` (create/update: cadence, room, instructor, capacity, price, start/end) and an explicit `generateSessionsForSeries(seriesId, untilDate)` that calls the existing materialiser once, idempotently (one pool per session, keyed by `(series_id, starts_at)`); refuse `overlapping_room`, `no_instructor`, `past`.
2. **Substitute instructor / move participant / cancel session with scope** (W39 right panel; D-POS-35). `session_set_instructor(tenant, session, user_id, scope 'this'|'future'|'series')`, `session_move_participant(tenant, admission, to_session, operation_key)` (seat via `reserve_resource_set_v2`, release the old under lock), `session_cancel(tenant, session, scope, reason, operation_key)` (releases pools, marks admissions `cancelled`, opens refund exceptions through the existing refund path for paid seats; never refunds silently). Refusals `sold_out`, `already_cancelled`, `paid_seats_need_refund`, `conflict`.
3. **Cancel appointment + customer manage** (A07, A10, R04/R05; D-POS-36). `cancel_booking_set(tenant, booking, operation_key, reason, by 'staff'|'customer')` over `agency_bookings` + `talent_bookings` + allocations under lock (mirror of `reschedule_booking_set`), applying the workspace's cancellation policy (commercial terms) to compute the refundable amount and opening the refund through the existing path; a signed customer-manage token (`lib/guest-cookie.ts` pattern) so a customer can reschedule/cancel their own booking from the email link (public page contract only; the UI session builds the page).
4. **Project team replace / amendments / milestones** (W48, W46, W47, O06; D-POS-37, D-POS-38, D-POS-43). `project_replace_talent(tenant, booking, from_talent, to_talent, operation_key)` (moves `booking_talent` row + holds, refuses `talent_unavailable`, `already_started`); `amendment_send` / `amendment_discard` over `inquiry_offers` versions (one live version rule already in place; add the explicit send/discard writers with `expected_version`); milestones gain `amount_cents` and a file reference (`booking_deliverables.file_path` on the existing storage bucket pattern in `lib/media`); `project_archive` / `project_reopen` as status transitions with reasons.
5. **Packages and price phases** (P01–P03, P06, E02; D-POS-53). `offering_components (offering_id, component_offering_id, qty, required)` so a package is an offering with components, priced by `lib/resources/hybrid-combinations.ts`; `offering_price_phases (offering_id, variant_id, label, starts_at, ends_at, price_cents)` read by `repriceAndValidate` (a phase never changes a line already priced; record the phase id on the line). Refund of a package refunds by component share (proof). Passes/memberships/gift cards are OUT (owner product decision pending): do not build them.
6. **Booking policy intake and overrides** (W24, W56; D-POS-65). `booking_policy_overrides (tenant_id, offering_id, deposit_bps, cancel_free_hours, no_show_fee_cents)` read by the deposit/cancellation code paths with the workspace default as fallback; `approval_requests (tenant_id, kind, subject_id, requested_by, decided_by, decision, reason)` with `request_approval` / `decide_approval` and a per-role limit table `role_limits (tenant_id, role, action, limit_cents)` consulted by discounts and refunds above the limit.

Same acceptance as Package 1. Add `test:scheduling`, `test:sessions`, `test:reservations` to the gates.

## Closing report (both packages)

One table: task · migration version · objects proven on the isolated branch (query + row) · production applied (yes/no, versions) · race/proof PASS lines · contract file · boards unblocked · commit/PR. Then: blockers with the exact unblocking action; decisions recorded (D-POS ids); anything you left unfinished and why.
