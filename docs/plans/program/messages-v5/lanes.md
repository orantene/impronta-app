# Lane ledger

| Lane | Model | Status | Branch | PR | Gates (exit codes) | Notes |
|---|---|---|---|---|---|---|
| S1 | Claude Opus 5 | PR open | work/msg-s1 | #2061 | lint 0 · test:messaging 0 (48/48) · test:inquiry-workspace 0 (123/123) · typecheck 0 (load 5.5) | One thread-type rule (D-MSG-2): engine writes every staff message to "private"; every client reader excludes internal_note; POS ThreadMessage carries `thread`; static test `client-readers.static.test.ts`; seam D-MSG-2a (RLS by thread only) |
| S4 | Claude Sonnet 5 | PR open | `work/msg-s4` | [#2060](https://github.com/orantene/impronta-app/pull/2060) | `test:messaging` 0 (74/74) · `test:inquiry-workspace` 0 (123/123) · `lint` scoped to every touched file 0 errors (whole-repo `npm run lint` was still queued behind the machine-wide 1-slot lock at hand-off — contended by several other lanes' concurrent lint runs the whole session, confirmed live/progressing via `ps`, not wedged) · `typecheck` not obtained (same queue) | Rename, conversation history, merge, `deriveTasks`. No migration. D-MSG-2..6 in decisions.md: no `subject`/`title` column (event-sourced via `inquiry_action_log`), history is staff-only, merge's paid/confirmed guard covers only `order`+`offer` kinds, assign/handover/resolve/reopen/close-lost now self-log to `inquiry_action_log` (existing RPCs don't), and a tooling note on the node_modules symlink fix mid-session. |
| S7 | Sonnet 5 | PR open | work/msg-s7 | [#2057](https://github.com/orantene/impronta-app/pull/2057) | voice-meta.test.ts 0 (7 pass); thread-token.test.ts 0 (10 pass); messaging+messages-v5 suite 55 pass/2 fail (pre-existing, unrelated — see PR); lint blocked (broken shared node_modules, ESM ignores NODE_PATH); typecheck skipped (load 7.71<9 but queue's tsc binary also broken; declined to bypass the serialisation queue at 5-min load 8.29) | 3 commits: voice message_kind text+metadata.voice, thread-token exp-from-record (D-MSG-6), useMessagingInboxLive replaces POS 12s poll. refreshThreadToken exported, NOT wired into payment/confirm (seam, D-MSG-4). |
| S8 | Sonnet 5 | PR open | work/msg-s8 | [#2064](https://github.com/orantene/impronta-app/pull/2064) | eslint (touched file) 0 · `test:messaging` 0 (95/95) · `test:tenant-isolation` 0 (622/622) | Closed D-MSG-2a: migration `20261231252000_rls_exclude_internal_notes_non_staff.sql` adds `message_kind <> 'internal_note'` to `inquiry_messages_private_select_participant_v2` (client thread) and `message_reactions_select`; staff read via the untouched, separate `inquiry_messages_tenant_staff` policy. Applied to isolated `qa-journeys` only; `pg_policies.qual` + rolled-back transactional client-role probe (0 notes/0 reactions-on-notes seen, 1 ordinary reply seen) in D-MSG-50. Static guard `rls-internal-note-exclusion.static.test.ts` picked up by `test:messaging` glob. Writers untouched. |
| S3 | Claude Opus 5 | PR open | work/msg-s3 | [#2065](https://github.com/orantene/impronta-app/pull/2065) | eslint (touched files) 0 · test:messaging 0 (128/128) · test:inquiry-workspace 0 (123/123) · test:scheduling 0 (361/361) · test:money 0 (1144 pass / 1 skipped, run with the package.json path-quoting bug worked around; the packaged script exits 1 on the base branch, see D-MSG-24) · scoped tsc not obtained (load 12-14 > 9 the whole session) | `messagingConfirmRecord` (`server-actions/messaging-confirm.ts`) -> `confirmRecord` (`lib/messaging/confirm.ts`) + pure `buildConfirmPlan`/`runConfirmChecks` (`confirm-plan.ts`). Offer -> `convertToBooking`, links `project`; draft -> `holdDraftOrderCapacity` + `commitCapacity` (or `completeZeroTotalOrder` when free), links `order`. New refusal `deposit_required` (EN/ES/FR). 35 tests. No migration. Seams D-MSG-22 (draft stays `draft`, sweep hazard) and D-MSG-23 (talent holds keep TTL on POS key). |
| S5 | Claude Opus 5 | PR open | work/msg-s5 | [#2066](https://github.com/orantene/impronta-app/pull/2066) | eslint on touched files 0 · test:messaging 0 (97/97) · test:commands 0 (140/140) · test:money 0 (1156 pass / 0 fail / 1 pre-existing SKIP; lane was unrunnable on the base until the package.json quoting fix, D-MSG-36) · typecheck not claimed (wave rule) | Migration `20261231260000_line_author_confirmation_snapshot.sql` applied to the ISOLATED branch only (collision check 0; in-file DO proof ran: client add / staff update / remove events, cascade). `order_lines` + `inquiry_offer_line_items` gain proposed_by, confirmed_at/by, price_snapshot_cents, catalog_price_cents_at_add, discount_cents/_label, tax_cents/_label; `order_line_events` + trigger; `pos_mutate_draft_line` same signature. `addLine` takes `proposedBy` (guest link → client, send options → staff); `confirmLines` exported (seam, D-MSG-34); `priceDrift` pure (D-MSG-30); diff rows carry proposedBy / changedBy / priceHistory from the events; offer lines carry discount/tax through `updateOfferDraft`; client allow-list static test extended (D-MSG-35). |
| S2 | Claude Opus 5 | PR open | work/msg-s2 | [#2068](https://github.com/orantene/impronta-app/pull/2068) | eslint on every touched file 0 · scoped tsc over the 22 touched files (temp tsconfig, niced; whole-repo typecheck not run per wave rule) 0 · test:messaging 0 (107/107) · test:inquiry-workspace 0 (123/123) · test:money file list with the pre-existing quote bug fixed in-shell 0 (1145 tests, 1144 pass, 1 pre-existing skip; `npm run test:money` itself exits 1 on origin/main before running anything, D-MSG-42b) · test:events 0 (193) · test:reservations 0 (145) · test:sessions 0 (173) · test:scheduling 0 (361) | Migration 20261231255000_conversation_record_state.sql applied to the ISOLATED branch (columns + CHECKs + `messaging_sync_record_state` / `messaging_derive_order_state`, SECURITY DEFINER, anon/authenticated denied, ledger row present; a real paid order synced twice → linked 1, paid/confirmed, version unchanged on the second call). `syncConversationRecord` wired into 20 writer paths (D-MSG-38). Chips read payment_state / fulfilment_state / record_date (`recordDate` added to RecordChip). `deriveAutoResolve` exported, cron is a seam (D-MSG-41). Collision + money-lane tooling notes (D-MSG-42). Not verified live on a tenant (integrator only).

| S6 | Claude Sonnet 5 | PR open | work/msg-s6 | [#2069](https://github.com/orantene/impronta-app/pull/2069) | `test:messaging` 0 (122/122) · `test:money` 0 (1144/1145, 1 pre-existing skip; package.json's own `test:money` script has a pre-existing quoting bug unrelated to this lane — a stray unquoted space merges two file args into one nonexistent path — worked around with a corrected copy of the same command to get a real run) · `test:tenant-isolation` 0 (622/622) · eslint (scoped, 8 touched + 3 new test files) 0 errors · typecheck not attempted (machine load 12-16, over the 9 threshold all session) · no migration | Cancel/refund/outside-payment from the thread (D20/D07/D10): `messagingPreviewCancel`, `messagingCancelRecord`, `messagingRefund`, `messagingRecordOutsidePayment` in NEW `lib/server-actions/messaging-money-actions.ts` (messaging-engine.ts was at 797/800). Cancel/refund route through `refundOrderLines`/`executeBookingRefund`, NOT `cancel_booking_set` — that RPC cancels `agency_bookings`, which no Messages v5 RecordKind resolves to (D-MSG-40). 6 new permission keys in `lib/messaging/money-permissions.ts`, open-by-default-per-tenant-per-key (`staff_permissions`, never seeded/gated anywhere before this lane — D-MSG-40); enforces in the 4 new actions plus `messagingCloseLost`/`messagingInternalNote`. `refusal.no_payout_receiver` added en/es/fr (D-MSG-41, gap NOT reproduced by static reading — mapped defensively). D-MSG-42: partial/keep-mode cancels release capacity but don't void `admissions.status` (real gap, no writer exists for it, filed). D-MSG-43/44/45 record every other seam. 33 new tests (money.test.ts, money-permissions.test.ts, messaging-money-actions.static.test.ts) all green. |
| KIT | Claude Opus 5 | PR open | work/msg-kit | [#2073](https://github.com/orantene/impronta-app/pull/2073) | eslint on every touched file (kit + route page, `--max-warnings 0`) 0 · scoped tsc over the kit + the route page 0 · `test:messaging` 0 (266/266, the lane now includes the 89 kit tests) · `verify:ui-messages` 0 · `test:phase1-i18n` 0 (20/20) · no dev server, no build, no whole-repo lint/typecheck (integrator) | `web/src/components/messages-v5/kit/`: `tokens.css` (v5.1 tokens scoped under `.msgv5`, every colour a `--msgv5-*` var, no inline styles), 30 components over `lib/messaging/types.ts` + S4 tasks + S2 chip states + S3 conflicts, `copy.ts` (317 EN/ES/FR strings under `dashboard.messagesV5.*`, refusals via `dashboard.pos.messages.refusal.*`), `preview/KitPreview.tsx` at `/c/t/preview?kit=1` (dev only, 390/1194/1440), `kit-tokens.static.test.ts`. D-MSG-60..66. |
| L2 | Claude Opus 5 | merged wave A | work/msg-l2 | [#2078](https://github.com/orantene/impronta-app/pull/2078) | eslint on every touched file (`--max-warnings 0`) 0 for all messages-v5 + messaging-sheets + route files; `InboxPage.tsx` 0 errors / 21 pre-existing warnings · scoped tsc over screens + shell + mount files (temp tsconfig, niced) 0 errors in touched files (17 pre-existing `google` namespace errors in unrelated transitive files the scoped config cannot resolve) · `test:messaging` 0 (294/294, includes the 28 new L2 tests) · `test:phase1-i18n` 0 (20/20) · `verify:ui-messages` 0 · no dev server, no build, no whole-repo lint/typecheck | Shell container + thread + next step + composer. `screens/contracts.ts` (pushed first), `shell/MessagesV5Shell.tsx` (three / two + drawer / one column by container width), `screens/Thread.tsx` + `ThreadCards.tsx` + `thread-stream.ts`, `NextStep.tsx` routing table, `composer-machine.ts` + `ComposerWire.tsx`, `IdentityCaptureWire.tsx`, placeholders for L1/L3/L4 slots, `messagingThreadLink` action, 107 EN/ES/FR keys under `dashboard.messagesV5.shell.*`. Env flag `NEXT_PUBLIC_MESSAGES_V5=1`; preview `/c/t/preview?screen=shell`. D-MSG-70..79; coming seams listed in D-MSG-72. |
| L1 | Sonnet 5 | merged wave A | work/msg-l1 | [#2075](https://github.com/orantene/impronta-app/pull/2075) | eslint on every touched file (`--max-warnings 0`) 0 · `test:messaging` 0 (309/309, now also covers `src/lib/messages-v5/*.test.ts`) · `test:phase1-i18n` 0 (20/20, not required, checked anyway) · `verify:ui-messages` 0 (not required, checked anyway) · no whole-repo lint/typecheck, no dev server, no build (wave rule) | Inbox pane (D01-D03, D11, M01, M10): `web/src/components/messages-v5/screens/Inbox.tsx` + `Inbox.render.test.tsx` (11 tests, desktop/mobile x loading/failed/empty(needs,wait,all,search)/ready/grouping/counts), pure helpers `web/src/lib/messages-v5/inbox-view.ts` + tests (20 tests): `isNeedsAction`, `rowsForSegment`/`inboxSegmentCounts`, `applyInboxFilters`, `searchInboxRows`, `groupInboxRows`. Cherry-picked L2's `screens/contracts.ts` (b989aad47) onto this branch unmodified. D-MSG-80..83: needs-action rule (payment > hold > unassigned > reply > follow-up priority), locally-extended `InboxScreenProps` (`now?: Date`, additive, not an L2-file edit) + a flagged gap in `onSearch` (one callback covers both client narrowing and the Enter-triggered server search), `test:messaging` script fixed to also run `src/lib/messages-v5/*.test.ts` (closes a gap that left S7's `use-inbox-live.test.ts` ungated), 9 new EN/ES/FR catalogue keys (`inbox.empty.{waitTitle,waitBody,allTitle,allBody}`, `inbox.groups.{today,yesterday,paymentIssue,holdExpiring,followUpDue}`). |
| L3 | Claude Sonnet 5 | merged wave A | work/msg-l3 | [#2076](https://github.com/orantene/impronta-app/pull/2076) | eslint on every touched file 0 · `test:messaging` 0 (310/310) · typecheck not attempted (machine load 10.8-11.9, over the 9 threshold all session) | `ContextPanel`/`ContextDrawer`/`DetailsSheet`/`ClientSheet` (boards D01, D11, M04, D15). L2's `screens/contracts.ts` copied byte-for-byte then extended additively (D-MSG-91): `ContextPanelExtra` (items/money/files/notes/follow-up, all optional, empty state when absent) and `ClientSheetPropsExtra` (`matchState` for the D15 two-matches/refusal/Save editor; absent falls back to L2's minimal summary + Edit contact). `lib/messages-v5/context-view.ts`: pure helpers — `itemsLabelKeyForPreset` resolves the real 20-preset table (`lib/words/presets.ts`), not the coarse `workspace_type` (D-MSG-90); `moneySummary`/`summaryFor`/`clientHistoryLabel` (omits, never fakes, the history line — `Essentials` carries no rollup, D-MSG-92)/`itemFlagsFor`. `HISTORY_KINDS` gains `client_edit` + `logClientEdit` (D-MSG-93, metadata carries only field names). +52 EN/ES/FR strings. `test:messaging` glob extended to `src/lib/messages-v5` (D-MSG-94). No migration, no new writer. |
| L4 | Claude Sonnet 5 | merged wave A | work/msg-l4 | [#2077](https://github.com/orantene/impronta-app/pull/2077) | eslint on every touched file (`--max-warnings 0`) 0 · `test:messaging` 0 (307/307) · scoped tsc over the 15 touched/read files (temp tsconfig, niced) 0 at load 8.51 and again 0 at load 13.83 (re-run after a later CSS edit; both real exit codes, neither faked) · `kit-tokens.static.test.ts` still 0 (6/6) after the `tokens.css` append · no dev server, no build, no whole-repo lint/typecheck | `web/src/components/messages-v5/screens/`: `RenameInline.tsx` (D13/M09, split into pure `RenameInlineView` + a thin stateful wrapper — this kit's render tests are `renderToStaticMarkup`-only, so the async phase had to live where it's a prop), `HistorySheet.tsx` (D23, reuses kit `DaySeparator`/`.pn-sec`/`Btn`/`Pill`; Export greyed+disabled, D-MSG-10x seam), `TasksTray.tsx` (D23, the "+N other tasks" popover over S4's `DerivedTask[]`), `MergeCard.tsx` (D14, three-option "Same person?" card). Cherry-picked L2's contracts.ts (`b989aad47`) and extended it additively: `RenameInlineProps.generated?`, `MergeCardProps.onKeepSeparate?` (D-MSG-100). `isGeneratedName` added to `lib/messaging/inquiry-name.ts` (D-MSG-101); `duplicateHint` added pure in new `lib/messages-v5/duplicates.ts` (D-MSG-102, SEAM: needs a caller-supplied `hasOpenConversation` per match, engine doesn't compute it yet). Fixed a real `test:messaging` coverage gap: `src/lib/messages-v5/*.test.ts` was never globbed by any lane, including S7's pre-existing `use-inbox-live.test.ts` (D-MSG-103). Copy: `dashboard.messagesV5.history/.rename/.merge/.tasks.*`, EN/ES/FR, `copy.test.ts` still 6/6. Small additive `tokens.css` append for two row shapes the kit had none of (D-MSG-104). 29 new test cases across 6 files (3 `inquiry-name.test.ts` + 5 `duplicates.test.ts` + 21 render tests), all green, `test:messaging` overall now 307/307. |
| L5 | Claude Opus 5 | PR open | work/msg-l5 | [#2084](https://github.com/orantene/impronta-app/pull/2084) | eslint on every touched file (`--max-warnings 0`) 0 · `test:messaging` 0 (435/435) · `test:phase1-i18n` 0 (20/20) · `verify:ui-messages` 0 · scoped tsc over the 9 touched files + the shell (temp tsconfig, machine load 13 so no whole-repo typecheck) 0 (after D-MSG-126) | Items picker (D05/M03/D17/D21) + Times sheet (D09) through the registry seam; pure plan `lib/messages-v5/items-picker.ts` (13 tests) + `runSendPlan` on a fake engine; six view states in `ItemsPicker.render.test.tsx` (23 tests); readers `messaging-items.ts` (catalog, person slots); writers are the POS's own (`messagingEnsureSharedDraft`, `posAddLine`, `posAddCustomLine`, `rosterAddTalent`, `messagingSendOptions`). D-MSG-120..126. Seams: tables have no card kind or non-booking writer (listed, not sendable); talent rates not read; offering variants not chosen; expired_hold re-send does not preselect the person. |
| L6 | Claude Sonnet 5 | PR open | work/msg-l6 | (opening) | eslint on every touched file (`--max-warnings 0`) 0 · `test:messaging` 0 (428/428) · `test:money` 0 (1156 pass / 0 fail / 1 pre-existing skip) · `message-key-usage.static.test.ts` 0 (5/5) · `client-readers.static.test.ts` 0 (unchanged, still passing) · scoped tsc not attempted (load 5.8-8, but the lane brief's own GATES section says no whole-repo lint/typecheck; ran eslint per touched file instead) | Offer editor sheet (D06/D18/M05 phone 1): `screens/sheets/OfferEditor.tsx` (`OfferEditorView` pure + `OfferEditorSheet` stateful wrapper), registers `create_offer` + `revise_offer` off the SAME component (D-MSG-130). `lib/messages-v5/offer-draft.ts` (pure draft reducer, cents-based, 17 tests) + `offer-compare.ts` (pure version diff, 6 tests, SEAM D-MSG-132: only diffs against the currently-loaded draft, cross-version fetch not wired). New staff reader `loadOfferForEditor` (`lib/messaging/sheets.ts`, D-MSG-133) + 6 new writers in NEW `lib/server-actions/messaging-offers.ts` (D-MSG-134, split out because `messaging-engine.ts` was already at the 800-line ratchet cap, same move S6 made). Revise-an-accepted-offer goes through `counterOffer`, not `reopenOfferForAmendment` (engine-verified: the latter only takes a `sent` offer) — D-MSG-135. Price drift wired for real via `talent_offerings.amount_cents` (D-MSG-136). Reuses (not reimplements) the admin shell's pure `offer-local-snapshot.ts` / `offer-save-state.ts` (D-MSG-137). 12 render tests cover empty/loading/ready/busy/refused/conflict states. 41 EN/ES/FR keys under `dashboard.messagesV5.offer.*`. No migration.
| L7 | Claude Sonnet 5 | PR open | work/msg-l7 | TBD | eslint on every touched file (`--max-warnings 0`) 0 · `test:messaging` 0 (432/432) · `test:money` 0 (1157 tests, 1156 pass / 1 pre-existing skip) · `test:phase1-i18n` 0 (20/20) · `verify:ui-messages` 0 · scoped tsc NOT attempted (machine load 18-26 all session, over the 9 threshold) · no dev server, no build, no whole-repo lint/typecheck | Payment request + cancel/refund sheets (D07/D20, M05/M10 phone 2). `screens/sheets/PaymentRequest.tsx` registers "request_payment" (already in `ShellActionId`); `screens/sheets/CancelRefund.tsx` registers two NEW ids, `cancel_record` and `refund` (additive on `contracts.ts`'s union and `NextStep.tsx`'s `ROUTES`, both lines noted in `NextStep.tsx` itself). Both sheets split View/Wrapper across TWO FILES (`*.view.tsx` / plain `.tsx`, D-MSG-146) so `node:test` render tests (no bundler) don't crash on the wrapper's `server-only`-chained imports. Pure helpers + 17 tests in NEW `lib/messages-v5/payment-view.ts`. `lib/messaging/sheets.ts`'s `OfferRow` gained `depositPct`/`depositAmountCents` (additive). `lib/messaging/money.ts` gained `loadRefundableTransaction`; `messaging-money-actions.ts` gained `messagingLoadRefundableTransaction` (both additive, D-MSG-145). 16 render tests across 6+ states per sheet. Copy: `dashboard.messagesV5.paymentRequest.*` + `.cancel.*`, EN/ES/FR (61 keys/locale). No migration. Seams D-MSG-141/142/143 (appointment/reservation/etc. recordId is not an orders.id; POS collect is a deep link, not embedded `CollectSheet`; outside-payment amount ignored on the non-order fallback). |
| L8 | Claude Sonnet 5 | PR open | work/msg-l8 | (opening) | eslint on every touched file (`--max-warnings 0`) 0 for `lib/messages-v5/confirm-view.ts`, `components/messages-v5/screens/sheets/ConfirmRecord.tsx`, their test files, `sheets/index.ts` and `kit/copy.ts` · `test:messaging` 0 (424/424, up from 310 baseline — includes this lane's 34 new tests) · typecheck not attempted (machine load 11-14, over the 9 threshold all session) · no dev server, no build, no whole-repo lint | `ConfirmRecordSheet` registers `"confirm"` in `sheet-registry.tsx` (shell/NextStep untouched — `dispatch()` already checks the registry first). Wraps S3's `messagingConfirmRecord` directly; source picker + deposit gate built from S2's chips + the existing `messagingLoadOffers` action, no new reader (D-MSG-151/152). "Checked just now" names categories only ("will be checked on confirm"), not a live ✓, since the availability readers are server-only and unexported (D-MSG-153); the real check's `unavailable` conflicts render verbatim on refusal. `ConfirmOrderButton` exported for D08's basket card (not yet mounted). New `lib/messages-v5/confirm-view.ts` (`confirmSourceOptions`, `depositGate`, `createdListFor`, `checkedCategoriesFor`, `overrideReasonValid`) with 12 unit tests; `ConfirmRecord.render.test.tsx` covers the six states (idle, deposit-blocked, override-enabled, busy, unavailable-conflict, already/done) plus draft-vs-offer copy, loading, empty and mobile. `kit/copy.ts` gains a `confirm` section, 32 EN/ES/FR keys under `dashboard.messagesV5.confirm.*`. No migration. D-MSG-150..154. |
| L10 | Claude Sonnet 5 | PR open | work/msg-l10 | TBD | eslint on every touched file (`--max-warnings 0`) 0 · `test:messaging` 0 (539/539, includes this lane's 17 new tests) · `test:design-system` 0 (127/127, POS render tests) · `verify:ui-messages` 0 · `test:phase1-i18n` 0 (20/20, confirms new `pos.*` keys are read) · scoped tsc not attempted (machine load 11-12, over the 9 threshold all session) · no dev server, no build, no whole-repo lint/typecheck | `components/messages-v5/pos/PosMessagesDock.tsx`: the counter's Messages v5 mount, This client / Inbox tabs + Return-to-sale strip, dock (>=1100) / drawer (<1100, also the phone overlay) via `shouldDockBesideSale`. Additive shell prop `hideInboxRail` on `MessagesV5ShellProps` (D-MSG-172) hides the inbox rail so the dock's own tabs are the only way to Inbox; shell internals untouched. `PosPhoneNav.tsx` draws the M06 phone bar (Sale/Orders/Messages/Clients/More) but is NOT wired into `pos-client.tsx`'s real phone chrome yet (D-MSG-174, no `clients` destination exists). `lib/messages-v5/pos-continuity.ts` (9 tests): continuity params/hrefs, `findConversationForOrder` (reuses `InboxRow.recordChips`, no new reader), `shouldDockBesideSale`. Two link sites edited minimally for D-MSG-175: `PosModeSwitch.tsx`'s `openMode`/`goWorkspace` now carry `?inquiry=`/`?order=` off the current url. `messages-view.tsx` resolves `initialCustomerInquiryId` server-side via the existing `messagingLoadInbox` action (no new reader); `MessagesModeClientProps` gained `tenantSlug`/`currentUserId`/`openOrderId`/`initialCustomerInquiryId` (additive). Copy: `dashboard.messagesV5.pos.*`, 9 EN/ES/FR keys. No migration. Seams D-MSG-173 (the live sale is a separate route, not mounted beside the dock yet) and D-MSG-174 (phone bar not wired into the real counter chrome). |
