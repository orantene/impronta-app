# Decisions (append only)

D-MSG-1 · 2026-09-17 · Owner decisions 1–14 recorded in README.md are binding.

D-MSG-2 · 2026-09-17 · One thread-type rule. `inquiry_messages.thread_type` "private" is the CLIENT thread (staff + client/guest); "group" is the TALENT thread (staff + invited talent). Every staff-authored POS message, replies, cards and internal notes alike, is written to "private" (`threadTypeForStaffMessage` in `web/src/lib/messaging/thread-rule.ts`); the engine never writes "group". Internal notes are `message_kind = 'internal_note'` on "private" so staff see one conversation, and EVERY client-facing reader (workspace client page, `/api/client/messages`, guest dock + `/c/[inquiryId]`, `/c/t/[token]`, and the talent inbox reader) excludes that kind. Staff readers (POS `loadMessagingThread`, classic `loadInquiryMessages`) read everything and each POS ThreadMessage now carries `thread`. Evidence for the rule: before S1 the engine wrote replies to "group", which no client surface reads (a POS reply never reached the client), and wrote notes to "private", which `loadInquiryMessages` and the guest reader served unfiltered (a note reached the client). The DB trigger `messaging_touch_inquiry_from_message` already returns early on `internal_note`, so a note never flips conversation state. Enforced by `web/src/lib/messaging/client-readers.static.test.ts`.
D-MSG-2a · 2026-09-17 · SEAM (no migration in S1): RLS policy `inquiry_messages_private_select_participant_v2` (supabase/migrations/20260920000000_rls_messaging_coverage.sql) lets a client participant or `inquiries.client_user_id` SELECT any "private" row by thread alone; it has no `message_kind <> 'internal_note'` clause, so a client with a direct PostgREST/realtime query (not through our readers) can read staff notes. Every shipped client reader is server-side and now filters, so the app does not leak, but the policy should add the clause in a later migration. `message_reactions` policy (20260513210938) has the same shape. Filed for the integrator.
D-MSG-2b · 2026-09-17 · The classic admin shell (`admin-2.tsx`, `loadInquiryMessages` staff audience) now receives POS internal notes on the client tab (they were already there before S1); it has no `internal_note` renderer, so they show as plain text bubbles. Cosmetic, staff-only, resolved by D10 (old shells removed after v5 QA).

D-MSG-3 · 2026-09-17 (S4) · No `subject`/`title`/`project_label` column exists on `inquiries` —
grepped every `ALTER TABLE public.inquiries` and the init `CREATE TABLE` across all of
`supabase/migrations/*.sql`; the only free-text columns are `contact_name`, `company`,
`message`, `staff_notes`, none of which fit. Per Principle 0 / lane rules, `messagingRename`
does not add a column. Instead the internal name is event-sourced off `inquiry_action_log`
(`action_type = 'messaging_rename'`, `metadata: { old, new }`); the current name is the latest
successful rename's `metadata.new`, falling back to `contact_name` (the existing fallback
`loadMessagingInbox.subject` already uses). The write is still version-locked on the inquiry's
own `version` column even though no inquiry column's *value* changes — see
`web/src/lib/messaging/inquiry-name.ts` and `rename.ts`.

D-MSG-4 · 2026-09-17 (S4) · `inquiry_action_log`'s own migration comment says the table is
"Never user-visible in Phase 1" (distinct from `inquiry_events`, which is described as
user-visible/success-only). `loadConversationHistory` reads this as "never shown to the
CLIENT", not "never shown to staff" — it is a staff-only, `staff()`-guarded workspace reader
(`web/src/lib/messaging/history.ts`), not a customer-facing surface. Flagging this explicitly
since it is an interpretation of another lane's/an earlier migration's comment, not a rule this
lane wrote.

D-MSG-5 · 2026-09-17 (S4) · `messagingMerge`'s "refuses if either has a paid or confirmed
record" guard is real only for `order` (`orders.status` paid/fulfilled/partially_refunded) and
`offer` (`inquiry_offers.status` accepted) — the two record kinds with an actual status column
reachable in one hop. `appointment` / `reservation` / `class_enrolment` / `tickets` / `project`
have no dedicated status table wired into Messages v5 yet; `RecordChip.paymentState` /
`fulfilmentState` are `null` everywhere in the existing engine too (see `inbox.ts loadChips`,
`essentials.ts` — pre-existing, not introduced by S4). Those five kinds are **not checked** by
the merge guard; a merge that should have been blocked on one of them will not be. Documented
in `web/src/lib/messaging/merge.ts`, not silently assumed safe. A future lane that wires a
status table for one of these kinds should extend `eitherHasPaidOrConfirmedRecord`.

D-MSG-6 · 2026-09-17 (S4) · `loadConversationHistory` needs assignment/handover/resolve/
reopen/close-lost events, but `messaging_assign_owner`, `messaging_set_conversation_state` and
`messaging_close_lost` (the existing RPCs in `20261231222000_pos_messaging_state.sql`) write
only to `inquiries`, never to `inquiry_action_log` or `inquiry_events`. Editing those RPCs
would be a migration; instead `messagingAssignOwner`/`messagingHandOver`/`messagingResolve`/
`messagingReopen`/`messagingCloseLost` in `messaging-engine.ts` now write a best-effort
`inquiry_action_log` row themselves, right after the RPC succeeds (never before, never blocking
the RPC's own result on the log write). `messagingAssignOwner` and `messagingHandOver` share
one `assignOwnerCore(input, handover: boolean)` so the log can tell the two apart.

D-MSG-7 · 2026-09-17 (S4) · Tooling note: this worktree's `web/node_modules` symlink was found
pointing at an incomplete install (missing `tsx`, `eslint` binaries; `next`'s compiled
`server-only` stub directory was empty), so `npm run test:messaging` / `test:inquiry-workspace`
/ `lint` all failed with `ENOENT`/`MODULE_NOT_FOUND` — reproduced identically on pre-existing,
untouched files (`inbox-unread.test.ts`, `sheets.test.ts`), so it was environmental, not this
lane's code. The coordinator repointed the symlink mid-session; all three gates then passed
clean (see lanes.md for exit codes). Logic was verified independently beforehand with a
throwaway `server-only`-stub `--require` shim (not committed) run against `src/lib/messaging/*.test.ts`
directly via `npx tsx --test`: 74/74 passing before the symlink fix, confirming the earlier
failures were infra, not S4 code.

D-MSG-8 (S7) · 2026-09-17 · S7 · Voice notes insert as `inquiry_messages.message_kind = "text"`, never `"voice"`. `inquiry_messages_message_kind_check` (supabase/migrations/20261231222000_pos_messaging_state.sql) never listed `"voice"` — `insertVoiceRows` (web/src/lib/server-actions/voice-notes.ts) was writing a value the CHECK would reject wherever it's actually enforced. No migration: the voice identity already lives on `metadata.voice` (web/src/lib/messages/voice-meta.ts's `readVoiceMetaFromMessageMetadata`), so every bubble renderer now detects a voice message from that metadata, not from `message_kind`. Touched: ClientMessagesShell.tsx, talent-thread-stream.tsx. NOT touched: `src/components/admin/shell/internal/messages/admin-4.tsx` — grepped for importers, found none; it's an unreferenced mockup-catalog file, same family as the other `internal/messages/*` numbered files, not live code.
D-MSG-9 (S7) · 2026-09-17 · S7 · Thread link expiry (owner decision 6) is computed once at mint time and baked into the token payload as an absolute `exp` (epoch ms); `verifyThreadToken` compares `now` against `payload.exp` only — it never re-queries the DB, staying a pure/DB-free check on every guest request. `resolveThreadTokenExpiry` (web/src/lib/messaging/thread-token.ts) takes 30 days after the LATEST live `conversation_records` date for the inquiry: `orders.created_at` for kind `order`; `admissions.starts_at` for `appointment` / `reservation` / `tickets` / `class_enrolment` (admissions is the one booking-holder table across Sessions & Classes, Reservations and Events/Ticketing — confirmed via web/src/lib/reservations/store.ts's `seatWalkIn`, which inserts a reservation directly as an admissions row carrying `starts_at`). `project` / `offer` have no date column anywhere yet and are skipped (not an error) — a record kind with no date semantics simply doesn't extend the window. No links, or none resolve -> falls back to `mint + 30d`, matching the pre-D-MSG-6 behaviour. Tokens minted before `exp` existed in the payload fall back to `iat + 30d` on verify, so nothing already issued breaks on deploy.
D-MSG-10 (S7) · 2026-09-17 · S7 · SEAM, not wired in: `refreshThreadToken(admin, inquiryId, tenantId)` (web/src/lib/messaging/thread-token.ts) mints a token with the D-MSG-6 expiry and is exported for "re-issued by any new payment or confirmation" (decision 6, second clause). This lane does not call it from any payment/confirm path — a follow-up lane wires it into wherever those actions land (messaging-engine.ts and/or the payment webhook/confirm handlers) and hands the refreshed token to whatever surface shows the customer their link.

D-MSG-50 (S8) · 2026-09-17 · S8 · Closed the D-MSG-2a seam. Migration `supabase/migrations/20261231252000_rls_exclude_internal_notes_non_staff.sql` recreates two policies, newest-wins audit across every migration touching `inquiry_messages`/`message_reactions` (grepped "ON public.inquiry_messages" / "ON public.message_reactions"): (1) `inquiry_messages_private_select_participant_v2` (client/coordinator SELECT on the 'private' thread, live version was from `20260920000000_rls_messaging_coverage.sql`) — added `AND message_kind <> 'internal_note'` to the USING clause; staff keep full read via the separate, untouched `inquiry_messages_tenant_staff` (FOR ALL, `is_staff_of_tenant(...)`) policy, which is OR'd in as its own permissive policy. The 'group' (talent) thread policy needs no change — D-MSG-2 already guarantees staff never write `internal_note` to `thread_type = 'group'`, so it's structurally unreachable there. Guests never read under RLS at all (`20261017090000`: guest reads are service-role only) — no guest policy to touch. (2) `message_reactions_select` (live version from `20260615200003_rls_initplan_optimize.sql`) — added `AND m.message_kind <> 'internal_note'` to the EXISTS join; this policy had no staff/tenant branch to begin with (staff reactions read, if reachable at all, goes through the same participant check as everyone else — pre-existing gap, not introduced or fixed here), and the reaction UI (`MessageReactions.tsx`/`MessageReactionMenu`) is unwired dead code (same family as `admin-4.tsx`, D-MSG-8) — but the RLS gap was real for a direct query, same class of seam as D-MSG-2a, so closed unconditionally. Applied to the isolated `qa-journeys` branch only (`journeys:repair`), never production. Proof: `pg_policies.qual` for both policies now contains `message_kind <> 'internal_note'::text` (pasted in lanes.md); a rolled-back transactional probe simulating an authenticated client (`SET LOCAL ROLE authenticated` + `request.jwt.claims` sub = the inquiry's `client_user_id`) inserted a real internal-note message + a reaction on it + an ordinary reply, then read all three under that RLS context: client saw 0/1 internal notes, 1/1 ordinary replies, 0/1 reactions on the note; a plain (RLS-bypassing) read confirmed both test rows existed. Nothing persisted (ROLLBACK). Static guard: `web/src/lib/messaging/rls-internal-note-exclusion.static.test.ts` greps the newest migration defining each policy name for the `message_kind <> 'internal_note'` predicate, so a later migration cannot silently drop it; registered automatically via the existing `test:messaging` glob (`src/lib/messaging/*.test.ts`), no package.json change. Writers untouched (INSERT policies, `messaging-engine.ts`, `insert-message.ts` — none of this lane's changes touch a write path).
D-MSG-20 (S3) · 2026-09-17 · `messagingConfirmRecord` lives in a SIBLING server-actions file, `web/src/lib/server-actions/messaging-confirm.ts` (engine is at 786/800 lines); the mechanics are `web/src/lib/messaging/confirm.ts` and the pure recheck is `web/src/lib/messaging/confirm-plan.ts`. Record kinds it links: source `offer` -> `project` (the `agency_bookings` row `convertToBooking` creates; `conversation_records_kind_known` has no `booking` kind and `project` is what the existing chips already use for agency bookings); source `draft` -> `order` (the shared draft's own `orders.id`). Idempotency for `already` is the LIVE `conversation_records` row of that kind (offer: any live `project` link on the conversation, or `inquiries.status` in booked/converted; draft: a live `order` link for THAT orderId, or `orders.status` paid/fulfilled), plus the inquiry version lock. No new column, no migration. New refusal `deposit_required` added to `MessagingRefusal`, `MESSAGING_REFUSAL_CODES` and the three catalogues; `refusals.static.test.ts` still green.
D-MSG-21 (S3) · 2026-09-17 · "Paid deposit exists" is read from two places and nowhere else: `payment_links` (`inquiry_id` = this conversation for an offer, `order_id` = the draft for a draft) with `status = 'paid'`, and `booking_transactions` with `source_inquiry_id` = this conversation and `status = 'paid'`. `agency_bookings.deposit_paid_at` cannot be used because for an offer the booking does not exist until the confirm creates it. The deposit RULE itself is the offer's `deposit_pct`/`deposit_amount_cents` (offer source) or any line offering with `reserve_mode = 'deposit'` and `deposit_pct > 0` (draft source; same rule `purchase-policy.ts` derives). Override: `overrideReason` trimmed >= 8 chars writes `inquiry_action_log` `messaging_confirm_override` BEFORE anything is created; a shorter reason is `deposit_required`, not `invalid` (the server action deliberately does not length-check it in zod).
D-MSG-22 (S3) · 2026-09-17 · SEAM: a confirmed DRAFT that still owes money keeps `orders.status = 'draft'`. The POS moves draft -> `pending_payment` only inside `startCollection` (`web/src/lib/pos/collection.ts`), in the same breath as a payment request, and there is no standalone writer for that transition; writing `orders.status` from Messages would be a second writer (Principle 0). What confirm DOES on a draft: `holdDraftOrderCapacity` (POS writer, idempotent by `pos-hold:<orderId>`) then `commitCapacity` on the returned allocations so the places stop lapsing with the hold TTL; a `total_cents = 0` draft goes through `completeZeroTotalOrder` and lands `paid`. Consequence to fix in a follow-up: `sweepExpiredOrders` (`web/src/lib/orders/expire-orders.ts`) cancels an idle `draft` after IDLE_DRAFT_TTL_HOURS regardless of a live `conversation_records` link or committed allocations, so a confirmed-but-unpaid draft that nobody touches can be swept. Proposed writer: extract `holdSaleForPayment(admin, {tenantId, orderId, expectedVersion})` (draft -> pending_payment, no hold_expires_at) from `startCollection` and call it from confirm; the sweep already leaves `pending_payment` with no hold alone.
D-MSG-23 (S3) · 2026-09-17 · SEAM: talent legs of a POS-held draft keep their TTL after confirm. `commitOrderTalentHolds` (`web/src/lib/scheduling/commit-order-holds.ts`) keys on `order:<id>:reserve` (the `createPurchase` key) while `holdDraftOrderCapacity` reserves under `pos-hold:<id>`, so the POS's own collection path and this confirm both commit CAPACITY but neither makes the person's `talent_holds` row permanent. Pre-existing for the POS (`completeZeroTotalOrder` calls `commitOrderTalentHolds` and matches zero rows for a POS-held sale); not fixed here because it means either a second operation key on the writer or a `talent_holds` write from Messages. Filed for the POS owner.
D-MSG-24 (S3) · 2026-09-17 · The recheck reads the SAME three tables `loadBusyIntervals` unions (`talent_holds` live, `talent_bookings` not cancelled, `talent_availability_blocks`) through the pure `collectBusyIntervals`, but excludes rows whose `inquiry_id` is this conversation: the offer's own firm hold (the one `enrichBookingFromReservation` deletes at convert) would otherwise refuse every correctly-held offer as busy. Capacity goes through `capacityRemaining` (the `capacity_remaining_public` RPC); a draft line that already has a live `capacity_allocations` row is skipped (`already_held`) rather than counted against itself, and a pool that cannot be read is a conflict (fail closed). A reader that throws is `unavailable` with no conflicts and nothing created. The offer's dated window is the inquiry's reservation stamp (`parseReservationStamp(source_context)`); an offer with no stamp has undated people and those lines are recorded as `skipped: no_date` on the plan the action returns, not silently passed. Tooling: `npm run test:money` is broken on `program/messages-v5` and on `origin/main` (package.json quotes two paths as one argument: `'.../door-client-never-silent.static.test.ts src/app/.../door-delivery-labels.static.test.ts'`, introduced in 1b1a6e962); S3 ran the same file list with the two paths split and got 1144 pass / 1 skipped / 0 fail. Not fixed in this PR (out of lane).
D-MSG-30 (S5) · 2026-09-17 · Catalog price change rule. A draft / offer line keeps `price_snapshot_cents` (the unit price it was added at) and `catalog_price_cents_at_add` (the raw offering / variant price at that moment, before any live phase). A later catalog change never changes an existing line: `addLine` stamps both at the add, `repriceAndValidate` is the only command that rewrites `unit_cents` and it never touches the snapshot, and the RPC `pos_mutate_draft_line` reads the catalog price itself on `add` so a caller cannot lie about it. The UI shows "catalog price now X" from the pure `priceDrift(line, catalogNow)` (`web/src/lib/pos/price-drift.ts`): the reference is the catalog stamp, then the snapshot, then `unit_cents` for pre-S5 rows; a null catalog price is a missing item, not a drift.
D-MSG-31 (S5) · 2026-09-17 · The new cents columns are `bigint`, not `integer` as the brief spelled them: every cents column on `order_lines` is bigint (`unit_cents`, `total_cents`, `tax_cents`, `refunded_cents`) and a narrower sibling would be the one that overflows first. `order_lines.tax_cents` already existed (20261228000142); the migration's `ADD COLUMN IF NOT EXISTS` is a no-op there and only `tax_label` is new on that table.
D-MSG-32 (S5) · 2026-09-17 · Who changed what is a DB fact, not an app guess. `order_line_events` is written by an AFTER trigger on `order_lines` (insert / update / delete, old + new units and cents), so the fallback writer, the RPC, `confirmLines` and any future writer all leave history. The actor comes from two transaction-local settings (`pos.actor_kind`, `pos.actor_id`) that `pos_mutate_draft_line` sets from `p_line.actor_kind` / `actor_id`; an insert with no setting is attributed to the line's own `proposed_by`, an update / delete with no setting to `staff` (the only writer outside the RPC is the service role behind a staff action). A stamp that changes nothing the customer reads (`price_phase_id`, `allocation_ids`, `refunded_cents`) writes no event. A cascade from the order's own delete writes no event either (the parent is gone; the history goes with the order via FK cascade).
D-MSG-33 (S5) · 2026-09-17 · Per-line `discount_cents` joins the order's own discount in the totals: the RPC sums `SUM(order_lines.discount_cents)` into `orders.discount_cents` (clamped to the subtotal), and the fallback writer in `draft.ts` does the same through `orderDiscountCents(order, before, after)`, which takes the previous line sum out of the stored value before adding the new one so a manual / promo discount survives. Nothing in this lane WRITES a per-line discount or tax yet: the RPC accepts them on `add` and `update`, `updateOfferDraft` carries them onto `inquiry_offer_line_items`, and the columns default to 0. Seam for whichever lane owns the discount sheet.
D-MSG-34 (S5) · 2026-09-17 · SEAM, not wired: `confirmLines(admin, { tenantId, orderId, lineIds, actor })` (`web/src/lib/pos/draft.ts`) sets `confirmed_at` / `confirmed_by` on a draft's lines (owner decision 3: picks hold, staff confirm). Exported only; S3's confirm calls it. Refuses a client actor, a line not on the sale, and a non-draft order; a repeat is idempotent (the first confirmation stands). `inquiry_offer_line_items.confirmed_*` has no writer yet either.
D-MSG-35 (S5) · 2026-09-17 · The client sees a label, a quantity and a line total. `proposed_by`, `confirmed_by`, `price_snapshot_cents`, `catalog_price_cents_at_add`, `discount_*`, `tax_*` never reach a client payload: `client-inquiry-details.ts` selects an explicit column list for offer lines, the pay page and the sent-basket snapshot select `label, units, unit_cents`, and `client-readers.static.test.ts` now holds all three closed (plus `talent_cost` / `coordinator_fee`). `proposed_by` / `confirmed_at` DO reach the staff diff sheet (`loadBasketDiff` reads them off `order_lines` and the history off `order_line_events`), which is the point.
D-MSG-36 (S5) · 2026-09-17 · Tooling: `npm run test:money` could not run on `program/messages-v5` at all — commit 1b1a6e962 fused two test paths into ONE quoted argument (`'…/door-client-never-silent.static.test.ts …/door-delivery-labels.static.test.ts'`), and `tsx --test` exits 1 with "Could not find" before running a single test. Fixed in this lane's `package.json` edit (the same line gains `draft-line-author.test.ts` and `price-drift.test.ts`). Any earlier lane reporting `test:money` green on this base did not run it.
D-MSG-37 (S5) · 2026-09-17 · `web/src/lib/supabase/database.types.ts` is hand-extended for `inquiry_offer_line_items` only (Row / Insert / Update gain the nine S5 columns) so the typed `updateOfferDraft` insert compiles; the generated file was already stale (its `order_lines` lacks `kind`, `price_phase_id`, `refunded_cents`-era siblings), the POS writers use the untyped `Admin` seam, and `check:types-fresh` is warn-only. `order_line_events` is not added to the types file; nothing reads it through the typed client. Regenerate after the integrator applies the migration.
D-MSG-38 (S2) · 2026-09-17 · `conversation_records` is now WRITTEN by the POS writers, through one bridge: `messaging_sync_record_state(p_tenant, p_record_kind, p_record_id)` (supabase/migrations/20261231255000_conversation_record_state.sql, SECURITY DEFINER, service_role only) called from `syncConversationRecord` (web/src/lib/messaging/record-sync.ts) AFTER each writer's own success, never before and never blocking its result. Record ids by kind: `order` = orders.id; `appointment` = agency_bookings.id; `reservation` / `tickets` / `class_enrolment` = admissions.id (reserve.ts inserts one admission per party; mint-on-paid one per seat; a session with `event_id` is `tickets`, without is `class_enrolment`). `project` and `offer` have no state table and the RPC refuses them as `invalid` (seam: whoever gives them a status table extends the CASE). Inquiries a record attaches to: orders.inquiry_id ∪ payment_links.inquiry_id (same order) ∪ agency_bookings.source_inquiry_id (booking on that order, or the booking itself); an admission reaches them through order_lines.order_id. The RPC only READS orders / order_lines / payment_links / booking_transactions / preparation_tickets / agency_bookings / booking_fulfillment / admissions / sessions (Principle 0). Writers wired: createPurchase (when a thread was opened), convertToBooking, mintAdmissionsForPaidOrder, createReservation, createPaymentLink / markPaymentLinkPaid / cancelPaymentLink, completeOrderForTransaction / completeZeroTotalOrder (covers the Stripe webhook → markPaid and settleAtDoor), refundOrderLines (+ each admission it stamped), flipBookingLifecycle (refund / dispute), every preparation_tickets transition, upsertBookingFulfillment, cancelBookingSet, markReservationSeated, checkInAppointment, scanAdmission (admitted only).

D-MSG-39 (S2) · 2026-09-17 · RACE NOTE. Two syncs of one record (a webhook retry racing the kitchen, or two writers on one order) must converge: the RPC derives from the source row at call time and upserts `ON CONFLICT (tenant_id, inquiry_id, record_kind, record_id) WHERE unlinked_at IS NULL` (the existing partial unique index `conversation_records_one_live`; the predicate is in the conflict target so inference works — cf. the "partial unique index broke ON CONFLICT at plan time" incident), updating only when a state actually differs (version bumps only on change; proven on the isolated branch: second call left version 1). Rows staff linked by hand to another inquiry follow the record too; a row staff UNLINKED on purpose (unlinked_at set, no live twin) is never re-linked. Cross-tenant inquiries are skipped. `syncConversationRecord` never throws, logs via logServerError, and returns `unavailable` silently when the caller's client has no `rpc` (several POS writers type their client `{ from; rpc? }` and their tests inject `from`-only fakes).

D-MSG-40 (S2) · 2026-09-17 · State vocabularies (CHECK-constrained): payment none|requested|opened|paid|failed|expired|refunded|partially_refunded|unknown; fulfilment none|hold|confirmed|preparing|ready|fulfilled|seated|checked_in|cancelled. `hold` was added to the brief's list because `deriveTasks` (S4) already reads `fulfilmentState === "hold"` for the "a hold is expiring" task; it now fires for `orders.status = pending_payment`. Mapping choices worth knowing: an open payment_link on a draft/quoted order = `requested`; pending_payment = `opened`; paid/fulfilled with order_lines.refunded_cents covering the lines = `refunded` (orders.status never becomes refunded from TS — refundOrderLines only touches lines and admissions); kitchen `queued/acknowledged/ready/handed_off_at` = confirmed/preparing/ready/fulfilled; agency_bookings `in_progress` = checked_in, `tentative/draft` = hold; booking_fulfillment `shipped|ready_for_pickup` = ready, `delivered|picked_up|downloaded|completed` = fulfilled, `returned` = cancelled; admissions `no_show_at` = cancelled (over, money kept), `seated_at` = seated, `admitted_count > 0` = checked_in. record_date: preparation_tickets.promised_at else orders.created_at; agency_bookings.starts_at else event_date; admissions.starts_at else sessions.starts_at else the order's date.

D-MSG-41 (S2) · 2026-09-17 · SEAM, not wired: `deriveAutoResolve(records, now)` (web/src/lib/messaging/lifecycle.ts, pure, 8 tests) is owner decision 1 + 14 as a function: no records → never; any record open or with unknown (null) state → open; money owed (requested|opened|failed|expired on a non-cancelled record) → open; otherwise resolve once now ≥ max(closedAt + grace) with grace 2 days (order/appointment/reservation/class_enrolment) and 7 days (tickets/project); time-anchored kinds count as closed once record_date has passed even if nobody pressed "fulfil". It returns `readyAt` so a cron can schedule the flip. NOT in this lane: the cron itself (reads live chips per open inquiry, calls `messaging_set_conversation_state` → resolved, writes the history line), the 30-day rule for conversations with no record, and the per-business grace override. The sync also does not touch `inquiries.updated_at`, so a state change alone does not reorder the inbox — the cron/UI lane decides whether it should.

D-MSG-42 (S2) · 2026-09-17 · Tooling notes. (a) The first version I picked, 20261231252000, was already recorded on the isolated ledger by another lane (`rls_exclude_internal_notes_non_staff`; 20261231260000 is `line_author_confirmation_snapshot`) — the remote collision check caught it AFTER `journeys:repair` had run the file once under that number; the file is IF NOT EXISTS / CREATE OR REPLACE throughout so that run left identical objects and no ledger residue, and the renumbered 20261231255000 re-applied cleanly and is on the ledger under its own name. Lesson for the next lane: run the collision check BEFORE the repair, not after. (b) At branch time `npm run test:money` exited 1 before running anything (two door test paths quoted as one argument); I ran the identical file list with the quote fixed in-shell (1145 tests, 1144 pass, 1 pre-existing skip, exit 0). The program branch fixed the script meanwhile (13e3ec399) and `npm run test:money` passes as-is after merging it. (c) `src/lib/visits/seat-reservation.test.ts` asserted `calls.length === 1` meaning "check_in ran once"; it now asserts exactly one `check_in` followed by the S2 sync RPC.
D-MSG-40 (S6) · 2026-09-17 · S6 · Two premises in the S6 brief didn't hold and this lane worked around both rather than forcing them. (1) `cancel_booking_set` (supabase/migrations/20261231218000_cancel_booking_set.sql) cancels `agency_bookings` — the coordination-hub's own booking table — and its own header comment says "Money is not moved here": it carries NO cancellation-fee or refund-amount logic at all. No `RecordKind` a Messages v5 thread carries (`order`/`appointment`/`reservation`/`class_enrolment`/`tickets`/`project`/`offer`) resolves to an `agency_bookings` row — every schedulable kind resolves through `admissions` (D-MSG-9's `RECORD_KINDS_ON_ADMISSIONS`), and `order` resolves straight to `orders`/`order_lines`. So `messagingCancelRecord` cancels through the writer `orders`/`order_lines`/`admissions` already have — `refundOrderLines` (lib/orders/refund-execute-lines.ts), which voids the admission and releases its capacity allocation atomically — never through `cancel_booking_set`. (2) There is no `messages.request_payment`-style permission key anywhere in the repo to mirror (grepped `lib/pos`, every migration, and the capability registry `src/lib/access/capabilities.ts` for `messages.*` — zero hits), and `staff_permissions (user_id, permission)` — the one generic table shaped for this — has never been seeded or gated on anywhere (zero INSERTs in any migration; the one reader, `admin-dashboard-data.ts`, is display-only). `web/src/lib/messaging/money-permissions.ts` defines the six keys (`messages.discount`, `messages.refund`, `messages.cancel`, `messages.close_lost`, `messages.notes.read`, plus the existing `messages.request_payment` gap is left unaddressed — no caller exists to gate) fresh, OPEN BY DEFAULT PER TENANT PER KEY: if no active staff member of a tenant holds ANY row for a key, every staff member passes (today's ungated behaviour, unchanged); the moment any one of them is granted the key, that tenant has opted in and only holders (+ `super_admin`) pass. This was chosen over `src/lib/access/capabilities.ts` (the real, CI-validated, plan/role capability registry) deliberately: that registry is billing/plan-tier-gated, cross-cutting, and touching it risks colliding with whichever other wave owns `roles.ts`/`plan-capabilities.ts` — a follow-up integration lane should decide whether these six keys graduate into that registry.

D-MSG-41 (S6) · 2026-09-17 · S6 · `refusal.no_payout_receiver` (en/es/fr `dashboard.pos.messages.refusal.no_payout_receiver`) is added to the refusal catalogue as the brief asked, but this lane could NOT reproduce the gap it describes by reading the current code: both off-platform settlement paths are already receiver-exempt — `settle-at-door.ts` inserts every counter/manual `booking_transactions` row with `provider: 'manual'`, and its own comment says that is "what exempts this from needing a payout receiver" (citing migration 20261230001500, "restores the rail-aware rule phase 8 dropped"); `_pipeline-actions.ts`'s `markInquiryPaidInCash` carries the identical comment ("manual needs no payout receiver") at its `markPaid` call. So `messagingRecordOutsidePayment` maps the refusal DEFENSIVELY rather than on a proven trigger: both `recordVerifiedCollection`'s and `markInquiryPaidInCash`'s error strings are pattern-matched for `/payout/i` and mapped to `no_payout_receiver` if it ever recurs (e.g. a future lane changes the exemption, or a non-manual rail reaches this path some other way), and to the generic `unavailable` refusal otherwise. If the gap is confirmed live by QA, it is elsewhere in the stack than what this lane could find by reading.

D-MSG-42 (S6) · 2026-09-17 · S6 · SEAM: a `refund: {mode: "partial"|"keep"}` cancel releases capacity (`release_capacity`, the same RPC `cancel_booking_set` calls for the same purpose) but does NOT flip the admission's own `status` column to `void`/`refunded` — only `refundOrderLines`'s internal call to `refund_admission` does that atomically, and that RPC always refunds a line's FULL remaining amount (there is no "void the ticket but refund only part of its value" writer anywhere in the codebase; building one is a new writer, which Principle 0 rules out for this lane). So a `mode: "full"` cancel is airtight (money + line state + ticket void + capacity release, all atomic); a `mode: "partial"` or `mode: "keep"` cancel is airtight on money and capacity but leaves `admissions.status = 'valid'` — a door/scanner surface reading that column directly would still admit a ticket whose seat has already been freed for resale. Filed for a follow-up lane to either extend `refund_admission` to accept a partial amount, or add a standalone "void without refunding" RPC.

D-MSG-43 (S6) · 2026-09-17 · S6 · `messagingRecordOutsidePayment`'s `recordId` carries no kind (per the brief's own signature), so the workspace-booking path (`markInquiryPaidInCash`, keyed on `inquiryId` alone — it takes no record id at all) and the POS order-collection path (`recordVerifiedCollection`, keyed on `orderId`) are told apart by asking `orders` for `recordId` first. A caller-supplied `recordId` that names neither an order nor (implicitly, by omission) this inquiry's own coordination booking is silently ignored on the workspace path rather than checked, because `markInquiryPaidInCash` offers nothing to check it against. Also on this action: the three input methods (`cash`/`transfer`/`terminal_offline`) don't line up 1:1 with either rail's own method vocabulary — `recordVerifiedCollection.paidVia` is only `"cash"|"card"` (a counter tender vs. a provider session; `terminal_offline` maps to `"card"`, `cash`/`transfer` both map to `"cash"`) and `markInquiryPaidInCash`'s `PaymentMethod` is `cash|wire|venue_paid|crypto|other` (`transfer`→`wire`, `terminal_offline`→`other`, `cash`→`cash`). Both mappings are documented inline at the call sites.

D-MSG-44 (S6) · 2026-09-17 · S6 · `messagingCancelRecord` / `messagingRefund` / `messagingRecordOutsidePayment` live in a NEW file, `web/src/lib/server-actions/messaging-money-actions.ts`, not in `messaging-engine.ts` (797 lines against the 800-line ratchet before this lane's two permission-check insertions). All of that new file's DB access routes through `web/src/lib/messaging/money.ts` rather than raw `.from(...)` calls, because `ratchet/no-untenanted-from` (eslint.config.mjs) bans raw `.from("table")` string literals outright inside `src/lib/server-actions/**` — `money.ts` is not in that directory and is exempt, matching the same split `messaging-engine.ts` already uses for its reader modules. One table needed hand-filtering rather than `tenantScopedQuery`: `booking_transactions` carries `source_tenant_id`, not `tenant_id` (confirmed in `supabase/migrations/20260901203000_booking_transactions_refund_record_alignment.sql`), so `tenantScopedQuery` (which hardcodes the `tenant_id` column name) cannot be used for it — `loadOwnedTransaction`/`refundArbitraryAmount` in `money.ts` filter on `source_tenant_id` by hand instead.

D-MSG-45 (S6) · 2026-09-17 · S6 · No migration. The `orders_auto_cancel_reminders` trigger (20261231222000_pos_messaging_state.sql) only watches `orders.status`, so an `order`-kind cancel needs no hand-cancel of its reminder — stamping `orders.status` after the money/capacity leg is enough, and `messagingCancelRecord` does exactly that. It does NOT cover the four admissions-kind record kinds (`appointment`/`reservation`/`class_enrolment`/`tickets`) at all — no trigger anywhere watches `admissions.status` for `scheduled_messages` — so those kinds cancel their own reminder by hand (`cancelRecordReminder` in `money.ts`, same shape `messagingCancelReminder` already uses in `messaging-engine.ts`). Verified by reading the trigger's SQL body, not by a live-DB test (no DB harness in this lane's unit-test gates); `money.test.ts`'s `cancelRecordReminder` tests cover the hand-cancel half only.

D-MSG-60 (KIT) · 2026-09-17 · KIT · Scope and class vocabulary. The kit lives in `web/src/components/messages-v5/kit/` with ONE stylesheet, `tokens.css`, scoped under a `.msgv5` root class (every selector starts with `.msgv5`; `kit-tokens.static.test.ts` asserts it) so nothing leaks into the admin, and NO inline styles anywhere (the same test asserts no `style=` in any kit file). The class names are the mockup's own (`btn primary/secondary/danger/disabled/xl`, `seg`, `chip`, `pill needs/wait/done/opp/won/lost/rec/money/due/ch/off`, `card k-offer/k-pay/k-order/k-appt/k-ticket/k-table/k-change/k-id`, `ladder`, `nextbar`, `summary`, `pn-sec`, `sheet`, `tray`, `refuse/okline/alertline`, and the `mx-*` mobile kit) so a screen lane can read the mockup's render function and the kit component side by side. Every colour is a `--msgv5-*` variable declared once in the token block; every rule after it reads a var (asserted). #0f4f3e appears nowhere; primary is `--msgv5-green` #2b8a63 with white text, `--msgv5-green-strong` #1f6e4e is text and selected borders only, destructive is `--msgv5-red` #b3261e, disabled is `--msgv5-gray-bg/--msgv5-gray-ink`. Light only for v1; dark is a second token block away, no rule change.

D-MSG-61 (KIT) · 2026-09-17 · KIT · Copy contract. Components take a `KitCopy` prop (built by `buildKitCopy(t)` in `copy.ts`, or `useKitCopy()` on the client) and never call the translator themselves, so every component is pure and renders under `renderToStaticMarkup` without a provider. All 317 new strings live under `dashboard.messagesV5.*` in EN/ES/FR (real translations, no em dashes, "client" never "customer"); refusals reuse `dashboard.pos.messages.refusal.<code>` through `copy.refusal(code)`. One existing catalogue string was corrected on the way: EN `dashboard.pos.messages.refusal.identity_unconfirmed` said "Customer identity is not confirmed." (ES/FR already said client) and now reads "Client identity is not confirmed." Interpolation is `fill(template, params)` with `{name}` holes; an unknown hole stays visible rather than vanishing.

D-MSG-62 (KIT) · 2026-09-17 · KIT · Variant rule. One component, two grammars: every piece that the mockup draws in both kits takes `variant?: "desktop" | "mobile"` (InboxRowV5 row-c / mx-row, ThreadHeader th-head / mx-th, MessageBubble msg / mx-msg, Card card / mx-card, Composer composer / mx-cmp, Sheet sheet / mx-sheet h60 h92 / mx-full, Tray tray / mx-act rows, OptionRow opt / mx-opt2, PanelSection pn-sec / mx-sec, LineEditorRow line-ed / mx-line-ed, Skeleton, EmptyState). NextStepBar (desktop) and NextStepBlock (mobile) are two exports of one props shape over S4's `DerivedTask[]`; EssentialsStrip and FilterSheet are mobile only. 1194 (tablet) uses the desktop grammar with narrower containers, as the mockup's `.msgs.tab` does.

D-MSG-63 (KIT) · 2026-09-17 · KIT · State rule. Every component that can be in flight takes `busy` (rendered `aria-busy="true"` + a `busy` class; the label stays, clicks stop), every list piece has a loading form (`Skeleton`, `SummaryBlock loading`, `NextStepBar loading`), every refusal is a `RefusalLine` over a `MessagingRefusal` code (never free text), and done states are drawn in the record's own words (Resolved, Paid, Fulfilled, Linked to <name>). The Composer's seven states are one `state` prop (idle / typing / sending / failed / offline / resolved / refused). The PaymentCard covers the eight payment states S2 writes (requested / opened / paid / failed / expired / refunded / partially_refunded / unknown); `unknown` says "Do not request again" and offers Check payment, matching refusal `payment_unknown`.

D-MSG-64 (KIT) · 2026-09-17 · KIT · Engine mapping. `cardCategoryForKind(kind: CardKind)` maps every engine card kind to one of the eight categories and `cardCategoryLabel` to its uppercase word (offer_review/offer_state → Offer; payment_request → Payment; menu_options/item_config/service_card → Choices; basket → Shared draft; order_confirmation → Order; professional_times → Times; appointment_confirmation → Appointment; class_card/tickets_card → Tickets; change_request/change_result → Change request; reminder → Reminder; internal_note → Note; text → Message). `recordStateLabel` reads S2's `paymentState`/`fulfilmentState` through the catalogue and falls back to the raw word so an unmapped state is visible, not blank. `AppointmentCard state="conflict"` takes S3's `ConfirmConflict[]` as-is (line, why). A control with no engine action is drawn greyed with a sentence (Tray `coming` rows are `disabled` + `aria-disabled`), never as a fake button.

D-MSG-65 (KIT) · 2026-09-17 · KIT · Preview and gating. `/c/t/preview?kit=1` (non-production only, same guard as the existing board preview) renders `KitPreview`: every component in every state at 390 (mobile kit), 1194 and 1440 (desktop kit) on one scrollable page, with `data-*` markers per component/state for the screenshot pass. The kit tests (28 files: one `.render.test.tsx` per component, `copy.test.ts`, `kit-tokens.static.test.ts`, the preview render test) run inside the existing `test:messaging` lane (its file list now appends `$(node scripts/list-test-files.cjs src/components/messages-v5)`), so they gate CI without a ci.yml change. Not verified in a browser by this lane (no dev server, rule 12); the integrator screenshots the preview against D01/D05/D07/M02/M03/M05.

D-MSG-66 (KIT) · 2026-09-17 · KIT · Not built faithfully, and why. (a) Icons: the mockup's stroke icon set is reproduced as single-path SVGs with `currentColor`; circles became arc paths, so a few glyphs (users, globe, clock) are close, not identical. (b) The desktop `.sheet` is `position: absolute` inside its host, as the mockup draws it inside a frame; the screen lane decides whether the host is the `.msgs` grid or a portal. (c) `formatWhen` is a minimal relative formatter (2m / 3h / Tue / Aug 1); screens that already have a date formatter should pass their own `meta` strings to `MessageBubble` (it does not format dates on purpose). (d) The Times card's slot chips hold only the label; the pick → `reservation-hold` wiring is L5/L11's. (e) Fonts: `--msgv5-display` reads `--font-geist-sans` and `--msgv5-body` reads `--font-inter-body` from the app's `next/font` variables with the mockup's fallbacks; if a tenant theme swaps `--font-inter-body`, the kit follows the theme, which is intended.

D-MSG-70 (L2) · 2026-09-17 · L2 · Contracts and slots. `web/src/components/messages-v5/screens/contracts.ts` is the one place the wave-A panes agree on props: `InboxProps` (L1), `ContextPanelProps` / `DetailsSheetProps` / `ClientSheetProps` (L3), `HistorySheetProps` / `TasksTrayProps` / `RenameInlineProps` / `MergeCardProps` (L4), plus `ShellActionId`, the union every button in the thread, the next-step bar, the context panel and the + tray dispatches. Panes are pure over their props (take `copy`, never call the translator or a server action). The shell mounts each pane through `screens/slots.tsx`, which today forwards to the L2 placeholders in `screens/placeholders.tsx`; when a lane lands, the integrator points the slot at the lane's component (one import per line) and the shell does not change. Types are additive only: a lane that needs one more field appends it as optional and notes it here.

D-MSG-71 (L2) · 2026-09-17 · L2 · Layout is by CONTAINER width, not viewport. `shell/layout.ts`: `layoutForWidth` gives `three` at ≥1280 (inbox 340 | thread | context 330, D01), `two` at 900–1279 (inbox 300 | thread, context as a 360px right drawer over the thread, D11) and `one` below 900 (inbox ⇄ thread ⇄ Details sheet, M01/M02/M04); the shell measures its own root with a ResizeObserver, so the dev preview draws 390 / 1194 / 1440 side by side and each frame lays itself out. 1194 uses the desktop grammar (D-MSG-62); the mobile kit starts below 900. `shell.css` is the grid only (`.msgv5.msgs`, `.tab`, `.one`), scoped like the kit and reading only kit tokens.

D-MSG-72 (L2) · 2026-09-17 · L2 · Routing: wired or "coming", never dead. `screens/NextStep.tsx` holds `TASK_ACTION` (every S4 task key → a `ShellActionId`, or null which draws a sentence) and `routeShellAction`. Wired in this wave: reply, add_note, resolve, reopen, assign, handover, rename, copy_link, close_lost, history, capture_identity, open_client, send_file, new_conversation. COMING (opens the kit Sheet "Coming in this program", seam named in `data-coming-seam`): `add_items` (L5 items picker), `send_times` (L5 times picker), `create_offer` and `revise_offer` (L6 offer builder), `request_payment` (L7 payment sheet), `confirm` (L8, over S3 `messagingConfirmRecord`), `remind` (L9 reminder sheet, over `messagingScheduleReminder`), `link_record` and `open_record` (L10), `book_again` (L11). The same table serves the context panel (`ContextPanelAction` → `ShellActionId`), the + tray (`TrayItemKey` → `ShellActionId`) and the cards' footer buttons, so one switch flips a target from coming to wired.

D-MSG-73 (L2) · 2026-09-17 · L2 · Composer binding. `screens/composer-machine.ts` is a pure reducer (idle / typing / sending / failed / offline / resolved / refused from four facts); `ComposerWire.tsx` runs it over `messagingReply` / `messagingInternalNote` / `messagingReopen` through an injectable `actions` object so the preview and tests use fixtures. A `conflict` / `version_stale` refusal is not shown as a refusal: the shell reloads the row from the unfiltered inbox and the thread, the text is kept, and an OkLine says so. `already_resolved` locks the composer with Reopen; Reopen then sends the waiting text. "Send via" over MESSAGING_CHANNELS: web_chat and email on; whatsapp "not connected" unless `loadWhatsAppConnection` reports `enabled && state === "connected"`; sms "coming" because `lib/messaging/channels/sms.ts` refuses `channel_unavailable` unconditionally today; counter is "noted here only". The composer is keyed by inquiryId and takes the thread's draft (`components/admin/pos/messages/draft-storage.ts`) as its initial value.

D-MSG-74 (L2) · 2026-09-17 · L2 · Attachments and voice. Attach goes through the existing `uploadInquiryAttachmentSigned`; `actionRegisterInquiryAttachment` writes `visibility: "staff"` for every staff upload, so "Shared with the client" is drawn as a choice whose Choose-a-file opens the coming sheet (seam: a client-visible attachment needs a writer that sets `shared` for staff and posts it to the client thread; not this wave). Voice reuses `components/chat-interactions/VoiceRecorderButton` (importable outside `admin/shell`; it writes `message_kind = "voice"` through `finalizeVoiceNote`), mounted inside a kit Sheet. Known: that recorder carries its own inline styles and a black default accent; it is outside the kit's static test and outside this lane's files (seam: a kit-native recorder, later).

D-MSG-75 (L2) · 2026-09-17 · L2 · Identity capture (D04, M07). `screens/IdentityCaptureWire.tsx` runs the kit card over `messagingMatchCustomers` (debounced 400ms after the last keystroke) and `messagingCaptureIdentity({ level: "confirmed", method: phone | email | name_only, customerId })` for an EXISTING match. "Create a new client" is a seam: no customers writer is reachable from the Messages engine, so that choice opens the coming sheet rather than saving a name-only identity that would not create the record. The card mounts at the end of the stream when the next step, the panel's Capture button or a refusal path asks for it.

D-MSG-76 (L2) · 2026-09-17 · L2 · Client link. `messagingStartConversation` mints a thread token only at creation and nothing kept it (D-145), so "Copy client link" had no reader for an existing thread. Added `messagingThreadLink({ inquiryId })` in `lib/server-actions/messaging-sheets.ts` (messaging-engine.ts is at the 800-line cap): staff guard, tenant-scoped inquiry check, then S7's `refreshThreadToken` (expiry follows the latest linked record, D-MSG-6). Pure read, no row written. The shell copies the URL to the clipboard and also shows it in a sheet with a Copy button for browsers that refuse the clipboard.

D-MSG-77 (L2) · 2026-09-17 · L2 · Read mark and the unread divider. Opening a thread calls the existing `markThreadRead(tenantSlug, inquiryId, "private")` (app/(workspace)/[tenantSlug]/admin/messages/actions.ts → RPC `inquiry_mark_thread_read`) after the thread loads, then zeroes the row's `unreadCount` and the badge total locally; the divider sits before the last `unreadCount` client messages (`thread-stream.ts`) and is the scroll target on open, else the bottom. Grouping is same sender within 3 minutes; a card, a day, a system line or the divider closes a group.

D-MSG-78 (L2) · 2026-09-17 · L2 · Task inputs the shell cannot supply yet. `deriveTasks` is fed conversation/opportunity state, the chips, identity level and a payment issue read from chip `paymentState` (failed / expired). `balanceDueAt`, `reminderDueAt`, `holdExpiresAt` and `talentConfirmationsPending` are null / 0 because no engine reader returns them to the workspace row or essentials. Seam: an essentials extension (or S2's record state) carrying due balance, next reminder, hold expiry and pending talent confirmations; until then those four tasks never surface from the shell.

D-MSG-79 (L2) · 2026-09-17 · L2 · Gating, preview, what was not verified. `WorkspaceMessagesPage` renders `<MessagesV5Shell/>` only when `NEXT_PUBLIC_MESSAGES_V5 === "1"` (owner decision 10's per-tenant `messages_v5` flag is Phase 3); the old shells are untouched otherwise and the shell rides the same single async entry (`shell-lazy-surfaces.tsx`). Dev preview: `/c/t/preview?screen=shell` (non-production only) renders the shell on `lib/messaging/fixture.ts` through an in-memory fixture engine at 1440 / 1194 / 390. Not verified in a browser by this lane (rule 12, no dev server): the integrator screenshots the preview against D01 / D11 / M02 and walks Resolve, Reopen, assign, rename, close-as-lost, copy link and a reply on the fixture tenant. Pre-existing: `InboxPage.tsx` carries 21 unused-import lint WARNINGS on the base branch (0 errors), unchanged by the flag block.
D-MSG-80 (L1) · 2026-09-17 · L1 · "Needs action" rule, pure. `isNeedsAction(row, now)` (`web/src/lib/messages-v5/inbox-view.ts`) is true when: `conversationState === "needs_reply"`; OR `ownerUserId === null` (unassigned); OR a record chip's `paymentState` is `failed`/`unknown`/`expired` (S2's own payment vocabulary, D-MSG-40); OR a chip is `fulfilmentState === "hold"` with a `recordDate` (S2, additive) already in the past — the hold nobody confirmed in time; OR `InboxRow.nextAction === "follow_up"`, the one field the row itself carries for a due follow-up (S4's richer `deriveTasks` computes the same shape per-thread from state the row doesn't include, e.g. `reminderDueAt`/`holdExpiresAt` — the inbox row is a summary, not the full state). Segments never double-count a row: `rowsForSegment` puts "Waiting" as `awaiting_customer AND NOT isNeedsAction` (so an unassigned-but-waiting row shows under Needs action, matching the mockup's own `Needs action (unanswered, unassigned, payment issue, expired hold, follow-up due)` wording, index.html:1415); "All" is every row. `applyInboxFilters` ANDs every active chip (narrow, never widen, per `InboxSegments.tsx`'s own docstring); `searchInboxRows` is a lowercase substring match over name + subject + preview. `groupInboxRows`: Needs action groups by the single highest-priority reason (payment > hold > unassigned > reply > follow-up, same order `deriveTasks` prioritises); Waiting groups by `opportunityState` (label from `copy.state.opportunity`, reusing S4/KIT's own catalogue rather than new strings); All groups by calendar day (Today / Yesterday / weekday / date), matching the mockup's `grp-h` markup (index.html:731-739, 1155-1157) which this lane also read directly for exact class names (`.pane.inbox`, `.ib-head`, `.ib-list`, `.grp-h`, `.mx-ih`, `.mx-gh`, `.tools`, `.filt`) since the kit doesn't wrap them in a dedicated component and they were already in `tokens.css`. 32 tests in `inbox-view.test.ts` + `Inbox.render.test.tsx`.

D-MSG-81 (L1) · 2026-09-17 · L1 · Extended `InboxProps` (contracts.ts, L2) locally rather than editing L2's file: `Inbox.tsx` exports `InboxScreenProps = InboxProps & { now?: Date }`, additive and optional, used only so grouping's day boundary is deterministic in tests (defaults to `new Date()`). Locale is read with `useDashboardLocale()` (`src/i18n/use-dashboard-locale.ts`), not a prop, matching how the rest of the dashboard resolves it — no contract change needed there either. Flagging for L2/the integrator: `InboxProps` carries one `onSearch(query: string) => void` for both the client-side query the pane narrows with immediately AND the brief's "messagingSearch on Enter for server search" — this lane calls `onSearch` on every keystroke (so the shell's `search` stays in sync) and again, redundantly, on Enter; if the shell wants a distinct trigger for the `messagingSearch` server action (debounced typing vs. an explicit submit), `InboxProps` will need a second optional callback (e.g. `onSearchSubmit`) — not added here since it wasn't there to extend and the visible behavior (server search fires on Enter) is unaffected either way.

D-MSG-82 (L1) · 2026-09-17 · L1 · `test:messaging`'s file list only expanded `src/lib/messaging/*.test.ts` (non-recursive) plus everything under `src/components/messages-v5`; it never covered `src/lib/messages-v5/*.test.ts`, so S7's `use-inbox-live.test.ts` was not gated by any npm script since it landed. Fixed in `web/package.json`: the glob now also runs `src/lib/messages-v5/*.test.ts`, which picks up both `use-inbox-live.test.ts` and this lane's new `inbox-view.test.ts` (309/309 passing after the change, confirmed both files' subtests appear in the run).

D-MSG-83 (L1) · 2026-09-17 · L1 · Added catalogue keys EN/ES/FR under `dashboard.messagesV5.inbox.empty.{waitTitle,waitBody,allTitle,allBody}` (Waiting/All segment empty states — the brief's one worked example, "Nothing needs you" / "Show Waiting on client", only covers Needs action) and `dashboard.messagesV5.inbox.groups.{today,yesterday,paymentIssue,holdExpiring,followUpDue}` (grouping headers not otherwise in the catalogue; `today`/`yesterday` are new rather than reusing `stream.today`, which is L2's thread-stream namespace). Everything else the pane needed (segments, filters, filterSheet, the three empty states the brief named, `threads` count template) was already in the KIT lane's `copy.ts`/catalogue. `test:phase1-i18n` (message-key-usage, duplicate-keys) and `verify:ui-messages` both run clean after the addition (not part of this lane's required gates, checked anyway since new catalogue keys are easy to get wrong).
D-MSG-90 (L3) · 2026-09-17 · L3 · Items label per business is a pure mapping, not a DB read in the component. `itemsLabelKeyForPreset` (`web/src/lib/messages-v5/context-view.ts`) resolves `agencies.settings.industry_preset` through the existing `resolveIndustryPreset` (`lib/words/presets.ts`, already pure, already the documented finer signal over `workspace_type`) to one of four copy keys: `representsPeople` (only the "agency" preset) -> talent & services; a preset that spreads `RESTAURANT_WORDS` (`restaurant`/`bar_club`/`beach_club` — a real food menu: "Dish"/"Kitchen") -> menu; `features.appointments` and no food menu (spa/salon/clinic/practice/studio_gym) -> services; everything else -> order items. `features.menu` itself could NOT be used alone to mean "Menu": it is true for 16 of the 20 presets (workshop_print, portfolio, dropoff_service, tours_activities...) because it really means "has a priced-items table," not "runs a kitchen" — using it directly would have mislabelled a workshop or a print shop's Items section "Menu". `ContextPanelProps.itemsLabel` (L2 contract) is a plain string the shell already resolves; `itemsLabelForPreset(raw, copy)` is the helper a shell wires it through, screens never call `resolveIndustryPreset` themselves.

D-MSG-91 (L3) · 2026-09-17 · L3 · SEAM: `ContextPanelProps` / `ClientSheetProps` (L2, `screens/contracts.ts`) carry state/chips/tasks/essentials but no offer/order LINE data, no money breakdown, no files/notes/follow-up counts, and `ClientSheetProps.onAction` is a two-key router (`capture_identity` / `edit_contact`), not an inline editor. Nothing upstream of the shell reads `inquiry_offer_line_items` / `order_lines` / `payment_links` for the panel yet, and no shell wiring exists for a two-matches identity editor. This lane's copy of `contracts.ts` (own branch only, L2's file never edited) appends two ADDITIVE, OPTIONAL extension blocks at the bottom: `ContextPanelExtra` (`items`, `itemsLoading`, `money`, `filesCount`, `notesCount`, `nextReminderLabel`, `clientHistoryLabel`) intersected as `ContextPanelPropsV2`, and `ClientSheetPropsExtra` (`matchState`, `onFieldChange`, `onSelectMatch`, `onSave`) intersected as `ClientSheetPropsV2`. Every field is optional: a caller that passes only the L2 shape gets the exact L2 behaviour (`ContextPanel`/`ContextDrawer`/`DetailsSheet` draw each extra section's empty/collapsed state, never a fake zero, when the field is absent; `ClientSheet` without `matchState` renders the plain L2 summary + "Edit contact" button unchanged). The write path for the fuller `ClientSheet` editor, when a later shell wave wires `matchState`, is the EXISTING `messagingMatchCustomers` / `messagingCaptureIdentity` server actions (`lib/server-actions/messaging-engine.ts`) plus, for a plain contact-field correction, `updateInquiryDetails` (`lib/inquiry/inquiry-engine-details.ts`, the version-locked `contact_name`/`contact_email`/`contact_phone` patch engine already used by "Edit job") — no new writer. The "phone belongs to another client" refusal reuses the catalogue's `already_linked` code (closest existing sentence; there is no dedicated phone-collision refusal in `MessagingRefusal`). L2 owns `contracts.ts`; the integrator reconciles these two copies when merging L2 + L3 into `program/messages-v5` (a straight append, no conflicting edits to L2's own types).

D-MSG-92 (L3) · 2026-09-17 · L3 · The Client section's "N past bookings · $X" history line (README) is always omitted today, on purpose: `Essentials` (`lib/messaging/essentials.ts`) carries no customer rollup (no visit count, no lifetime spend) and this lane added no reader for one (a customers-rollup reader is out of lane scope, and Principle 0 says a lane that needs a new read pattern documents the seam rather than inventing one under time pressure). `clientHistoryLabel` (`context-view.ts`) takes the rollup as an explicit optional argument and returns `null` — not `"0 past bookings"` — whenever it is missing or zero, so `ContextPanel`/`DetailsSheet` correctly show nothing rather than a fake number. A later lane that adds a rollup to `Essentials` only has to pass it through; the panel already handles both states.

D-MSG-93 (L3) · 2026-09-17 · L3 · `ClientSheet`'s Save (D15) logs a new `HistoryKind`, `client_edit`, written by `logClientEdit` (`lib/messaging/action-log.ts`, `action_type = "messaging_client_edit"`, same best-effort-after-the-real-write pattern as `logAssignment`/`logCloseLost`) and rendered by `history.ts` as "{actor} updated the client's {fields}". The log metadata carries only the CHANGED FIELD NAMES (`["phone"]`, not the phone number itself), so a wider staff audience reading `inquiry_action_log` never sees a client's contact value through the history timeline. `logClientEdit` is exported but not called from any write path in this lane (the write itself — `updateInquiryDetails` / `messaging_set_identity` — is a shell-level concern per D-MSG-91's seam); a follow-up wave calls it right after that write succeeds.

D-MSG-94 (L3) · 2026-09-17 · L3 · Tooling: `test:messaging`'s file list (`package.json`) only expanded `src/lib/messaging/*.test.ts` (one level, no descent) and `src/components/messages-v5/**` (recursive) — a new `src/lib/messages-v5/*.test.ts` directory (this lane's `context-view.test.ts`, and the pre-existing `use-inbox-live.test.ts`) was not enumerated by either glob and would have run in NO lane. Added `$(node scripts/list-test-files.cjs src/lib/messages-v5)` to the command, following the same `list-test-files.cjs` convention the rest of the script already uses (recursive, quarantine-aware, no shell-glob depth trap — see that script's own header for the prior incident this pattern fixes). Confirmed both `context-view.test.ts` and `use-inbox-live.test.ts` are now picked up; `npm run test:messaging` exit 0, 310/310 after the addition.
D-MSG-100 (L4) · 2026-09-17 · L4 · Contract extensions to `screens/contracts.ts` (L2-owned; additive per its own header comment). Cherry-picked L2's commit `b989aad47` as the base, then added two OPTIONAL fields, both documented inline at the field: (1) `RenameInlineProps.generated?: boolean` — the component has no `contactName` to compute `isGeneratedName` itself (the shell already reads `essentials.customer.name` for `ThreadHeader`), so the shell computes it once and passes the boolean down. (2) `MergeCardProps.onKeepSeparate?: () => void` — the brief's three-option card ("Merge into <name>" / "Keep separate, link the client" / "Not the same person") needs a THIRD engine action (`messagingCaptureIdentity`, level "linked") that the original two-callback shape (`onMerge`, `onDismiss`) has no seam for; when the shell does not pass it, `MergeCard` simply does not render the middle row rather than wiring it to a no-op. Both fields default to falsy/absent so any code already built against the un-extended contract still compiles.

D-MSG-101 (L4) · 2026-09-17 · L4 · `isGeneratedName(name, contactName)` added to `lib/messaging/inquiry-name.ts` (pure, exported alongside the now-also-exported `fallbackName`). `currentInquiryName` (D-MSG-3) returns `fallbackName(contactName)` exactly when no `messaging_rename` row exists yet — there is no separate "generator" to call (the brief's "named from the first message" is describing `contact_name`, which the intent engine already derives from the inbound message; S4 never built a second generator). A name is therefore "generated" iff it equals that fallback for the SAME contact name. Tested in `inquiry-name.test.ts` (pure, no fake-admin fixture needed).

D-MSG-102 (L4) · 2026-09-17 · L4 · `duplicateHint(row, matches)` added as pure in NEW `web/src/lib/messages-v5/duplicates.ts` (D14, owner decision 1: "Same person? only when identity is uncertain"). "Uncertain" = `identityLevel` is `none` or `linked` (matched automatically but never confirmed by a person; `confirmed`/`granted` are not uncertain). SEAM: `messagingMatchCustomers` (server-actions/messaging-engine.ts) returns `CustomerMatch` rows sourced from `customers` alone with no "does this customer have another OPEN conversation" flag — `duplicateHint`'s `matches` parameter takes a caller-supplied `hasOpenConversation` per match rather than assuming one, so a future lane wiring the inbox row pill has to compute that flag itself (one more query per match today; not built here, not silently assumed true).

D-MSG-103 (L4) · 2026-09-17 · L4 · Tooling gap found and fixed: `test:messaging` (package.json) only ever globbed `src/lib/messaging/*.test.ts` and `src/components/messages-v5/**` — `src/lib/messages-v5/*.test.ts` (this lane's `duplicates.test.ts`, and S7's pre-existing `use-inbox-live.test.ts`) was never in any lane's test command. Added a second `list-test-files.cjs src/lib/messages-v5` expansion to the same script. `use-inbox-live.test.ts` was already green once included; this is a coverage fix, not a behavior change.

D-MSG-104 (L4) · 2026-09-17 · L4 · Kit reuse over new classes: `HistorySheet` uses the kit's own `DaySeparator` (`kit/MessageBubble.tsx`) for day headers rather than a new element, and the `.pn-sec` / `Btn` / `Pill` classes already in `tokens.css` for its Export row. Two genuinely new rows had no existing kit shape (a plain icon + one sentence + a time for a history entry; a labelless field + two right-aligned buttons for the inline rename) — added their CSS (`hist-row`/`mx-hist-row`, `rename`/`mx-rename`/`rename-acts`) as a small append to `kit/tokens.css`, every declaration a `var(--msgv5-*)` read, no new colour literal (kept `kit-tokens.static.test.ts` green). `kit/tokens.css` is a KIT-lane file, not an L2 file; the lane brief says "Do NOT edit any L2 file" — tokens.css is not one, and this is the same additive spirit the contract file's own header invites.

D-MSG-105 (L4) · 2026-09-17 · L4 · `RenameInline.tsx` is split into `RenameInlineView` (pure, fully prop-driven: `value`, `phase`, `refusalCode`, `generated`) and `RenameInline` (the thin stateful wrapper matching the L2 contract, owns the async `onSave` round trip and the local idle/saving/refused phase). Every render test targets `RenameInlineView` directly for the four states the brief asks for (idle, saving, refused, the generated-name hint); `RenameInline` itself gets one smoke test asserting it starts idle from the contract props. Chosen because this kit's own render-test convention is `renderToStaticMarkup` only (no `@testing-library/react` wired into this lane's test harness anywhere in the repo) — a fully self-contained stateful component's "saving"/"refused" phases are not reachable by re-rendering static markup after a simulated click, so the state was pulled out to where it IS a prop.

D-MSG-110 (MERGE-A) · 2026-09-17 · wave-A merge hand · One contracts file again. `screens/contracts.ts` keeps L2's final shape; every extension a lane declared on its own copy is now an OPTIONAL field on the shared type and the lanes' local aliases are gone (`InboxScreenProps`, `ContextPanelPropsV2`, `DetailsSheetPropsV2`, `ClientSheetPropsV2`, `ContextPanelExtra`). Added: `InboxProps.now?` and `InboxProps.onSearchSubmit?` (L1, D-MSG-81 — Enter in either search field now calls it; the shell does not pass it yet, so Enter is a no-op beyond the per-keystroke `onSearch`); `ContextItemLine`, `ContextMoney`, `ContextPanelProps.items/itemsLoading/money/filesCount/notesCount/nextReminderLabel/clientHistoryLabel`, `ClientSheetMatchState`, `ClientSheetProps.matchState/onFieldChange/onSelectMatch/onSave` (L3, D-MSG-91); `RenameInlineProps.generated?`, `MergeCardProps.onKeepSeparate?` (L4, D-MSG-100). `DetailsSheet` imports `ContextPanel`'s `moneyOf` instead of carrying a copy. `screens/slots.tsx` now mounts the lane components (Inbox, ContextPanel, ContextDrawer, DetailsSheet, ClientSheet, HistorySheet, TasksTray, RenameInline, MergeCard); `placeholders.tsx` is deleted and the `ComingSheet` (the shell's own) moved into `slots.tsx`. Layout fit: the shell grid addresses `.msgs > .pane.inbox` / `.pane.panel`, so L1's Inbox roots are now those pane elements (desktop lost its `.msgv5` wrapper, the shell root already carries it; mobile is `.pane.inbox.mx`), and the column slot wraps L3's block panel in `<section class="pane panel">`. The 900–1279 drawer is L3's `ContextDrawer` (kit Sheet) instead of the shell's own scrim + `.drawer` markup; those rules and the placeholder-only `.history-row` / `.task-row` / `.rename-inline` rules left `shell.css`. `isGeneratedName` / `fallbackName` moved to `lib/messaging/inquiry-name-pure.ts` (no `server-only`) and are re-exported by `inquiry-name.ts`, because the shell is a client component and could not import the server-only module. `test:messaging` already globbed `src/lib/messages-v5` and `src/components/messages-v5` exactly once each (L1/L3/L4 each fixed it; the merge kept one line).

D-MSG-111 (MERGE-A) · 2026-09-17 · wave-A merge hand · What the shell passes to each pane, and what stays undefined. Inbox: rows, segment, chips, search, selection, loading/error, unread total, counts, `onNew`, `currentUserId` (unchanged). ContextPanel / ContextDrawer / DetailsSheet: essentials, state, chips, tasks, itemsLabel, loading, plus `notesCount = essentials.notes.length` (the one L3 extra an existing reader answers). Left undefined on purpose: `items` — no server action returns offer or order LINES to the workspace (`messagingLoadOffers` returns offer totals only; `messagingLoadBasketDiff` returns only the lines that moved under an open payment link; `lib/pos/draft.ts` readers are server-side with no action wrapper); `money` — `ContextMoney` needs paid cents and no reader returns them per inquiry (an offer total with a made-up "$0 paid" would be a false statement, D-MSG-92's rule), so `messagingLoadOffers` is NOT called from the shell either, since nothing would draw its result; `filesCount` — attachments are not counted by any reader the shell holds; `nextReminderLabel` — D-MSG-78 (no reminder reader); `clientHistoryLabel` — D-MSG-92 (no customers rollup). ClientSheet: essentials only; `matchState` (the D15 inline editor) is not wired — the only existing contact writer reachable from the client is `updateInquiryJobFields` (`admin/_pipeline-actions.ts`), which returns English error strings instead of refusal codes and does not call `logClientEdit` (D-MSG-93), so "Edit contact" keeps opening the coming sheet until a messaging-engine writer exists. HistorySheet: `loadConversationHistory` when the sheet opens (unchanged). TasksTray: the derived tasks. RenameInline: `generated = isGeneratedName(essentials.name, essentials.customer.name)`. Items label: new optional shell prop `industryPreset` routes through L3's `itemsLabelForPreset` (D-MSG-90); the mount (`InboxPage.tsx`) does not pass it because the admin bridge identity carries `workspaceType` only, so the label still falls back to the talent/generic rule until the bridge carries the preset.

D-MSG-112 (MERGE-A) · 2026-09-17 · wave-A merge hand · "Same person?" (D14) wired over existing readers and writers only. When a thread's essentials load with `identityLevel` none or linked AND a phone or email, the shell calls `messagingMatchCustomers` (through `engine.identity.match`); `messagingMatchCustomers` names customers, not conversations, so the `hasOpenConversation` flag `duplicateHint` asks the caller for (D-MSG-102) is answered from the rows the shell has loaded: another unresolved row whose phone digits or email equal the match's. The first such pair draws `MergeCard` at the end of the stream (new `Thread.mergeSlot`). Merge = `messagingMerge({ duplicateInquiryId: this thread, intoInquiryId: the other row, expectedVersion: THIS thread's version })` — the engine locks the duplicate (`lib/messaging/merge.ts`), then the inbox reloads and the other thread opens. Keep separate = `messagingCaptureIdentity(level "linked", method phone|email by which field agreed, customerId)`. Not the same person = session-only dismissal (no writer records "not a duplicate"; seam). Known limits, stated rather than papered over: a duplicate outside the loaded segment draws no card; after Keep separate the level is still "linked" (uncertain per D-MSG-102), so the thread is also dismissed for the session to avoid a second card. `ShellEngine.merge` added (live + fixture).

D-MSG-113 (MERGE-A) · 2026-09-17 · wave-A merge hand · Placeholder-only copy removed. The 26 `dashboard.messagesV5.shell.*` strings only `placeholders.tsx` read (`tasksTitle`, `historyTitle`, `historyEmpty`, `renameTitle`, `renameSave`, `renamePlaceholder`, `inboxThreads`, `inboxEmpty`, `inboxEmptyBody`, `inboxFailed`, `inboxFailedBody`, `backToInbox`, and the 14 `panel.*` keys) left `screens/copy.ts` and EN/ES/FR; the lanes carry their own catalogue keys under `dashboard.messagesV5.{inbox,panel,client,history,tasks,rename,merge}`. `copy.test.ts`'s size guard moved from `> 90` to `>= 80` (81 shell strings now). Gates: `test:phase1-i18n` 0 (20/20), `verify:ui-messages` 0.

D-MSG-120 (L5) · 2026-09-17 · L5 · One picker, one list, existing writers only (Principle 0). `screens/sheets/ItemsPicker.tsx` registers `add_items` and `send_times` through the registry seam (one import line in `screens/sheets/index.ts`; the shell and `NextStep.tsx` untouched). Every write the picker makes is a writer the POS already has: `messagingEnsureSharedDraft` (the conversation's shared draft), `posAddLine` (`lib/pos/draft.ts addLine`, staff-proposed, session and variant carried for classes and tickets), `posAddCustomLine` (the "Custom line" row), `rosterAddTalent` (`lib/inquiry/inquiry-engine-roster.ts addTalentToRoster`, so a picked person becomes an `inquiry_participants` row and their yes/no flows), and `messagingSendOptions` (one card per kind). The plan is pure (`lib/messages-v5/items-picker.ts sendModeToEngineCall` -> a list of `EngineCall`s) and `runSendPlan` (`screens/sheets/items-actions.ts`) executes it in order, stopping at the first refusal and answering its code; the render test runs the same plan on a fake engine. `rosterAddTalent` bumps the inquiry version, so before each talent add the plan re-reads the current version through `messagingLoadEssentials` (`Essentials.version`) rather than guessing `version + i`; `posAddLine` answers no new version, so only the first line on a freshly ensured draft carries `expectedVersion` and later lines go unlocked, the counter's own pattern. Custom lines carry a fresh `crypto.randomUUID()` idempotency key per attempt.

D-MSG-121 (L5) · 2026-09-17 · L5 · The catalog is a READER over the POS's own sources (`lib/messages-v5/items-catalog.ts loadItemsCatalog`, server-only, behind `messagingLoadItemsCatalog` in `lib/server-actions/messaging-items.ts` with the `messagingStaff` guard). Read and wired: talent = `agency_talent_roster` + `talent_profiles` (the New inquiry picker's join, `lib/inquiry/new-inquiry-intake-data.ts`; cap 40); packages / services / menu = `talent_offerings` kind package | service | product, status published (the counter's filter, `admin/pos/counter-catalog.ts`; a counted stock at zero is "sold out"); classes = `sessions` with an offering and no event, seats through `readSessionSeats` (`lib/scheduling/waitlist-desk.ts`, the `capacity_remaining_public` reader Waitlist and Classes share; 0 = "full"); tickets = `sessions` with an event, tiers = the event offering's visible `talent_offering_variants` with seats per tier pool through the same reader (the sold-out tier is disabled in the chooser); tables = the reservation block's own times for the thread's date through `loadReserveAvailability` (party of 2, listed only when the preset has reservations). Availability for people is the thread's date (`inquiries.event_date`, zone `event_timezone` else the venue's) against `loadBusyIntervals` (`lib/scheduling/load-busy.ts`: holds, bookings, blocks), one read per person in parallel; a calendar that cannot be read is BUSY (fail closed, the slots route's own rule); no date on the thread = availability "unknown" and the sheet says so (`items.noDate`) rather than drawing everyone free. Not read (seams, stated in the row rather than faked): a talent's rate (talent rows carry no price; the total is marked "from" when a priceless row is selected), an offering's variants for non-ticket items (the counter's Options chooser; the line goes on at the base price), the preset for chip order comes from `agencies.settings.industry_preset` inside the same read because the shell still does not pass `industryPreset` (D-MSG-111).

D-MSG-122 (L5) · 2026-09-17 · L5 · Tables are listed, not sendable. `CARD_KINDS` has no table card and `messagingSendOptions` takes only `menu_options | service_card | professional_times | class_card | tickets_card`; `addLine` needs an offering id a table time does not have; the only table writer is the reservation block's `createReservation`, which BOOKS (owner decision 3: choices never book). So a selected table time makes the plan a `seam` call (`no_card_kind` under choices, `no_writer` under offer/draft), the footer draws `items.tableSeam` and the send stays disabled; the plan never silently drops the row. A custom line under "Send choices" is the same shape (`custom_not_a_choice`): a custom line is a draft/offer line, not a choice. Lands with the table card (D21 "Send choices with kind table", a later lane).

D-MSG-123 (L5) · 2026-09-17 · L5 · Card kinds per category and the hold rule. Talent, packages and services share one `service_card` (D05: one list, `offeringIds` + `talentProfileIds` + `labels` + `pricesCents`); menu = `menu_options` (`offeringIds`, `labels`, `pricesCents`, USD); one `class_card` per session (`sessionId`, `offeringId`, `startsAt`, `timezone`); one `tickets_card` per event session with every visible tier (`tiers[].id/label/priceCents`). "Send choices" writes nothing to the draft: the client's pick is what writes the "client chose" line (S5 guest link, owner decision 3). A card whose pick HOLDS capacity (`professional_times`, `class_card`, `tickets_card`) needs at least a linked identity (D21 "a table needs at least a linked identity"; same rule for any hold): `holdRefusal` refuses locally with `identity_unconfirmed` (the engine's own code for the hold path) before any call, so the staff member sees the refusal sentence and the thread's Capture identity step rather than a dead send.

D-MSG-124 (L5) · 2026-09-17 · L5 · Times (D09) is the public slots projection behind a staff guard. `messagingLoadPersonSlots` -> `loadPersonSlots` (`items-catalog.ts`) runs the same three steps as `GET /api/public/booking/slots` (`talent_booking_hours` -> `parseBookingHours`, `loadBusyIntervals`, `computePublicSlots`) for a person on this workspace's roster (refused `not_found` otherwise), from the thread's date, 7 days, `durationMinutes` from the chosen service or the person's slot length. Two things the public route gates that this one does not, on purpose: the offering's `visibility = public` (staff may offer a time for a service the site hides) and `resolveTalentBookingMode` (a staff-sent card is not the storefront's instant-book surface; the client's pick still goes through the hold engine on the link). The sheet enforces 3 to 6 picks (`timesSelectionState`; fewer when fewer are free) and sends ONE `professional_times` card (`timesPayload`: `slots[].startsAt / professionalName`, the shape `ThreadCards` already reads, plus `talentProfileId`, `offeringId`, `timezone` for L9's hold). An empty answer carries the route's reason (`no_booking_hours`, `closed_in_window`, `fully_booked`, `hours_unreadable`) as a sentence, never a bare empty list. The pick and the hold happen on the client link (L9); here only the send. Not built: the "expired_hold" task's re-send with the previous person preselected (the task routes to `send_times`, the sheet opens on the person list).

D-MSG-125 (L5) · 2026-09-17 · L5 · View/wrapper split, same as L4 (D-MSG-105): `ItemsPickerView` and `TimesSheetView` are pure over their props (phase `loading | ready | busy | refused | done | failed`, plus `slots` on times), so every state the brief asks for renders in `renderToStaticMarkup`; `ItemsPickerSheet` / `TimesSheet` are the stateful wrappers the registry mounts, each taking an optional `actions: ItemsActions` so a test or the dev preview can swap the engine (`engineItemsActions` is the live one). "Done" = the ok sentence in the sheet, `ctx.notify({ kind: "ok" })`, `ctx.reloadThread()`, `onClose()`, then `ctx.dispatch("create_offer")` for the offer mode (the shell opens L6's sheet when it registers, the coming sheet until then). New CSS is a small append to `kit/tokens.css` (list spacing, mode list, times grid), every declaration a `var(--msgv5-*)` read, no new colour literal (kit-tokens static test green). Copy lives under `dashboard.messagesV5.items.*` (66 keys, EN/ES/FR) through `kit/copy.ts`; the picker's date and time labels use `Intl` with the venue's zone.

D-MSG-126 (L5) · 2026-09-17 · L5 · A type error in the registry seam itself, fixed in passing. `screens/sheet-registry.tsx` and `screens/sheet-host.tsx` (commit `57886bba4`) imported `DerivedTask` from `@/lib/messaging/tasks`, which only exports `deriveTasks`; the type lives in `@/lib/messaging/types` (the shell imports it from there). Nothing in the messaging test lane exercises types, so it was invisible until the scoped tsc for this lane pulled the two files in. Two import lines changed, no behaviour. A whole-repo `npm run typecheck` on `program/messages-v5` would have reddened on this; the integrator should expect it gone after this PR.
D-MSG-130 (L6) · 2026-09-17 · L6 · `OfferEditorSheet` (boards D06/D18/M05 phone 1) registers for BOTH `create_offer` and `revise_offer` (`sheet-registry.tsx`) with the SAME component. `ActionSheetHost` does not forward which action id opened the sheet, only the shell context (`sheet-host.tsx`) — so the sheet tells create from revise by reading the thread's own state: no `RecordChip` of kind `offer` means start v1 (`messagingCreateOffer`, idempotent — reuses an orphan draft per the engine's own guard); an existing chip's offer is loaded (`messagingLoadOfferForEditor`) and its `status` decides what "revise" means (D-MSG-135).

D-MSG-131 (L6) · 2026-09-17 · L6 · The "Internal · never sent" block (talent net / agency fee / platform fee) is a DISPLAY ESTIMATE from the draft alone (`lib/messages-v5/offer-draft.ts`: `draftTalentNetCents` sums live lines' `talent_cost`, `coordinatorFeeCents` is the offer's own `coordinator_fee`, `draftPlatformFeeCents` is whatever total is left over, floored at 0), NOT a call to `lib/billing/commission.ts` `resolveBookingCommissions` — that resolver needs a booking record and a resolved commission plan this draft does not have until conversion. Never reaches the client-facing `OfferCard` preview rendered beside it in the same sheet.

D-MSG-132 (L6) · 2026-09-17 · L6 · `lib/messages-v5/offer-compare.ts` (`compareOfferVersions`, tested) is a pure line-matching diff for the Compare view (D18): matched by `sourceServiceId`, else `talentProfileId`, else `ownerTenantId`, else normalized label (custom lines have none of the first three). SEAM: the stateful sheet only has the CURRENT draft's lines loaded client-side; comparing against an older version needs that version's lines fetched too, which is not wired in this pass (`OfferEditorSheet.openVersionCompare` opens the dialog comparing the current draft against itself — every line reads "unchanged" — rather than fetching and than pretending a real diff). A follow-up lane should add a `messagingLoadOfferForEditor`-style call keyed by a past offer id (the row survives in `inquiry_offers` after being superseded) and feed both line sets to `compareOfferVersions`.

D-MSG-133 (L6) · 2026-09-17 · L6 · New staff-only reader `loadOfferForEditor` (`lib/messaging/sheets.ts`) returns the FULL offer (header + every line's staff-only fields: `talent_cost`, `proposed_by`, `confirmed_at`, price snapshot, discount, tax) shaped directly as `OfferDraftState`/`OfferDraftLine` (`lib/messages-v5/offer-draft.ts`) so the editor loads straight into the reducer. Distinct from the pre-existing `loadInquiryOffers` (id/status/version/total/updatedAt only, used for the version-chip list) and from the CLIENT-facing offer reader in `client-inquiry-details.ts` (explicit narrow column list, held closed by `client-readers.static.test.ts`) — `loadOfferForEditor` must never be reused by a client surface. Also does one batched `talent_offerings.amount_cents` lookup per distinct `source_service_id` on the offer's lines for the price-drift hint (`catalogNowCents`, D-MSG-136).

D-MSG-134 (L6) · 2026-09-17 · L6 · Six new `messaging-engine.ts`-style writers, thin `staff()`-gated wrappers around `lib/inquiry/inquiry-engine-offers.ts` exactly the way the pre-existing `messagingSendOffer` wraps `sendOffer`: `messagingCreateOffer`, `messagingLoadOfferForEditor`, `messagingUpdateOfferDraft`, `messagingReopenOfferForAmendment`, `messagingCounterOffer`, `messagingListOffers`. `offerEngineFail` translates an `EngineResult` failure into the Messages refusal catalogue; `conflict` / `error === "version_conflict"` map to `version_stale` (not `conflict`, which the rest of Messages uses for the conversation row's own optimistic lock — a different surface) so the sheet's three-way choose reads the right code. Split into a NEW `lib/server-actions/messaging-offers.ts` rather than appended in place, because `messaging-engine.ts` was already at 800 lines (`max-lines` ESLint rule) before this lane touched it — same move S6 made for its own 4 money actions (`messaging-money-actions.ts`); `staff()` is now `export`ed from `messaging-engine.ts` for `messaging-offers.ts` to import, everything else (`uuid`/`version`/`scoped`) is redefined locally rather than adding more cross-file exports for three one-line values.

D-MSG-135 (L6) · 2026-09-17 · L6 · No partial acceptance (owner ruling 2) reads through to revision: an ACCEPTED (or DECLINED/EXPIRED) offer is never edited in place. `reopenOfferForAmendment` itself only accepts a `sent` offer (`not_amendable` otherwise — read from the engine source, not assumed) so "revise" on one of those instead opens a fresh version via `counterOffer` (which calls `createOffer` under the hood), mirroring the old workspace Offer tab's "Counter" path. `OfferEditorSheet.loadEverything`: `sent` -> `messagingReopenOfferForAmendment` (same offer id, bumps its version, flips to draft); `accepted`/`declined`/`expired` -> `messagingCounterOffer` (new offer id, v(next)); `draft` -> load and edit as-is (this is what re-entering an unsent create_offer draft, or continuing a draft revision, looks like).

D-MSG-136 (L6) · 2026-09-17 · L6 · Price-drift hint uses the real `priceDrift` function (`lib/pos/price-drift.ts`), not a placeholder: `loadOfferForEditor` reads `talent_offerings.amount_cents` for every line's `source_service_id` and stamps it as `OfferDraftLine.catalogNowCents`, which `priceDrift(line, catalogNowCents)` compares against the line's own recorded `catalogPriceCentsAtAdd`/`priceSnapshotCents`. A custom line or one whose `talent_offerings` row was deleted/archived reads `catalogNowCents: null`, which `priceDrift` treats as "no catalog item", never a drift.

D-MSG-137 (L6) · 2026-09-17 · L6 · Reused rather than reimplemented, per the lane brief: `components/admin/shell/internal/messages/shared/offer-local-snapshot.ts` (`writeOfferSnapshot`/`readOfferSnapshot`/`clearOfferSnapshot`) and `offer-save-state.ts` (`classifySaveError`, `canSendOffer`) are imported directly into `OfferEditorSheet` — both modules are plain TypeScript with no admin-shell import, confirmed before importing across the `screens/messages-v5` boundary. A `version_stale` save error snapshots the local draft to localStorage (same as an `auth` classification) so the three-way conflict choice never loses the coordinator's in-progress edit even if they close the tab; "Keep mine" re-saves over the newer version (last write wins — a true three-way MERGE of individual lines is not built in this pass, flagged as a follow-up, not silently assumed done); "Use their version" reloads.
D-MSG-140 (L7) · 2026-09-17 · PaymentRequestSheet registers "request_payment" (already in `ShellActionId`/`ROUTES`, checked before `NextStep.tsx`'s "coming" table in the shell's `dispatch`). Amount ladder (deposit/full/other) reads the inquiry's ACCEPTED offer (`messagingLoadOffers` → `selectAcceptedOffer`), never the selected target's own record — the deposit rule lives on the offer (owner decision 4), independent of what the payment is collected against. `lib/messaging/sheets.ts`'s `OfferRow` gained `depositPct`/`depositAmountCents` (additive select + fields; `deposit_pct`/`deposit_amount_cents` already exist on `inquiry_offers`, read the same way `lib/messaging/confirm.ts` reads them for the confirm gate). Pure helpers + 17 tests in NEW `lib/messages-v5/payment-view.ts`: `depositFor` mirrors `updateOfferDraft`'s own `round(total_client_price_cents * depositPct / 100)` formula exactly, `amountOptions`, `dollarsToCents`, `paymentTargetsFrom`/`defaultPaymentTarget`, `canMintPaymentLink`, `openRequestFor`.

D-MSG-141 (L7, SEAM) · 2026-09-17 · `RecordChip.recordId` for `appointment`/`reservation`/`class_enrolment`/`tickets` is a booking or admission id, NOT an `orders.id` — every `syncConversationRecord` caller for those kinds passes a booking/admission id (`lib/scheduling/cancel-booking.ts:124`, `lib/orders/purchase.ts:768`, `lib/pos/classes/checkin.ts:106`, etc.), while `messagingRequestPayment` and a POS "Collect here" deep link both need a real `orders.id`. `canMintPaymentLink` therefore restricts "Send a pay link" and "Collect here" to an `order` chip only; bridging the other id space (through `resolveMoneyRecordLines`'s admission lookup, which itself assumes `recordId` IS an admissions id — a SECOND mismatch, since the sync writers pass a booking id for `appointment` specifically) needs a reader this lane does not build. Filed, not papered over: the OLD classic-shell `MessagesShell.tsx:340-354` already does `orderId: chip.recordId` for an `appointment` chip with no such guard — a live, pre-existing bug in that surface, not something this lane's "coming" honesty should imitate. "Record as paid outside" still reaches every kind through `messagingRecordOutsidePayment`'s own inquiry-level fallback.

D-MSG-142 (L7, SEAM) · 2026-09-17 · "Record as paid outside" against a non-`order` target (appointment/etc.) falls through to `markInquiryPaidInCash(..., "full")` inside `messagingRecordOutsidePayment` (`messaging-money-actions.ts`, pre-existing, D-MSG-43) — that function takes NO amount at all and always settles the full balance, so an amount picked in this sheet (deposit/other) is honored for an `order` target but silently ignored for any other kind. Not fixed here (no writer exists that takes a partial amount for that path); documented so nobody reads the sheet's amount picker as authoritative for that combination.

D-MSG-143 (L7) · 2026-09-17 · "Collect here" (owner brief: reuse `CollectSheet` if importable, else deep-link) resolves to the deep-link form: `admin/pos/CollectSheet.tsx` takes a POS-specific prop surface (`PosCollectionMethodState`, cash-outbox wiring, tender math) built for a live sale screen, not a thread-context record id — embedding it here would make Messages a second POS surface rather than a front door to it (Principle 0). `PaymentRequestSheet` instead renders a "Collect here" action that closes the sheet and is meant to navigate to `/{tenantSlug}/admin/pos?view=sell&order={orderId}` (only offered for an `order` target, D-MSG-141); the wrapper does not itself call `router.push` (no router handed to `ActionSheetProps`) — `onSend` closes the sheet with `collectHref` computed and ready for the shell/caller to use. Seam for a later lane: wire the actual navigation.

D-MSG-144 (L7) · 2026-09-17 · CancelRefundSheet adds TWO new `ShellActionId`s, additive on `contracts.ts`'s union and `NextStep.tsx`'s `ROUTES` (both `{ kind: "coming", seam: "L7" }`, since the registry check in the shell's `dispatch` runs before that table — see the comment left in `NextStep.tsx`): `cancel_record` (`messagingPreviewCancel` → `messagingCancelRecord`, full/partial/keep refund) and `refund` (`messagingRefund`, no cancellation). Neither is dispatched by any wired button yet in this wave (no context-panel/tray action fires them) — the plumbing exists for the moment a later lane adds a caller, same pattern L1-L4 left for their own not-yet-wired extras.

D-MSG-145 (L7, SEAM) · 2026-09-17 · `messagingRefund` takes a `paymentId` (a `booking_transactions` row), which nothing on `RecordChip`/`Essentials` carries — S6 built the write path assuming a caller already has one. NEW `loadRefundableTransaction` (`lib/messaging/money.ts`, additive, reuses `resolveMoneyRecordLines` + the existing private `loadPaidTransactions`) resolves the newest paid transaction with `grossAmountCents - refundedCents > 0` for a given `recordKind`/`recordId`, and NEW server action `messagingLoadRefundableTransaction` (`messaging-money-actions.ts`) wraps it for the `refund` (no-cancellation) sheet only — `cancel_record` never needs a `paymentId`, it stays on `messagingCancelRecord`'s own `recordKind`/`recordId` shape.

D-MSG-146 (L7) · 2026-09-17 · Both sheets split View from Wrapper across TWO FILES, not two exports in one file (`RenameInline`'s pattern) — the wrapper imports `messagingRequestPayment`/`messagingRecordOutsidePayment`/`messagingCancelRecord`/`messagingRefund`, which chain into `lib/messaging/money.ts`'s `import "server-only"`, and this worktree's `node_modules` has no `server-only` package at all (not a symlink glitch — confirmed absent under the real `web/node_modules` too) outside the `NODE_OPTIONS='--require ./scripts/register-server-only-test.cjs'` shim `test:messaging`/`test:money` set. A render test that imports the wrapper file crashes on `MODULE_NOT_FOUND: server-only` under plain `tsx --test`. `PaymentRequest.view.tsx`/`CancelRefund.view.tsx` hold the pure `*View` components and import no server action; `PaymentRequest.tsx`/`CancelRefund.tsx` hold the stateful wrapper + `registerActionSheet(...)` calls and import from the `.view` file. Copy: `dashboard.messagesV5.paymentRequest.*` (new namespace — `payment.*` was already taken by `PaymentCard`'s flat state-pill strings) and `dashboard.messagesV5.cancel.*`, EN/ES/FR, added to `kit/copy.ts` and all three `web/messages/*.json` (61 new keys per locale, verified by `copy.test.ts` and `verify:ui-messages`).
D-MSG-150 (L8) · 2026-09-17 · L8 · `ConfirmRecordSheet` (board D19 desktop 520 / M09 mobile full screen, D08 "Confirm order") registers `"confirm"` in `screens/sheet-registry.tsx` — the shell's `dispatch()` already checks `registeredActionSheet(id)` before `routeShellAction`, so this needed no edit to `MessagesV5Shell.tsx` or `NextStep.tsx` (its `ROUTES["confirm"]` still reads `{ kind: "coming", seam: "L8 confirm..." }`; that line is now unreachable for this action id and left as-is per the "do not edit the shell or NextStep.tsx" instruction — a later cleanup PR can remove the dead route once every "coming" lane has landed). Wraps S3's `messagingConfirmRecord` (`lib/server-actions/messaging-confirm.ts`) directly from the client component (a `"use server"` export, callable like any server action) rather than adding a `ShellSheetContext` field for it. Split like L4's `RenameInline`: pure `ConfirmRecordView` (all six required states, `ConfirmRecord.render.test.tsx`) + the thin stateful `ConfirmRecordSheet` wrapper. `ConfirmOrderButton` (D08) is exported for the basket/order card to mount later; it dispatches the SAME `"confirm"` action id (the registry is one component per id, so it cannot own a second sheet) and relies on the sheet's own default-pick rule (prefer an accepted offer, else the draft) to land on the draft when no offer chip exists on that conversation.

D-MSG-151 (L8) · 2026-09-17 · L8 · Source picker (`confirmSourceOptions`, `lib/messages-v5/confirm-view.ts`) is built from `RecordChip[]` (S2, already on `ShellSheetContext.chips`) plus `OfferRow[]` from the EXISTING server action `messagingLoadOffers` (`lib/server-actions/messaging-sheets.ts`, itself wrapping S3's `loadInquiryOffers`) — no new reader added. Only `status === "accepted"` offers are candidates; an `order` chip already `paymentState === "paid"` or whose `fulfilmentState` is past `hold` (`preparing`/`ready`/`fulfilled`/`seated`/`checked_in`) is excluded, since `confirmRecord` would answer `already` for either.

D-MSG-152 (L8) · 2026-09-17 · L8 · `depositGate` is a CLIENT-SIDE PREVIEW ONLY, read from the picked option's `RecordChip.paymentState` (S2) — it is not the authoritative rule. `confirmRecord` reads the offer's own `deposit_pct`/`deposit_amount_cents` (or the draft's offering `reserve_mode`) server-side and is the only thing that can actually refuse `deposit_required`; a chip with no payment signal (`paymentState: null`) renders `due: false` (never a false block on missing data), and if the server disagrees anyway, its `deposit_required` refusal still surfaces via the sheet's own `refusal` state, which the person can clear by typing the override reason. `overrideReasonValid` / `CONFIRM_OVERRIDE_MIN_CHARS` (8) in `confirm-view.ts` MIRROR the same-named export in `lib/messaging/confirm.ts` by value, not by import — that file is `"server-only"` and importing it from a client component would break at build time. If the server's minimum ever changes, this copy needs a manual update; flagged here rather than silently risking drift.

D-MSG-153 (L8) · 2026-09-17 · L8 · "Checked just now" does not run a live per-line availability check before the click. `buildConfirmPlan` (S3, pure) is importable client-side, but `runConfirmChecks` needs the calendar-busy and capacity-remaining readers, which are server-only and NOT exported (`defaultReaders` is a private function inside `lib/messaging/confirm.ts`, taking a service-role Supabase client). Re-implementing those two queries here to get a live ✓ would be exactly the re-implementation Principle 0 forbids. Chose the brief's documented fallback: the sheet lists WHAT WILL BE CHECKED by category only (`checkedCategoriesFor`: an offer checks "People" and "Resources & seats", a draft checks "Resources & seats" only — matching `buildConfirmPlan`'s own per-line kinds), each row copy reading "Will be checked on confirm". The real check is the one `messagingConfirmRecord` runs on click; its `unavailable` refusal's `conflicts[]` are rendered verbatim (`ConfirmConflict.why`, already the full "<line> is no longer free on <date>" sentence the engine built) — the sheet does not reformat or re-derive that text.

D-MSG-154 (L8) · 2026-09-17 · L8 · `createdListFor(source, hasPeople)` — "What gets created" (D19: project + assignments + calendar blocks + balance reminder + client confirmation, or D08: order + kitchen ticket + receipt). `OfferRow` (S3) carries no field for whether an offer has a talent line, and fetching the offer's line items just to answer that one boolean would be a new reader for a display-only list; the sheet calls it with `hasPeople = true` for every offer (Messages v5's confirm flow is D19's appointment/booking path, where a talent line is the common case). A future lane that extends `OfferRow` with a real `hasTalentLines` (or exposes the line items another way) should thread it through instead of the hardcoded `true`.

D-MSG-155 (L11) · 2026-09-17 · L11 · "Mark picked up" on `OrderCard` (D08) was a writer button with no wired action (`onAction("mark_picked_up")` reached nothing outside this kit); fulfilment is the POS's own state machine and Messages has no fulfilment writer (Principle 0). Changed it to a read-only sentence, `copy.order.fulfilmentFromPos` ("Fulfilment updates from the POS"), shown once `step === "paid"`. `OrderCardAction` drops `"mark_picked_up"` (unused, no caller). `OrderCard.render.test.tsx` updated to assert the sentence and the absence of the action attribute. "Confirm order" is unchanged: it already dispatches the shell's `"confirm"` action id, which the registry (`sheet-registry.tsx`) already routes to L8's `ConfirmRecordSheet` — the sheet that mounts `ConfirmOrderButton` — so no new wiring was needed for that half of the brief.

D-MSG-156 (L11) · 2026-09-17 · L11 · Closes the "no card kind" half of D-MSG-122 (L5): table choices ARE sendable now. `cardKindForCategory("table")` returns `"service_card"` (same kind talent/package/service already use) instead of `null`; `optionCalls` in `lib/messages-v5/items-picker.ts` emits one `send_options` call with `payload.variant = "table"` built by the new pure `tableChoicePayload()` (`lib/messages-v5/record-cards.ts`), carrying `tables: [{ label, partySize, startsAt, spaceId }]`. No new `CardKind`, no CHECK constraint change. `ServiceCardPayload` (`lib/messaging/cards.ts`) gains optional `variant`/`tables`/`holdExpiresAt` fields, additive. `tableChoicePayload` is exported specifically so L9's client-side renderer can build the identical shape rather than re-deriving it (per the brief). The OTHER half of D-MSG-122 is unchanged and still open: a table as a draft/offer LINE still has no writer (`lineCalls` still emits `{ action: "seam", category: "table", reason: "no_writer" }`), and a client's pick on the new table choices card does not yet hold capacity — `service_card` is not in `HOLDING_CARD_KINDS` (it is shared with non-holding talent/service picks, so adding it there would wrongly gate those too). Filed as D-MSG-157.

D-MSG-157 (L11) · 2026-09-17 · L11 · SEAM: a client's pick on a table-choices card (D-MSG-156) is visible only — no reservation-hold writer is wired to it. `lib/reservations/reserve.ts`'s hold path exists but nothing in the messaging engine calls it from a `service_card`/`variant:"table"` pick today. Owner decision 3 ("choices never book") is respected (nothing books), but the D09-style "pick holds the time" behavior D21 implies for tables is NOT yet true — the card only shows a static "Held" step until a later lane wires the pick to `lib/reservations/reserve.ts`'s hold and starts writing `holdExpiresAt` back onto the message payload.

D-MSG-158 (L11) · 2026-09-17 · L11 · Thread record cards for reservation/tickets (D21): new kit components `TableCard.tsx` (Held → Confirmed → Seated → Closed) and `TicketsCard.tsx` (Paid → Issued → Checked in N of M → Done), mapped in `ThreadCards.tsx` from `cardKind === "service_card" && payload.variant === "table"` and `cardKind === "tickets_card"`. Both ladders and the hold countdown come from ONE new pure module, `lib/messages-v5/record-cards.ts` (`ladderFor`, `holdCountdown`, `formatHoldCountdown`, `cardPropsFromMessage`, `cardPropsFromChip`) so the message card and a panel chip would compute the identical step from the same `RecordChip.paymentState`/`fulfilmentState` (S2) if a later lane wires the panel to call it too — not done in this lane (the panel's `mainRecordChip`/`summaryFor`, L3's file, was left untouched to avoid touching another lane's tested surface for a non-required change). `TicketsCardPayload`/`ProfessionalTimesPayload` gain optional `holdExpiresAt` (+ `capacity`/`checkedIn` on tickets), additive, null until the events/reservations engines write them — filed as the seam a later lane closes when it wires the real hold/check-in numbers onto the payload. `TimesCard`'s `hold_ended` button now reads "See alternatives" (`copy.times.seeAlternatives`) via a per-call copy override at the `ThreadCards` call site, not a `TimesCard` prop change, to avoid touching that component's existing contract; `picked` now shows a real countdown from `payload.holdExpiresAt` instead of always rendering with no `holdLeft`.

D-MSG-159 (L11) · 2026-09-17 · L11 · 24 new EN/ES/FR keys under `dashboard.messagesV5.{order,table,tickets,ladder,times}.*` (`order.fulfilmentFromPos`, the `table.*`/`tickets.*` sections, `ladder.{held,seated,issued,checkedIn,done}`, `times.{seeAlternatives,holdLeft}`). `tickets.quantity` reads differently per locale on purpose ("{count} × {label}" EN, "{count} entradas de {label}" ES, "{count} billets de {label}" FR) to keep `copy.test.ts`'s "es/fr are translations, not copies" check under its 12-identical-string budget.
D-MSG-172 (L10) · 2026-09-17 · POS Messages dock (`PosMessagesDock`, `components/messages-v5/pos/`), mounted from `messages-client.tsx` behind `NEXT_PUBLIC_MESSAGES_V5=1` (same flag L2 used for the workspace inbox) instead of the v1 `MessagesShell`. Additive shell prop `hideInboxRail?: boolean` on `MessagesV5ShellProps` (`shell/MessagesV5Shell.tsx`, `shell/layout.ts`'s `ShellClassInput`, `shell.css`'s `.hide-inbox` rule) — hides the inbox column so the thread + context panel take the width; the dock draws its own This client / Inbox tabs instead of a second inbox list. Shell internals (dispatch, sheets, composer, engine) untouched. "This client" starts on `initialCustomerInquiryId`, resolved SERVER-SIDE in `messages-view.tsx` by calling `messagingLoadInbox` (the SAME reader the shell itself calls, no new query) and `findConversationForOrder` (`lib/messages-v5/pos-continuity.ts`) against the counter's own open sale — a chip already on `conversation_records`, never a new table read. `shouldDockBesideSale` is the owner's 1100px rule: ≥1100 dock beside the sale, below it a drawer/phone overlay, one code path (the shell's own width-based layout already collapses to mobile under 900 inside that same drawer). `MessagesModeClientProps` gained `tenantSlug`, `currentUserId`, `openOrderId`, `initialCustomerInquiryId` (additive); `page.tsx` now passes `tenantSlug` into `messagesModeView`.
D-MSG-173 (L10) · 2026-09-17 · SEAM: `?view=messages` (`page.tsx`) is a full branch that REPLACES the mode's own `PosFrame` content — the live, editable sale (`pos-client.tsx`) is a separate render tree, not mounted alongside this dock. "Dock beside the sale" today means the strip + tabs this lane built, with a one-click link (`returnHref`/`returnLabel`, already built server-side) back to the real sale, not a live sale panel rendered in the same DOM. Making the sale genuinely visible behind the ≥1100 dock / <1100 drawer needs `PosMessagesDock` mounted inside `pos-client.tsx` itself, touching the counter's own render tree — out of this lane's scope ("front door to the POS engine", not a POS rewrite). Filed for the POS owner.
D-MSG-174 (L10) · 2026-09-17 · SEAM: the counter's phone navigation is `PosHeader`'s `portraitMenu` dropdown (`pos-client.tsx`), listing `POS_MODE_META.counter.destinations` (sell/orders/receipts/shifts/issues/messages) plus device/connection/display/workspace doors — not a bottom tab bar, and there is no `clients` destination in `lib/pos/modes.ts`. `PosPhoneNav` (`components/messages-v5/pos/PosPhoneNav.tsx`) draws the literal Sale · Orders · Messages · Clients · More bar the brief and M06 specify, render-tested (order, active state, unread badge), but is NOT wired into `pos-client.tsx`'s real phone chrome — that reorders navigation on every counter screen and needs a new destination, bigger than one dock component. `PosMessagesDock`'s own phone path (<900px, inside the <1100 drawer) DOES ship live: the "Return to sale" strip sits on top of the full-screen thread overlay, one render path shared with the desktop drawer.
D-MSG-175 (L10) · 2026-09-17 · Continuity: `lib/messages-v5/pos-continuity.ts` (pure, 9 tests) proves what a Workspace ⇄ POS switch preserves — the inquiry id and the open order id as `?inquiry=`/`?order=` query params; the unsent draft needs NO new key since `draftStorageKey(tenantId, locationSlug, inquiryId)` (existing, `draft-storage.ts`) is already route-independent. Two link sites edited minimally: `PosModeSwitch.tsx`'s `openMode` (workspace top bar's POS · Counter switch) now builds `posSwitchHref` and `goWorkspace` (the same hook's Workspace door, shared with the POS rail's MODE chip per its own doc comment) now builds `workspaceSwitchHref`, both reading `?inquiry=`/`?order=` off the CURRENT url via `useSearchParams` — the hook has no other view of "what thread is open". `findOpenConversationForCustomer` (contact-match fallback when a brand-new order carries no chip yet) is written and tested but not yet called from `messages-view.tsx` (seam: needs the sale's customer contact fields threaded through, which `loadPosSale`'s current return shape may not carry — left for the next lane to confirm rather than guessed here).
D-MSG-160 (L9) · 2026-09-17 · L9 · The token thread (/c/t/[token]) had NO client composer: the POS-era `CustomerThread` only offered "continue by SMS code" (`messagingIssueVisitorCode`), and `messagingGuestDraftAdd` was the only token-identity writer. New thin server action `messagingClientReply` in NEW `lib/server-actions/messaging-client.ts` (`messaging-engine.ts` is at the 800-line cap, same split S6/L6 made) writes ONE `inquiry_messages` row through the existing `insertMessage` with `senderUserId: null` and kind `text`; identity is the signed thread token (`verifyThreadToken`), exactly as `messagingGuestDraftAdd` treats it. `guest_session_id` is NOT stamped (the link holder has no guest cookie session; `isFromClient` keys on the null sender, and the DB trigger keys conversation state the same way). Every action in that file verifies the token before any read (static test `client-bundle.static.test.ts`), and the file never takes the staff guard.

D-MSG-161 (L9) · 2026-09-17 · L9 · "Send my choice" (menu_options / service_card / class_card / tickets_card) → `messagingClientChoose`: the POS's own `addLine` on the conversation's shared draft (`orders` with `status = draft`, `source_channel = messages`, the same row `messagingEnsureSharedDraft` finds), `proposedBy: "client"` (S5), one unit per pick, `sessionId` passed through for a class/ticket line; no `expectedVersion` is sent because the card carries no draft version and the POS writer bumps it per line. When no shared draft exists the action opens one with `createDraftOrder` on behalf of `inquiries.owner_user_id` (a draft records who opened it; `no_owner` refusal if the conversation has none). The pick is recorded ON THE CARD ROW (`card_payload.state = "selected"`, `chosenIds`, `chosenAt`) so a reload draws the sent state, and a client `text` row with the labels so staff see the choice in the stream. A pick never books (owner decision 3). SEAM: a class/ticket pick adds the draft line but places no SEAT hold (capacity reserve is the S3 confirm path's `holdDraftOrderCapacity`); only a TIME pick holds (D-MSG-163). L5's ticket tiers map to the card's `offeringId` (a tier is a variant; the draft line is the offering, variant not chosen).

D-MSG-162 (L9) · 2026-09-17 · L9 · Accept binds to an EXACT offer version: `messagingClientAcceptOffer({ offerId, offerVersion })` refuses `version_stale` when `inquiry_offers.version` differs from the card's, `already` when accepted, `expired` when the status is not `sent` or `valid_until` has passed. WRITER: when the inquiry has a `client_user_id`, the engine's own `clientAcceptOffer` (`inquiry-engine-approvals.ts`, acting as that user through the service-role client, the same self-elevation the engine already does for client writes) — the real multi-party approval RPC. FALLBACK (guarded, filed here, not hidden): a link-holder on an inquiry with NO client user cannot go through `engine_submit_approval` at all — it needs an `inquiry_participants` row and `role = 'client'` REQUIRES `user_id` by CHECK (`20260523000000_inquiry_participants_hardening.sql`). `acceptDirect` then writes exactly the two rows the RPC's all-accepted branch writes (`inquiry_offers.status = accepted, accepted_at`; `inquiries.status = approved, next_action_by = coordinator, version + 1` guarded by the inquiry version) and ONLY when no other party's approval is pending on that offer (`inquiry_approvals` non-accepted rows → `not_allowed`); it emits no engine event. Decline mirrors it (`clientRejectOffer` when a client user exists, else the same two rows that function writes: offer `rejected` + `rejection_reason = other` + free text, inquiry back to `coordination` with `current_offer_id = null`). Both paths insert an `offer_state` card (`offerStatus: accepted | declined`, sender null) so the staff thread shows the outcome, and the decline reason lands as a client `text`. A follow-up should give link-holder clients a proper participant identity so the fallback can be deleted.

D-MSG-163 (L9) · 2026-09-17 · L9 · A time pick (`professional_times`) → `messagingClientPickTime` → `placeReservationHold` (`lib/scheduling/reservation-hold.ts`, the storefront's own talent hold: `talent_holds`, `hold_strength = firm`, `inquiry_id` set), `ttlSeconds = 900` (board C01/D09 "held 15 min"), end = start + `talent_offerings.duration_minutes` (60 when the card names no offering or it has no duration). The slot must be one the card offered and in the future. The hold is recorded on the card row (`pickedStartsAt`, `holdId`, `holdExpiresAt`); the client card derives `picked` / `hold_ended` from `holdExpiresAt` against a ticking `now` and shows the countdown; after the hold ends the slots reopen and a new pick places a new hold (a live hold refuses `already`). `slot_taken` (gist exclusion) maps to the catalogue's `unavailable` sentence with the card's own alternatives line under it ("That time was just taken. Pick another, or write to {business}."). No calendar or booking row is written (Principle 0); staff confirm through S3/L8.

D-MSG-164 (L9) · 2026-09-17 · L9 · "Ask for a change" (on a sent offer and on a confirmation card) → `messagingClientRequestChange`: ONE `change_request` card row (`state: sent`, `recordKind`/`recordId`, `requestedAt`, the client's text as body and summary, sender null). It does NOT call `counterOffer`: that engine function is the coordinator's (it creates a new offer VERSION for staff to price, D-MSG-135), and letting the client mint offer versions would make the link a second offer editor. Staff answer through L6's revise path; the client sees the request card read-only with its state ("Sent" / "Applied" / "Not possible"), the `change_result` card the same way. `hoursBefore` / `freeUntil` / `oldWhen` / `newWhen` are null/0 on a client-raised request (the policy fields belong to staff's ImpactPreview).

D-MSG-165 (L9) · 2026-09-17 · L9 · Client readers: NEW `lib/messaging/client-link.ts` (server-only). `loadClientOfferSummaries` is the client-audience twin of `loadOfferForEditor` (D-MSG-133): explicit column list `id, status, version, total_client_price, currency_code, valid_until, deposit_pct, deposit_amount_cents, refund_policy_key, notes, created_at` + lines `offer_id, label, units, total_price, sort_order`; no `talent_cost`, `coordinator_fee`, discount/tax, authorship (held closed by `client-readers.static.test.ts` and `client-bundle.static.test.ts`). Cents are derived (`total_client_price * 100`, `total_price * 100`), deposit = explicit `deposit_amount_cents` else `round(total * pct / 100)` (the same rule L7's `depositFor` uses). The offer CARD is drawn from the engine's own `offer_event` row (`sendOffer` inserts `message_kind = offer_event`, `card_payload.offer_id`; there is no `offer_review` writer in the engine today) joined to the live summary: one card per offer, on the LAST "sent" event (`offerCardMessageIds`), every other offer row reads as one system line. `loadOpenPaymentCode` returns the newest OPEN `payment_links` row on the conversation; `loadClientLinkBusiness` reads `agency_business_identity.public_name` / `default_locale` (falls back to `agencies.display_name`) and the owner's first name from `profiles.display_name`. The thread locale is the WORKSPACE default locale (D-151), resolved server-side and handed to the client component (`translatorFor(locale)`), never the visitor cookie.

D-MSG-166 (L9, SEAMS) · 2026-09-17 · L9 · (a) "Accept and pay deposit" redirects to `/pay/<code>` ONLY when an open payment link already exists on the conversation: `createPaymentLink` needs an `orders.id` (D-MSG-141) and an accepted offer has none until staff confirm/convert, so the client cannot mint a link from the link; after acceptance without a link the card reads "{business} will send you a payment link to confirm" and staff use L7's Payment request sheet. `messagingRequestPayment` stays staff-only; no `clientAcceptAndPay` writer was added because there is no order to mint against. (b) "Save this conversation to your email" renders greyed with a title: `sendGuestClaimToEmail` (`app/t/[profileCode]/_actions/guest-chat-actions.ts`) is gated on the GUEST COOKIE session owning the inquiry (`loadOwnedInquiry`), which a link holder does not have; a token-identity variant is a follow-up. (c) Receipt on the confirmation card is greyed (no client-safe receipt route keyed by the token yet). (d) Boards C01/C02 draw an avatar per choice; the L5 card payload carries labels/prices only, so the client choices card shows label + price. (e) The scoped tsc over this lane's files is clean; the same temp config surfaces three PRE-EXISTING errors in wave-B files this lane did not touch (`screens/sheets/OfferEditor.tsx` imports `ScreenVariant` from `../copy` which does not export it, and a `"system"` line author / `"other"` amount kind narrowing in `OfferEditor.tsx` / `PaymentRequest.tsx`) — reported, not fixed here.

D-MSG-200 · 2026-09-17 (L13 guest dock) · The guest dock (TalentProfileChatLauncher / MiniChatPanel on every storefront and the tulala.digital apex) is IMPROVED, not rebuilt: Impronta's dock works end to end and the apex failures were two mount bugs (support "?" FAB covering the chat bubble; `AgencyChatLauncherMount` rendered outside `PublicDiscoveryStateProvider`), fixed on `main` in PR fix/hide-marketing-help-fab. Design of record for the dock: artifact https://claude.ai/artifact/8ZEetLo1Wk6fMhazV9h22K (boards F01–F08, Items per business, control→engine map).

D-MSG-201 · 2026-09-17 (L13) · ONE client card surface. The dock draws every v5 card kind (`CLIENT_CARD_KINDS`) through `components/messages-v5/client/ClientCard.tsx`, the dispatch `ClientThreadView` uses, extracted as a component; and acts through `use-client-card-actions.ts`, the link's handlers extracted as a hook. `ClientThread` is left as-is (its render tests hold); L12's sweep may move it onto the hook. Text rows keep the dock's own bubbles, so Impronta's look is unchanged.

D-MSG-202 · 2026-09-17 (L13) · Identity bridge. The dock is guest-cookie identified; the v5 client actions are thread-token identified. `getGuestThreadMessages` already proves the cookie OWNS the inquiry (`loadOwnedInquiry`), which is the same proof the token carries, so on the FULL load (afterIso null) it now returns `v5: { threadToken, threadTokenExpiresAt, offers, payCode }` via `_actions/guest-thread-v5.ts` (sibling; the actions file is past the size cap). The token expiry follows `resolveThreadTokenExpiry` (D-MSG-6). Offers and pay code come from `lib/messaging/client-link.ts`, the link page's own readers. No new writer. Guard: `_chat/guest-dock-v5.static.test.ts`.

D-MSG-203 · 2026-09-17 (L13) · Offer rows move to the v5 offer card only once the offer summaries are loaded (`v5.offers` non-empty); until then, and when `GUEST_COOKIE_SECRET` is unset (token null), the legacy enriched `offer_event` bubble keeps drawing them and card actions refuse `not_allowed` instead of throwing. Nothing the dock showed before is lost on a tenant that never sends a v5 card.

D-MSG-204 · 2026-09-17 (L13) · The guest dock is TENANT-WIDE and switchable in Settings (owner ruling this session). Two additive columns on `tenant_guest_chat_settings` (migration 20261231270000_guest_chat_dock_v5_settings.sql, collision-checked against production, NOT yet applied to the isolated branch: no `.env.capacity-isolated.local` in this lane; integrator please apply): `items_tab` (null/true = the Items tab shows) and `cards_v5` (null/false = legacy bubbles; true = v5 cards; OPT-IN until QA, decision 10). Both read by `loadGuestChatSettings` (fail-open for items_tab, opt-in for cards_v5, `guest-chat-settings-shape.test.ts`), written by the admin "Guest chat" drawer under a new "Inside the chat" section, and threaded onto `MiniChatBrand` by both launcher mounts via `lib/inquiry/guest-dock-flags.ts`.

D-MSG-205 · 2026-09-17 (L13) · Lineup → Items, labelled per business through the Words engine: new row `customers.chat_items` (fallback "Items"/"Selección"); when the operator has not set it, `lib/words/chat-items-label.ts` derives it from the preset (representsPeople → "Talent & services"; appointments → "Services"; events without reservations → "Tickets & tables"; menu → "Your order"; else "Items"). `GuestDockNav` reads `brand.dockItemsLabel` and hides the tab when `dockItemsTab` is false. The i18n default `dockNavLineup` is now "Items" (es "Selección", fr "Sélection").

D-MSG-206 · 2026-09-17 (L13 wave 2) · The dock's Items tab shows the conversation's NON-talent items read-only: the shared POS draft lines (newest `orders` draft on the inquiry with `source_channel = messages`, the same draft `messaging-client.ts sharedDraft` writes to) with the S5 author flag rendered as "you chose" / "added by {business}" / "confirmed", and the `conversation_records` chips with both state families (labels in EN/ES/FR under `public.guestChat.dockItems*`). Loaded by `loadGuestConversationItems` in `guest-thread-v5.ts` on the full thread load; the `order_lines` select names only client-safe columns (static test). Adding happens through the cards in Chat; removing and booking never from the shelf (decision 3). Holds still show on the times card in Chat (countdown), not as shelf rows; a shelf "Held" row is deferred until L11's hold reader exposes expiry per record.

D-MSG-216 · 2026-09-18 (P4 F03) · Closes the D-MSG-206 shelf-hold deferral. `GuestRecordChip` gains optional `holdExpiresAt` + `label`. `loadGuestConversationItems` batch-reads private `professional_times` / `service_card` payloads and `decorateHoldChips` (`guest-hold-rows.ts`) overlays the picked slot/table onto the chip. The Items row shows the label plus `formatHoldCountdown` (live / ended / omitted when no expiry). The 1s clock in `use-guest-dock-model.ts` also ticks for table payloads and chips that already carry expiry. Tables still do not invent a hold (D-MSG-157). No reservation-hold writer.

D-MSG-207 · 2026-09-17 (L13 wave 3) · The dock shows ONE next step above the composer, derived from state (`lib/messages-v5/guest-next-step.ts`, pure, 6 tests): open payment link → Pay {amount} (deposit when the pending offer has one); pending offer → Accept offer v{n} · {total}; held time → "Waiting for {business} to confirm" (sentence, no button); confirmed record → "You're booked · {date}"; nothing → hidden. A draft not yet sent stays the SendToAgencyBar's job. Buttons call the same `useClientCardActions` the cards use; the card model + clock moved to `use-guest-dock-model.ts` so the stream and the block share one model (and MiniChatPanelColumn stays under the 800-line cap). Shown only when the tenant's `cards_v5` is on.

D-MSG-208 · 2026-09-17 (L13) · SEAMS filed, not built (Principle 0, no new writers in a lane): (a) **Book again** (owner decision 13, board F02/F06): no `messagingBookAgain` writer exists in `lib/server-actions`, `lib/messaging`, `lib/pos` (grepped); the card needs a writer that duplicates the last confirmed record's lines into a NEW POS draft (`createDraftOrder` + `addLine`, `source_channel = messages`, `proposed_by = client`) and links it to a new conversation on the same client record. The dock draws the card once that writer lands. (b) **Client name self-correction** (owner decision 5, board F07): `messaging-client.ts` has no rename writer for the client side; L9 listed it as coming. (c) The **link-expiry sentence** (F07) states a fact about `/c/t/[token]`, not about the cookie-identified dock, so the dock does not draw it; the link page should. Owned by the program session.

D-MSG-209 · 2026-09-17 (L13 live QA on staging-qa-journeys, fixture tenant, cards_v5 on) · PROVEN: Settings → Guest chat → "Inside the chat" saves; the tab reads "Your order" on the restaurant preset; the v5 offer card draws inside the dock with Accept / Ask for a change / Decline; the next-step block reads "Accept offer v4 · $18.00"; the Items shelf lists the shared draft lines with "added by QA Journeys" and the record chips. FIXED in the dock (this commit): (a) record kind `offer` had no label; (b) "No talent in your inquiry yet" on a business that sells things (new `dockRepresentsPeople` flag from the preset, copy `dockLineupEmptyItems`); (c) Home card said "Your lineup / Talent you've added" (now the per-business label + `homeItemsSub`); (d) engine lines with `metadata.system_event_type` stamped with a staff actor drew as a business bubble ("Offer v3 sent"), `deriveAuthorRole` now returns system for them.

D-MSG-210 · 2026-09-17 (L13 live QA) · DEFECTS OUTSIDE THE DOCK, for the owning lanes: (1) `lib/server-actions/messaging-client.ts` `messagingClientAcceptOffer` → `acceptDirect` (≈line 285): Accept from the dock on inquiry 195d4d01-1d63-456b-a6fa-9df523ab0bc9 / offer 7c082347-61fa-433e-92c4-5a03c18578d2 (tenant 3333…, v4 sent, valid) returned `not_allowed`; the token verified (well-formed, exp 2026-10-18); the only `not_allowed` left is the "another party's approval pending" guard, which likely counts the client's OWN pending `inquiry_approvals` row; the refusal copy "You cannot do that from here" then blames the surface. (2) Offer version disagrees across surfaces: editor header "Offer v2", inbox row "Offer v3 sent", dock card + summaries "v4". (3) The offer editor sheet stays open after "Send v2" (the thread behind shows "Offer sent"). (4) Send times: with "Any service" selected, 3/6 picked, the Send button stays disabled; picking a named service enables it. (5) `ClientOfferCard` footer at 375px inside the dock: "Valid until Thu, Sep 24" wraps one word per line beside the wide Accept button (`components/messages-v5/client/ClientCards.tsx` offer `.cf`). (6) Times for an unverified guest refuse `identity_unconfirmed` by design; the dock has no identity step before a pick (board F07 seam, client writer owed).

D-MSG-212 · 2026-09-18 (L13 wave 5, owner ruling) · The dock's Items tab is the business's front of house, not a receipt: it opens on the CATALOG (category chips per tenant: Talent / Packages / Services / Classes / Tickets / Tables / Menu, from the same `loadItemsCatalog` reader the workspace picker uses, client-safe fields only, `_actions/guest-catalog-actions.ts`), and every row offers the action that fits: talent → Add to lineup (inquiry cart), priced items → Add to inquiry (`messagingClientAddItem`, a sibling of `messagingClientChoose` over the same POS `addLine`, `proposedBy: client`, no card needed), self-serve kinds → Buy now (storefront `/book`, `/events`), anything → Ask (prefills the composer). A visitor with no inquiry gets the early row on the first add (panel ensure path), then the add runs when the token lands. The shelves ("In your inquiry", "Also in this inquiry", "Saved") stay below. Tables/times are browse + Ask only (a hold needs identity, D21).
D-MSG-215 · 2026-09-18 (P3 F05) · Client dock cards: `tickets_card` leaves the chooser once `payload.state` is paid/issued/checked_in/selected and draws `ClientTicketsCard`. "Open ticket" goes to `/q/<code>` when the engine stamped `ticketCode`/`code`/`qrCode`/`linkCode`/`ticketUrl`. SEAM: `messagingSendOptions` / `confirmRecord` never write a door code onto the tickets card (items-picker payload is eventId/sessionId/offeringId/tiers; confirm writes `order_confirmation` with `orderId` only). Without a code the card reads "Your tickets are ready. {business} will send the door link." Cancel/refund is the existing `change_result` (`refundedCents` or a Cancelled/Refunded summary) plus `payment_request.state=cancelled`; no dock-side refund writer. Paid payment card and confirmation cards were already in ClientCards. Live Stripe proof on qa-journeys is the integrator pass after merge.
D-MSG-217 · 2026-09-18 (Front Door Chat v2 P5 / F07) · Closes D-MSG-208b and D-MSG-208c. `messagingClientRename({ token, name })` in `messaging-client.ts` verifies the thread token then calls `renameClientContact`: writes `inquiries.contact_name` (the client's own name, owner decision 5 — not the staff conversation title), bumps `version`, logs `messaging_client_edit` `{ fields: ["name"] }` (D-MSG-93), updates `profiles.display_name` when `client_user_id` is set and `customers.display_name` for the same tenant+email. Email and phone are never touched. Dock Home draws a **Your details** card (`GuestDockDetailsCard`, not a `ClientCard`) wired through `use-guest-dock-model.ts` so `MiniChatPanelColumn` stays under 800. The `/c/t/[token]` page passes `threadTokenExpiresAt` from the token's baked `expiresAtMs` (the value `resolveThreadTokenExpiry` minted, D-MSG-6) into `ClientThread`; one sentence, not drawn on the cookie dock. No guest merge writer (Same person? stays staff).
D-MSG-214 · 2026-09-18 (P2 F06) · Guest Inquiries list rebuilt. Rows are name+time, subject (`projectLabel`), ONE state line, ONE `conversation_records` chip with amount (orders.total_cents / inquiry_offers.total_client_price). Segments Needs you / Waiting on them / Done invert the staff families in `lib/messaging/state.ts` (`guest-inquiries-view.ts`). Closed/cancelled inquiries stay in Done (`isDropped` only hides archived). "Same person?" is a read-only banner when a placeholder name owns two open inquiries; no merge writer. No new POS/calendar writes.
D-MSG-218 · 2026-09-18 (Front Door Chat v2 P6 / Book again) · Closes D-MSG-208a. `messagingClientBookAgain({ token, recordId })` verifies the thread token then `bookAgainFromRecord`: new inquiry via `createInquiryFromIntent` (`source: "book_again"`, `source_context: { rebook_of_inquiry_id, original_booking_id }`, same `guest_session_id` / `contact_*` / `client_user_id`), then POS `createDraftOrder` + `addLine` per **confirmed** line on the named record (`proposedBy: "client"`, `source_channel: "messages"`). Unconfirmed lines are skipped; old `orders.id` and its lines are never updated. No calendar / admissions / booking insert. Dock Home shows Book again when the current thread has a confirmed/fulfilled record chip; Inquiries shows it on booked cards (opens that thread when it is not the active one, because the writer is token-identified). MiniChatPanelColumn stayed 800/800.

D-MSG-213 · 2026-09-18 (L13 Front Door Chat v2 P1) · Catalog landed on main as PR #2116 (`68489d401`). `program/journeys-2026-09` was already at that SHA. Redeploy `dpl_HMxtatRwczo2fXX3MgfPCnApQeGp` (journeys branch, isolated Preview env) aliased to `staging-qa-journeys.tulala.digital`. This cloud agent could not click the dock: Vercel Authentication on the QA host still 302s to SSO even with `_vercel_share` (custom domain and `*.vercel.app`); `get_project_token` is 403; no `VERCEL_AUTOMATION_BYPASS_SECRET` / CLI login in the VM. Live 375px Add / Buy / Ask + "you chose" proof is still owed on a machine that can send `x-vercel-protection-bypass`. Static tests this commit did not ship with #2116: `guest-dock-catalog.static.test.ts` (reader-only, add via `messagingClientAddItem` `proposedBy: client`, Buy now `/book`+`/events`, tables have no Buy now). No catalog product defect found in the files. Tables stay browse+Ask (P4 / D-MSG-157), not a P1 fail.
D-MSG-220 · 2026-09-19 (Front Door Chat v2 P5 live proof) · Amends D-MSG-217. Live on staging c099125b5 the rename persisted (`inquiries.contact_name` v10 → v11) but the `messaging_client_edit` history row never landed: `inquiry_action_log.actor_user_id` and `tenant_id` are NOT NULL, the guest inquiry has `client_user_id = null`, and the insert result was discarded (the fake admin in the unit test enforces no constraints, so the gate was green). Fix: `renameClientContact` writes the log row only when a client user exists and always stamps `tenant_id`; `messagingClientRename` posts a thread line "Name updated to X (was Y)." through the existing `clientText` writer so staff read the change in the thread whether or not a log row exists. Test added for the guest case.
D-MSG-221 · 2026-09-19 (Front Door Chat v2 F03 live proof) · On the fixture restaurant the Items view drew "In your inquiry: Nothing picked yet" above five real lines (one "you chose") and "Saved: Save talent you like" under it. Both are the talent cart and the favourites shelf, which a business that does not sell people cannot fill. `GuestDockLineupView` now hides both people shelves when `representsPeople` is false and they are empty (`showPeopleShelves`); the catalog, the conversation's items and holds remain. No copy or reader changes.
D-MSG-222 · 2026-09-19 (Front Door Chat v2 F03 production check) · On tulala.digital (release c099125b5) the dock's Talent catalog offered six profiles the public directory hides (Rulo, Ro, Luli, Identity QA-…, Test TalentQA…, Local QA): `loadItemsCatalog` lists the whole `agency_talent_roster` because the staff picker needs it. The guest reader `getGuestItemsCatalog` now applies the directory's own predicate (`deleted_at is null`, `is_publicly_hidden = false`, `is_publicly_listed = true`, `profile_kind <> resource`, as in `fetch-directory-page.ts`) in its existing `talent_profiles` lookup and drops talent rows that fail it. Offerings, sessions and tables unchanged; the staff picker unchanged. Static test pins the predicate.
D-MSG-219 · 2026-09-18 (L13 Front Door Chat v2 P7) · `cards_v5` is fail-open. `GUEST_CHAT_DEFAULTS.cardsV5 = true`; the mapper is `cards_v5 !== false` (same contract as `items_tab`). Migration `20261231276000_guest_chat_cards_v5_default_on.sql` backfills `tenant_guest_chat_settings.cards_v5 = true WHERE cards_v5 IS NULL` so a present-but-null row matches a missing row. Explicit false stays legacy bubbles. Admin Guest chat drawer drops "(beta)" and defaults the toggle on. Proof of default-on is on qa-journeys (fixture), not Impronta (AGENTS.md).
D-MSG-223 · 2026-09-20 (Front Door Chat v2 P4 live proof) · Staff "Send times" reached the dock as a plain "Times" bubble. `VISIBLE_KINDS` in `guest-chat-actions.ts` (the guest thread reader) listed only the legacy kinds plus `offer_event`/`payment_request`; every other v5 card kind (`professional_times`, `offer_review`, `offer_state`, `service_card`, `class_card`, `tickets_card`, `menu_options`, `basket`, `order_confirmation`, `appointment_confirmation`, `change_request`, `change_result`) was collapsed to `"text"` before `isClientCardKind` ever saw it, so the contract comment "the reader passes every non-note kind through" was false. This is also the "staff-authored offer_event as bubble" seam ("Offer v3 sent" is the `offer_state` row). Fix: `VISIBLE_KINDS` spreads `CLIENT_CARD_KINDS` from `client-thread-view.ts`. No new writers; file stays under its 1962-line cap (1954).
D-MSG-224 · 2026-09-20 (Front Door Chat v2 F03) · Buy now on a ticket row sent the visitor to the `/events` list. `CatalogRow` gains optional `eventSlug` (read with the event title in `items-catalog.ts`), the guest reader passes it through, and `GuestDockCatalog` builds `/events/<slug>` for a ticket when the event has a slug; the list stays the fallback. Services and classes still go to `/book`. No writer changes.
D-MSG-225 · 2026-09-20 (staff shell, found during P4/P6 live proof) · `/admin/messages?inquiry=<id>` landed on "Pick a conversation": the Orders row link, notifications and the POS switch all build that URL, but the workspace mount (`InboxPage.tsx`) never passed it to `MessagesV5Shell`, whose `initialInquiryId` prop only the POS dock and the preview used. The mount now reads `?inquiry=` (uuid only) and passes it; the shell opens the thread by id whatever the inbox filter shows. No reader or writer change.
D-MSG-226 · 2026-09-20 (Front Door Chat v2 P6 live proof) · Book again on the fixture refused "Could not start a new inquiry": two gates. (1) `no_owner`: an unassigned guest thread (every fresh visitor until staff assign it) has `owner_user_id = null`, and both `messagingClientBookAgain` and the catalog's Add-to-inquiry (when no shared draft exists yet) used it as the POS draft's actor. Both now fall back to the workspace owner via `resolveTenantOwnerId` (the coordinator fallback). (2) A sale paid at the counter flips the record to `confirmed` but never stamps `confirmed_at` on `order_lines`; the writer now treats the record's fulfilment state as the gate and copies every priced line when no line carries a stamp. Old record still untouched. Tests added for both.
D-MSG-227 · 2026-09-20 (Impronta read-only pass) · Impronta's dock tab read "Items · Talent you've added and saved" above a talent lineup. Impronta has no industry preset, so `loadTenantWords` resolves `custom` (`representsPeople: false`); `loadGuestDockFlags` already treats `custom` as people for the cart but passed the raw preset to `chatItemsLabel`, which fell through to "Items". The label now follows the same rule ("Talent & services" / "Talento y servicios"); an operator's own word still wins.
D-MSG-228 · 2026-09-20 (Front Door Chat v2 P4 live proof, staging 1ba69c993) · With D-MSG-223 in, the Times card renders and a pick holds the slot ("held for you · 14:54 left") and the Items shelf draws the hold row with its countdown. But the same slot read 9:45 AM on the card (tenant zone, `America/Mexico_City`, from the payload) and 10:45 AM on the shelf row (browser zone), and the STAFF Times card in the workspace shell also showed 10:00/10:45/11:30 for 15:00Z/15:45Z/16:30Z. Guest side fixed here: `GuestRecordChip` carries the payload `timezone`, `readHoldFromTimes` fills it, `RecordRow` formats with it. SEAM (staff shell, not dock): the workspace `ThreadCards` times card formats in browser time; the same `timeZone` rule belongs there.
D-MSG-229 · 2026-09-20 (Front Door Chat v2 P6 live proof, staging dceaf1e04) · Book again WORKS: new inquiry 45a9b17e (source_context rebook_of_inquiry_id + original_booking_id) with a new `messages` draft carrying the three lines as `proposed_by: client`; the original order stays `paid` with its lines; after a reload the Inquiries tab reads "2 Inquiries" (Waiting on them · 1, Done · 1). Two findings: (a) the Inquiries card for the original thread showed the `Offer $18.00` chip although a paid, confirmed order exists, because `pickRecordChip` took the newest live record; it now ranks by meaning (order/booking/reservation over offer, paid or confirmed over open, newest breaks ties), which also puts Book again on that card. (b) SEAM: right after Book again the dock stayed on Home with the old Inquiries count until reload; the list hook refetches on active-inquiry change, so the switch to the new thread may not be reaching `handleSwitchInquiry` from Home. To reproduce and fix separately.

D-MSG-300 · 2026-09-18 (QA program, staging-qa-journeys) · Auto-ack default copy used an em dash ("Thanks — we'll get back to you within 4 hours."), which violates the product copy rule and fails the Messages QA console/body scan. Fixed code fallbacks in `inquiry-engine-submit.ts`, `admin-workspace-settings.ts`, and `admin/policy/auto-ack/*` to use a comma; migration `20261231277000_auto_ack_no_emdash.sql` updates `agencies.auto_ack_message` DEFAULT + existing rows. Fixture `inquiry_messages` bodies on the QA branch were normalized in place (historical previews). Spec: `web/e2e/qa-program/admin/inbox-load.spec.ts`.

D-MSG-301 · 2026-09-20 (QA program) · Closes the `holdExpiresAt` half of D-MSG-78 for the open thread. `MessagesV5Shell` was hardcoding `holdExpiresAt: null` into `deriveTasks`, so "Hold expired" never surfaced even when a `professional_times` / table card carried an expired TTL. Pure helper `latestHoldExpiresAt` (`lib/messages-v5/hold-expiry-from-messages.ts`) reads the newest private-thread card payload expiry; the shell feeds it to `deriveTasks`. `balanceDueAt`, `reminderDueAt`, and `talentConfirmationsPending` remain null/0 until an essentials reader lands. Specs: unit `hold-expiry-from-messages.test.ts`; e2e `admin/remaining-deep.spec.ts` (pick + hold signal; full next-step flip needs this commit on the QA host).

D-MSG-302 · 2026-09-22 (Round 2 required-assert suite) · Continue-to-offer on the first Needs-action row opened Offer editor in `refused` with "This could not be completed. Try again." — those rows are Lost. Cause: QA inbox surfaces Lost conversations first; `openFirstInboxRow` previously clicked the first row. Harness now skips `\bLost\b` rows and requires an unlocked composer. Spec: `web/e2e/qa-program/_harness.ts` `openFirstInboxRow` / `sendPricedOffer`. Product seam: Continue-to-offer should refuse with a named code (not the generic try-again line) when the conversation is Lost.

D-MSG-303 · 2026-09-22 (Round 2 required-assert suite) · Times sheet on an open QA thread returns 0 slot buttons after person+named service pick (`sendTimesCard` required ≥3). Fixture talent/service availability is empty or unread for the default window. Spec fails loudly (was soft-bail). Fix path: seed bookable hours for QA Journeys Therapist B (or the catalog person the sheet picks) via scheduling seed / UI, then re-prove. Spec: `admin/times-hold-deep.spec.ts`, `admin/remaining-deep.spec.ts`.

D-MSG-304 · 2026-09-22 (Round 2 required-assert suite) · Hold-expired next-step on seeded inquiry `195d4d01-…` showed "Lost" (ladder-12 had marked it lost with reason "QA ladder lost"), which short-circuits `deriveTasks` before Hold expired. Cleared `opportunity_state` on the QA DB and taught the spec to click Reopen when present. Proven green: next-step title matches /hold expired/i on staging-qa-journeys (D-MSG-301 live). Spec: `admin/hold-expired-next.spec.ts`.

D-MSG-305 · 2026-09-22 (Round 2 required-assert suite) · Continue-to-offer after picking a priced Menu row on a fresh conversation opens Offer editor with "No lines yet" / Total $0 while the context panel shows the menu line as added to the shared draft. Save then lands in `sendBlockedError` (button title still a raw `dashboard.adminTabs.lineup.saveErr.*` key). Harness workaround: add a custom priced line when empty, then Save+Send. Root cause: items-picker Continue path not transferring selected catalogue rows into the new offer draft. Spec: `sendPricedOffer` in `_harness.ts`.

D-MSG-306 · 2026-09-22 (Round 2 required-assert suite) · Send times on a brand-new conversation (name+email only) refuses with "Client identity is not confirmed." after picking 3 slots — `holdRefusal` / D21. Required assert surfaces it; times path must run on a thread with confirmed identity (or Capture identity first). Spec: `sendTimesCard` in `_harness.ts`.

D-MSG-307 · 2026-09-22 (Round 2 required-assert suite) · `sendPricedOffer` treated editor-gone / "offer sent" copy as success, so payment-deep minted a client link on Cora Cuevas while the staff stream still showed only "Lines added. Now the offer" (shared draft lines, no `[data-card="offer"]`). Client thread had auto-ack only — no Accept. Fix: require stream offer-card count to increase; payment-deep uses `{ fresh: true }` + `[data-client-action="accept_offer"]`. Spec: `_harness.ts` `sendPricedOffer`, `admin/payment-deep.spec.ts`.

D-MSG-308 · 2026-09-22 (Round 2 required-assert suite) · New conversation → Start repeatedly refused with generic "This could not be completed. Try again." (`unavailable`) while name+email were filled — `messagingStartConversation` → `createInquiryFromIntent` failed on the QA tenant. Harness now surfaces the refusal and retries; payment-deep prefers an Awaiting-acceptance inbox row (or live row + sendPricedOffer) so the Accept→pay-link path is not blocked on New. Root cause of createInquiryFromIntent on QA still open. Spec: `startFreshConversation` / `openAwaitingAcceptanceOrLive` in `_harness.ts`.

D-MSG-309 · 2026-09-22 (Round 2 required-assert suite) · Payment request sheet defaults `amountKind` to `deposit`; when the accepted offer has no deposit rule the Amount radios only show Full/Other and neither is selected, so `canSend` stays false and Send stays disabled even with an Order target + pay-link how. Spec must click Full amount before Send. Spec: `admin/payment-deep.spec.ts`.

D-MSG-310 · 2026-09-22 (Round 2 confirm recheck) · Confirm is not in the Plus tray (`TrayItemKey` has no `confirm`) and next-step only maps `confirm_talent` → confirm. The live doors are `data-times-action="confirm"` (picked Times card), `data-order-action="confirm"` (draft Order card), and `data-table-action="confirm"`. An accepted offer with a reservation stamp and Collect-deposit next-step has no Confirm control until a picked times/draft order card exists. Spec opens Confirm via the Times card door. Seed: inquiry `c5150576-…` (Cora) + confirmed identity + `professional_times` state=selected + overlapping `talent_bookings` on inquiry `45a9b17e-…` for talent `33330003-…0001`. Proven: conflict phase + "no longer free" sentence, no new appointment/project card. Spec: `admin/confirm-recheck-refusal.spec.ts`. Product seam (optional follow-up): add Confirm to the Plus tray or mount `ConfirmOrderButton` when an accepted offer is the confirmable source.

D-MSG-311 · 2026-09-22 (Round 2 double-book) · `confirmRecord` maps `convertToBooking` `requirement_groups_unfulfilled` to generic `unavailable` ("This could not be completed. Try again.") with no shortfall sentence. The Confirm sheet should surface the missing talent-approval shortfall the engine already logged. Spec: `admin/pos-double-book.spec.ts` seed must include accepted `inquiry_approvals` for the offer's talent participant.

D-MSG-312 · 2026-09-22 (Round 2 double-book) · Concurrent confirm on two inquiries for the same talent window both returned `messaging_confirm` success (QA log 21:24:04Z on `88122fb7-…` and `08f2a0b7-…`). The recheck in `confirmFromOffer` is TOCTOU against `convertToBooking` — both pass `runConfirmChecks` before either writes `talent_bookings`. Fix belongs in the convert/recheck path (advisory lock or recheck-inside-RPC), not the sheet. Spec: `admin/pos-double-book.spec.ts` (sequential loser path still required; concurrent race documents this defect).

D-MSG-313 · 2026-09-22 (Round 2 Stripe) · `createPaymentLink` sets `provider = process.env.STRIPE_SECRET_KEY ? "stripe" : "mock"`. Vercel `STRIPE_SECRET_KEY` is **production-target only**; `staging-qa-journeys` (preview / journeys branch) always mints `provider=mock`, so `/pay/<code>` never opens Stripe Checkout (mock uses `?confirm=mock`). Stripe 4242 end-to-end must run on a production-ref host against an agent-owned QA business (not Impronta / not a real tenant). Spec: `admin/stripe-pay-refund.spec.ts`.

D-MSG-314 · 2026-09-22 (Round 2 capacity) · Class / event / table capacity proof specs land under `web/e2e/qa-program/capacity/`. Last-place class (units=1) and Morning fill-to-12 (13th refused) use SQL helpers `releaseLastPlaceClassSeat` / `fillMorningClassToCapacity` in `_isolated-db.ts`. Door tier sold-out reuses C12 pattern + `releaseQaNightDoorSeat`. Table overbook fills `TABLE_GROUP_POOL_ID` to 4 then asserts storefront sentence + Messages Tables busy. Appointment double-book reuses race-B Confirm door (D-MSG-310). SEAM: table LINE Confirm writer still D-MSG-156 — table proof is storefront+Items busy, not Confirm conflict.

D-MSG-315 · 2026-09-22 (Round 2 locale/payment) · `admin/locale-es.spec.ts` no longer soft-bails when the EN/ES/FR control is missing — required. `client/locale-pay-deep.spec.ts` asserts specific ES "Aceptar esta oferta" / FR "Accepter cette offre" (and Pagar after Accept) plus zero raw keys. Payment outside cash and collect-at-counter are separate required specs (`payment-outside.spec.ts`, `payment-collect.spec.ts`); "green soft for all three together" is deleted. Restaurant Items vocabulary required as Menu not Talent & services (`vocabulary-restaurant.spec.ts`) — guards the historical talent-cart-on-restaurant leak.

D-MSG-316 · 2026-09-22 (Round 2 vocabulary) · Restaurant fixture (`industry_preset=restaurant`, `plan_tier=agency`) drew context-panel Items as "Talent & services". Cause: `WorkspaceMessagesPage` (`InboxPage.tsx`) passed `workspaceType` to `MessagesV5Shell` but omitted `industryPreset`, so the shell fell through to `workspaceType === "talent"` → `copy.shell.itemsTalent`. Fix: pass `industryPreset={bridgeTenantIdentity.industryPreset ?? null}`. Spec: `admin/vocabulary-restaurant.spec.ts`.

D-MSG-317 · 2026-09-22 (Round 2 capacity) · Fixture sessions Morning / Last place / QA Night had starts_at in the past (2026-09-18/19), so storefront session_picker and ticket picker showed "no dates" / "No night is on sale yet". Bumped on QA DB to now()+2/3 days. Capacity specs prove Messages Items busy + POS refusal (restaurant homepage no longer mounts session_picker). Spec: `capacity/class-seat-limit.spec.ts`.

D-MSG-318 · 2026-09-22 (Round 2 capacity) · POS walk-in for Morning class when the pool is already at 12 units shows a capacity refusal sentence (required) but also throws Minified React error #310 (hooks order) in the console — a crash on the refusal path. Spec: `capacity/class-seat-limit.spec.ts` "morning filled to 12". Product: sell/chooser render must stay hook-stable when `sold_out` lands mid-sale.

D-MSG-320 · 2026-09-22 (Round 2 hold-live) · Live-hold next-step proof must seed a *future* `holdExpiresAt` on a selected times card. `latestHoldExpiresAt` ignores null expiries and picks the chronologically latest ISO across cards — sending a fresh times card (null expiry) then failing a client pick leaves an older expired selected card in charge, so the bar still reads "Hold expired". Spec now deep-links both directions (`hold-expired-next.spec.ts`); live seed inquiry `45a9b17e-…` kept with holdExpiresAt = now()+7d on QA.
