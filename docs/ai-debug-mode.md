# AI debug mode (Phase 8.7+)

## Audience

**Staff / `super_admin` only** — never expose raw scores or internal signals to public clients.

## Payload (optional)

- Similarity / rank score  
- Signal breakdown (per `ranking_signals.md`)  
- Explanation rule id(s)  
- `search_mode`, active flags  

Gate behind role + optional setting. UI can live in admin directory QA or AI Workspace.
